diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/forms.py helios-server-translation/helios/forms.py
--- helios-server-original/helios/forms.py	2019-10-28 03:15:16.930087172 -0300
+++ helios-server-translation/helios/forms.py	2019-10-29 03:15:07.838263284 -0300
@@ -8,42 +8,44 @@
 from fields import *
 from django.conf import settings
 
+from django.utils.translation import ugettext_lazy as _
+
 
 class ElectionForm(forms.Form):
-  short_name = forms.SlugField(max_length=40, help_text='no spaces, will be part of the URL for your election, e.g. my-club-2010')
-  name = forms.CharField(max_length=100, widget=forms.TextInput(attrs={'size':60}), help_text='the pretty name for your election, e.g. My Club 2010 Election')
-  description = forms.CharField(max_length=4000, widget=forms.Textarea(attrs={'cols': 70, 'wrap': 'soft'}), required=False)
-  election_type = forms.ChoiceField(label="type", choices = Election.ELECTION_TYPES)
-  use_voter_aliases = forms.BooleanField(required=False, initial=False, help_text='If selected, voter identities will be replaced with aliases, e.g. "V12", in the ballot tracking center')
+  short_name = forms.SlugField(max_length=40, label=_("Short Name"), help_text=_('no spaces, will be part of the URL for your election, e.g. my-club-2010'))
+  name = forms.CharField(max_length=100, widget=forms.TextInput(attrs={'size':60}), label=_("Name"), help_text=_('the pretty name for your election, e.g. My Club 2010 Election'))
+  description = forms.CharField(max_length=4000, label=_("Description"), widget=forms.Textarea(attrs={'cols': 70, 'wrap': 'soft'}), required=False)
+  election_type = forms.ChoiceField(label=_("type"), choices = Election.ELECTION_TYPES)
+  use_voter_aliases = forms.BooleanField(required=False, initial=False, label=_("Use voter aliases"), help_text=_('If selected, voter identities will be replaced with aliases, e.g. "V12", in the ballot tracking center'))
   #use_advanced_audit_features = forms.BooleanField(required=False, initial=True, help_text='disable this only if you want a simple election with reduced security but a simpler user interface')
-  randomize_answer_order = forms.BooleanField(required=False, initial=False, help_text='enable this if you want the answers to questions to appear in random order for each voter')
-  private_p = forms.BooleanField(required=False, initial=False, label="Private?", help_text='A private election is only visible to registered voters.')
-  help_email = forms.CharField(required=False, initial="", label="Help Email Address", help_text='An email address voters should contact if they need help.')
+  randomize_answer_order = forms.BooleanField(required=False, initial=False, label=_("Randomize answer order"), help_text=_('enable this if you want the answers to questions to appear in random order for each voter'))
+  private_p = forms.BooleanField(required=False, initial=False, label=_("Private?"), help_text=_('A private election is only visible to registered voters.'))
+  help_email = forms.CharField(required=False, initial="", label=_("Help Email Address"), help_text=_('An email address voters should contact if they need help.'))
   
   if settings.ALLOW_ELECTION_INFO_URL:
-    election_info_url = forms.CharField(required=False, initial="", label="Election Info Download URL", help_text="the URL of a PDF document that contains extra election information, e.g. candidate bios and statements")
+    election_info_url = forms.CharField(required=False, initial="", label=_("Election Info Download URL"), help_text=_("the URL of a PDF document that contains extra election information, e.g. candidate bios and statements"))
   
   # times
-  voting_starts_at = SplitDateTimeField(help_text = 'UTC date and time when voting begins',
+  voting_starts_at = SplitDateTimeField(label=_("Voting starts at"), help_text =_('UTC date and time when voting begins'),
                                    widget=SplitSelectDateTimeWidget, required=False)
-  voting_ends_at = SplitDateTimeField(help_text = 'UTC date and time when voting ends',
+  voting_ends_at = SplitDateTimeField(label=_("Voting ends at"), help_text = _('UTC date and time when voting ends'),
                                    widget=SplitSelectDateTimeWidget, required=False)
 
 class ElectionTimeExtensionForm(forms.Form):
-  voting_extended_until = SplitDateTimeField(help_text = 'UTC date and time voting extended to',
+  voting_extended_until = SplitDateTimeField(label=_("Voting extended until"), help_text = _('UTC date and time voting extended to'),
                                    widget=SplitSelectDateTimeWidget, required=False)
   
 class EmailVotersForm(forms.Form):
-  subject = forms.CharField(max_length=80)
-  body = forms.CharField(max_length=4000, widget=forms.Textarea)
-  send_to = forms.ChoiceField(label="Send To", initial="all", choices= [('all', 'all voters'), ('voted', 'voters who have cast a ballot'), ('not-voted', 'voters who have not yet cast a ballot')])
+  subject = forms.CharField(label=_("Subject"), max_length=80)
+  body = forms.CharField(label=_("Body"), max_length=4000, widget=forms.Textarea)
+  send_to = forms.ChoiceField(label=_("Send To"), initial="all", choices= [('all', _('all voters')), ('voted', _('voters who have cast a ballot')), ('not-voted', _('voters who have not yet cast a ballot'))])
 
 class TallyNotificationEmailForm(forms.Form):
-  subject = forms.CharField(max_length=80)
-  body = forms.CharField(max_length=2000, widget=forms.Textarea, required=False)
-  send_to = forms.ChoiceField(label="Send To", choices= [('all', 'all voters'), ('voted', 'only voters who cast a ballot'), ('none', 'no one -- are you sure about this?')])
+  subject = forms.CharField(label=_("Subject"), max_length=80)
+  body = forms.CharField(label=_("Body"), max_length=2000, widget=forms.Textarea, required=False)
+  send_to = forms.ChoiceField(label=_("Send To"), choices= [('all', _('all voters')), ('voted', _('only voters who cast a ballot')), ('none', _('no one -- are you sure about this?'))])
 
 class VoterPasswordForm(forms.Form):
-  voter_id = forms.CharField(max_length=50, label="Voter ID")
+  voter_id = forms.CharField(max_length=50, label=_("Voter ID"))
   password = forms.CharField(widget=forms.PasswordInput(), max_length=100)
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/media/static_templates/pt-BR/question.html helios-server-translation/helios/media/static_templates/pt-BR/question.html
--- helios-server-original/helios/media/static_templates/pt-BR/question.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/media/static_templates/pt-BR/question.html	2019-10-29 03:15:07.837263284 -0300
@@ -0,0 +1,132 @@
+
+{#foreach $T.questions as question}
+<div id="q_view_{$T.question$index}">
+<h4>
+{#if $T.admin_p}[
+{#if $T.question$index > 0}<a href="javascript:question_move_up({$T.question$index});">^</a>]&nbsp;[{#/if}
+<a onclick="return confirm('Deseja realmente excluir esse pleito?');" href="javascript:question_remove({$T.question$index})">x</a>] [<a href="javascript:question_edit({$T.question$index})">editar</a>] {#/if}{$T.question$index + 1}. {$T.question.question} (escolha entre {$T.question.min} e {#if $T.question.max != null}{$T.question.max}{#else}unlimited{#/if} opções, {$T.question.result_type}.)</h4>
+<ul>
+{#foreach $T.question.answers as answer}
+<li> {$T.answer}
+{#if $T.question.answer_urls[$T.answer$index]}
+  [<a target="_new" href="{$T.question.answer_urls[$T.answer$index]}">more</a>]
+{#/if}
+</li>
+{#/for}
+</ul>
+</div>
+<div id="q_edit_{$T.question$index}" style="display:none;">
+<form id="question_edit_{$T.question$index}_form" onsubmit="question_update({$T.question$index}, this); return false;" action="#">
+<p>
+<!--
+  Type of Question:
+  <select name="choice_type">
+    <option selected>approval</option>
+  </select>
+-->
+<input type="hidden" name="choice_type" value="approval" />
+<b>{$T.question$index + 1}.</b> &nbsp;&nbsp;&nbsp;Escolha entre &nbsp;&nbsp;
+<select name="min">
+    <option selected>0</option>
+{#for option_index = 1 to 20}
+    <option>{$T.option_index}</option>
+{#/for}
+</select>
+
+&nbsp;&nbsp; e  &nbsp;&nbsp;
+
+<select name="max">
+    <option>0</option>
+    <option selected>1</option>
+{#for option_index = 2 to 50}
+    <option>{$T.option_index}</option>
+{#/for}
+    <option value="">- (approval)</option>
+</select>
+
+&nbsp;&nbsp;
+opções.
+
+&nbsp;&nbsp;
+&nbsp;&nbsp;
+Tipo:&nbsp;
+<select name="result_type">
+    <option selected>absoluto</option>
+    <option>relativo</option>
+</select>
+
+</p>
+
+<table id="answer_table_{$T.question$index}">
+  <tbody>
+    <tr><th colspan="2">Pleito:</th><td><input type="text" name="question" size="70" /></td></tr>
+    <tr><th>&nbsp;</th><th>&nbsp;</th><th>&nbsp;</th></tr>
+  </tbody>
+  <tfoot>
+    <tr><th colspan="2"></th><th><a href="javascript:add_answers($('#answer_table_{$T.question$index}'),5)">adicionar 5 opções</a></th></tr>
+    <tr><td colspan="3"><input type="submit" value="atualizar" /> &nbsp; <input type="reset" value="cancelar" onclick="question_edit_cancel({$T.question$index});" /></td></tr>
+  </tfoot>
+
+</table>
+</form>
+</div>
+{#else}
+nenhum pleito adicionado até o momento
+{#/for}
+
+{#if $T.admin_p}
+<h4>Adicionar Pleito:</h4>
+<form id="question_form" onsubmit="question_add(this); return false;" action="#">
+<p>
+<!--
+  Type of Question:
+  <select name="choice_type">
+    <option selected>approval</option>
+  </select>
+-->
+<input type="hidden" name="choice_type" value="approval" />
+&nbsp;&nbsp;&nbsp;Escolha entre &nbsp;&nbsp;
+<select name="min">
+    <option selected>0</option>
+{#for option_index = 1 to 20}
+    <option>{$T.option_index}</option>
+{#/for}
+</select>
+
+&nbsp;&nbsp; e  &nbsp;&nbsp;
+
+<select name="max">
+    <option>0</option>
+    <option selected>1</option>
+{#for option_index = 2 to 50}
+    <option>{$T.option_index}</option>
+{#/for}
+    <option value="">- (approval)</option>
+</select>
+
+&nbsp;&nbsp;
+opções.
+
+&nbsp;&nbsp;
+&nbsp;&nbsp;
+Tipo:&nbsp;
+<select name="result_type">
+    <option selected>absoluto</option>
+    <option>relativo</option>
+</select>
+
+</p>
+
+<table id="answer_table" style="width:100%;">
+  <tbody>
+    <tr><th colspan="2">Pleito:</th><td><input type="text" name="question" size="70" /></td></tr>
+    <tr><th>&nbsp;</th><th>&nbsp;</th><th>&nbsp;</th></tr>
+  </tbody>
+  <tfoot>
+    <tr><th colspan="2"></th><th><a href="javascript:add_answers($('#answer_table'), 5)">adicionar 5 opções</a></th></tr>
+    <tr><td colspan="2"><input type="submit" value="adicionar pleito" /></td></tr>
+  </tfoot>
+
+</table>
+{#/if}
+</form>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/models.py helios-server-translation/helios/models.py
--- helios-server-original/helios/models.py	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/models.py	2019-10-29 03:15:07.833263284 -0300
@@ -20,6 +20,8 @@
 
 from helios import datatypes
 
+from django.utils.translation import ugettext_lazy as _
+from django.utils.translation import ugettext_noop as ugettext_noop
 
 # useful stuff in helios_auth
 from helios_auth.models import User, AUTH_SYSTEMS
@@ -49,8 +51,8 @@
   name = models.CharField(max_length=250)
   
   ELECTION_TYPES = (
-    ('election', 'Election'),
-    ('referendum', 'Referendum')
+    (ugettext_noop('election'), _('Election')),
+    (ugettext_noop('referendum'), _('Referendum'))
     )
 
   election_type = models.CharField(max_length=250, null=False, default='election', choices = ELECTION_TYPES)
@@ -320,7 +322,7 @@
   @property
   def pretty_eligibility(self):
     if not self.eligibility:
-      return "Anyone can vote."
+      return _("Anyone can vote.")
     else:
       return_val = "<ul>"
       
@@ -329,7 +331,7 @@
           for one_constraint in constraint['constraint']:
             return_val += "<li>%s</li>" % AUTH_SYSTEMS[constraint['auth_system']].pretty_eligibility(one_constraint)
         else:
-          return_val += "<li> any %s user</li>" % constraint['auth_system']
+          return_val += _("<li> any %s user</li>") % _(constraint['auth_system'])
 
       return_val += "</ul>"
 
@@ -355,27 +357,27 @@
     if self.questions == None or len(self.questions) == 0:
       issues.append(
         {'type': 'questions',
-         'action': "add questions to the ballot"}
+         'action': _("add questions to the ballot")}
         )
   
     trustees = Trustee.get_by_election(self)
     if len(trustees) == 0:
       issues.append({
           'type': 'trustees',
-          'action': "add at least one trustee"
+          'action': _("add at least one trustee")
           })
 
     for t in trustees:
       if t.public_key == None:
         issues.append({
             'type': 'trustee keypairs',
-            'action': 'have trustee %s generate a keypair' % t.name
+            'action': _('have trustee %s generate a keypair') % t.name
             })
 
     if self.voter_set.count() == 0 and not self.openreg:
       issues.append({
           "type" : "voters",
-          "action" : 'enter your voter list (or open registration to the public)'
+          "action" : _('enter your voter list (or open registration to the public)')
           })
 
     return issues    
@@ -480,7 +482,7 @@
 
     # password is now separate, not an explicit voter type
     if self.voter_set.filter(user=None).count() > 0:
-      voter_types.append('password')
+      voter_types.append(ugettext_noop('password'))
     else:
       # no password users, remove password from the possible auth systems
       if 'password' in auth_systems:
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/_castconfirm_password.html helios-server-translation/helios/templates/_castconfirm_password.html
--- helios-server-original/helios/templates/_castconfirm_password.html	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/templates/_castconfirm_password.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,4 +1,6 @@
-Please provide the voter ID and password you received by email.<br /><br />
+{% load i18n %}
+
+{% trans "Please provide the voter ID and password you received by email." %}<br /><br />
 <form method="post" action="{% url "helios.views.password_voter_login" election.uuid %}" onsubmit="show_waiting()">
 <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
 <input type="hidden" name="return_url" value="{{return_url}}" />
@@ -8,16 +10,16 @@
 </table>
 {% if bad_voter_login %}
 <p style="color: red;">
-bad voter ID or password, please try again.
+{% trans "bad voter ID or password, please try again." %}
 </p>
 {% endif %}
 {% if cast_ballot == "1" %}
 <p class="small">
-You may cast as many ballots as you wish:<br />only the last one counts.
+{% trans "You may cast as many ballots as you wish:<br />only the last one counts." %}
 </p>
-<input type="submit" class="button" value="cast ballot" />
+<input type="submit" class="button" value="{% trans "cast ballot" %}" />
 {% else %}
-<input type="submit" class="button" value="log in" />
+<input type="submit" class="button" value="{% trans "log in" %}" />
 {% endif %}
 </form>
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/cast_done.html helios-server-translation/helios/templates/cast_done.html
--- helios-server-original/helios/templates/cast_done.html	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/templates/cast_done.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,26 +1,28 @@
 {% extends TEMPLATE_BASE %}
-{% block title %}Confirm Vote{% endblock %}
+{% load i18n %}
+
+{% block title %}{% trans "Confirm Vote" %}{% endblock %}
 
 {% block content %}
-<h1>{{election.name}} &mdash; Vote Successfully Cast!</h1>
+<h1>{{election.name}} &mdash; {% trans "Vote Successfully Cast!" %}</h1>
 
 <p>
-    Congratulations, your vote has been <b><u>successfully cast</u></b>!
+    {% trans "Congratulations, your vote has been <b><u>successfully cast</u></b>!" %}
 </p>
 
 <p>
-  Your smart ballot tracker is:<br /><br />
+  {% trans "Your smart ballot tracker is:" %}<br /><br />
     <tt style="font-size:1.8em; font-weight: bold; padding-left: 20px;">  {{vote_hash}}</tt>
 </p>
 
 {% if logout %}
-<p><b>For your safety, we have logged you out.</b></p>
+<p><b>{% trans "For your safety, we have logged you out." %}</b></p>
 <iframe width="0" height="0" border="0" frameborder="0" src="/auth/logout">
 </iframe>
 {% endif %}
 
 <p style="font-size: 1.4em;">
-[ <a href="{% url "helios.views.one_election_view" election.uuid %}">return to election info</a> ]
+[ <a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "return to election info" %}</a> ]
 </p>
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/castvote.html helios-server-translation/helios/templates/castvote.html
--- helios-server-original/helios/templates/castvote.html	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/templates/castvote.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,11 +1,12 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block title %}{{cast_vote.vote_tinyhash}} &mdash; {{election.name}}{% endblock %}
 {% block content %}
-<h2 class="title">Cast Vote {{cast_vote.vote_tinyhash}}</h2>
-cast in <a href="{% url "helios.views.one_election_view" election.uuid %}">{{election.name}}</a><br />
-Fingerprint: <tt>{{cast_vote.vote_hash}}</tt><br />
-by <b><u>
+<h2 class="title">{% trans "Cast Vote" context "vote id follows" %} {{cast_vote.vote_tinyhash}}</h2>
+{% trans "cast in" context "election name follows" %} <a href="{% url "helios.views.one_election_view" election.uuid %}">{{election.name}}</a><br />
+{% trans "Fingerprint:" %} <tt>{{cast_vote.vote_hash}}</tt><br />
+{% trans "by" context "voter name follows" %} <b><u>
 {% if the_voter.alias %}
 {{the_voter.alias}}
 {% else %}
@@ -13,7 +14,7 @@
 {% endif %}
 </u></b>
 <br /><br />
-<a href="#" onclick="$('#castvote_content').slideToggle(250); return false;">details</a><br /><br />
+<a href="#" onclick="$('#castvote_content').slideToggle(250); return false;">{% trans "details" %}</a><br /><br />
 <div style="display:none;" id="castvote_content">
 <textarea cols="100" rows="10">
 {{vote_content}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/combine_decryptions.html helios-server-translation/helios/templates/combine_decryptions.html
--- helios-server-original/helios/templates/combine_decryptions.html	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/templates/combine_decryptions.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,16 +1,17 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Compute Tally &mdash; {{election.name}}{% endblock %}
+{% block title %}{% trans "Compute Tally" %} &mdash; {{election.name}}{% endblock %}
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Compute Tally <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">cancel</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Compute Tally" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "cancel" %}</a>]</span></h2>
 
   <p>
-    You are about to compute the tally for this election. You only will then see the results.
+    {% trans "You are about to compute the tally for this election. You only will then see the results." %}
   </p>
 
   <form method="POST" action="">
     <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
-    <input type="submit" class="button" value="compute the tally!" />
+    <input type="submit" class="button" value="{% trans "compute the tally!" %}" />
   </form>
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_audited_ballots.html helios-server-translation/helios/templates/election_audited_ballots.html
--- helios-server-original/helios/templates/election_audited_ballots.html	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/templates/election_audited_ballots.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,25 +1,26 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Audited Ballots for {{election.name}}{% endblock %}
+{% block title %}{% trans "Audited Ballots for" context "name follows" %} {{election.name}}{% endblock %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Audited Ballots <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election_uuid=election.uuid %}">back to election</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Audited Ballots" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election_uuid=election.uuid %}">{% trans "back to election" %}</a>]</span></h2>
 
 <p>
-When you prepare a ballot with Helios, you immediately receive a smart ballot tracker. Before you choose to cast that ballot, you have the option to ask Helios to "break open" that encrypted ballot and verify that Helios encrypted your ballot correctly. Once that's done, you can post that opened ballot here, on the audited ballots' list, for everyone to verify (your identity is not included). Once you've done this, you have to re-encrypt your choices and obtain a different smart ballot tracker. This helps reduce the chance that someone might coerce you to vote differently from your true choice.
+{% blocktrans %}When you prepare a ballot with Helios, you immediately receive a smart ballot tracker. Before you choose to cast that ballot, you have the option to ask Helios to "break open" that encrypted ballot and verify that Helios encrypted your ballot correctly. Once that's done, you can post that opened ballot here, on the audited ballots' list, for everyone to verify (your identity is not included). Once you've done this, you have to re-encrypt your choices and obtain a different smart ballot tracker. This helps reduce the chance that someone might coerce you to vote differently from your true choice.{% endblocktrans %}
 </p>
 
 <p>
-These ballots are <em>not cast</em>, and they will not be counted. They are just here for auditing purposes, to spot-check that Helios is properly encrypting voter's choices.
+{% blocktrans %}These ballots are <em>not cast</em>, and they will not be counted. They are just here for auditing purposes, to spot-check that Helios is properly encrypting voter's choices.{% endblocktrans %}
 </p>
 
 <p>
-  To verify an audited ballot, copy its entire content and paste it in the <a target="_new" href="/booth/single-ballot-verify.html?election_url={% url "helios.views.one_election" election.uuid %}">single ballot verifier</a>.
+  {% trans "To verify an audited ballot, copy its entire content and paste it in the" context "ballot verifier name follows" %} <a target="_new" href="/booth/single-ballot-verify.html?election_url={% url "helios.views.one_election" election.uuid %}">{% trans "single ballot verifier" %}</a>.
 </p>
 
 {% if audited_ballots %}
 <p>
-Audited Ballots {{offset_plus_one}} - {{offset_plus_limit}} &nbsp;&nbsp;
+{% trans "Audited Ballots" %} {{offset_plus_one}} - {{offset_plus_limit}} &nbsp;&nbsp;
 </p>
 
 {% if next_after %}
@@ -31,15 +32,15 @@
 <a href="?">back to start</a> &nbsp;&nbsp;
 {% endifequal %}
 {% if more_p %}
-<a href="?after={{next_after}}&offset={{next_offset}}">next {{limit}}</a>
+<a href="?after={{next_after}}&offset={{next_offset}}">{% trans "next" context "number of ballots follows" %} {{limit}}</a>
 {% endif %}
 <table class="pretty">
 {% for b in audited_ballots %}
-<tr><td><tt style="font-size: 1.4em;">{{b.vote_hash}}</tt> [<a target="_blank" href="?vote_hash={{b.vote_hash|urlencode}}">view</a>]</td></tr>
+<tr><td><tt style="font-size: 1.4em;">{{b.vote_hash}}</tt> [<a target="_blank" href="?vote_hash={{b.vote_hash|urlencode}}">{% trans "view" %}</a>]</td></tr>
 {% endfor %}
 </table>
 {% else %}
-<em>no audited ballots yet</em>
+<em>{% trans "no audited ballots yet" %}</em>
 {% endif %}
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_cast_confirm.html helios-server-translation/helios/templates/election_cast_confirm.html
--- helios-server-original/helios/templates/election_cast_confirm.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_cast_confirm.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,5 +1,7 @@
 {% extends TEMPLATE_BASE %}
-{% block title %}Confirm Vote{% endblock %}
+{% load i18n %}
+
+{% block title %}{% trans "Confirm Vote" %}{% endblock %}
 
 {% block content %}
 <script language="javascript">
@@ -24,7 +26,7 @@
     evt = window.event;
   }
   
-  var message = "You have not yet cast your ballot! Make sure to complete the voting process if you want your vote to count.";
+  var message = {% trans "You have not yet cast your ballot! Make sure to complete the voting process if you want your vote to count." %};
 
   if (evt) {
     evt.returnValue = message;
@@ -34,10 +36,10 @@
 };
 
 </script>
-<p style="margin-top: 10px">Submitting your vote for <b>{{election.name}}</b>...</p>
+<p style="margin-top: 10px">{% trans "Submitting your vote for" context "name follows" %} <b>{{election.name}}</b>...</p>
 
 <div id="waiting_div">
-    Verifying and Casting your ballot<br />
+    {% trans "Verifying and Casting your ballot" %}<br />
     <img src="/static/helios/loading.gif" />
 </div>
 
@@ -51,10 +53,10 @@
 
 {% if show_password %}
 {% if user %}
-<h1>Wait!</h1>
+<h1>{% trans "Wait!" %}</h1>
 <p>
-You are logged in as <u>{{user.display_html_small|safe}}</u>, but this election<br />
-requires election-specific credentials.
+{% trans "You are logged in as" context "name follows" %} <u>{{user.display_html_small|safe}}</u>, {% trans "but this election" context "complement follows" %}<br />
+{% trans "requires election-specific credentials." %}
 </p>
 {% endif %}
 {% with cast_ballot="1" %}
@@ -64,16 +66,16 @@
 {% else %}
 
 {% if user %}
-<h1>Sorry!<h1>
-<b>Sorry, you are <em>
+<h1>{% trans "Sorry!" %}<h1>
+<b>{% trans "Sorry, you are" %} <em>
   {% if election.openreg %}
-    <u>not eligible</u></em> for this election.
+    {% trans "<u>not eligible</u></em> for this election." %}
   {% else %}
-    not registered</em> for this election, and registration is closed.
+    {% trans "not registered</em> for this election, and registration is closed." %}
   {% endif %}
   </b><br /></p>
 <p>
-    [<a href="{% url "helios.views.one_election_view" election.uuid %}">return to the main election page</a>]
+    [<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "return to the main election page" %}</a>]
 </p>
 {% else %}
 <p>
@@ -99,7 +101,7 @@
 
 {% endif %}
 <br />
-Your smart ballot tracker is:<br /><br />
+{% trans "Your smart ballot tracker is:" %}<br /><br />
     <tt style="font-size:1.3em; font-weight: bold; padding-left: 20px;">  {{vote_fingerprint}}</tt>
 </p>
 {# this closes the IF ELSE of this being password_only #}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_compute_tally.html helios-server-translation/helios/templates/election_compute_tally.html
--- helios-server-original/helios/templates/election_compute_tally.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_compute_tally.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,30 +1,31 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Compute Encryted Tally for {{election.name}}{% endblock %}
+{% block title %}{% trans "Compute Encryted Tally for" context "name follows" %} {{election.name}}{% endblock %}
 
 {% block content %}
-  <h2 class="title">Compute Tally for Election: {{election.name}}</h2>
+  <h2 class="title">{% trans "Compute Tally for Election:" context "name follows" %} {{election.name}}</h2>
 
 <div id="instructions">
 {% if election.num_cast_votes %}
 <p>
-    You are about to compute the encrypted tally for election <b>{{election.name}}</b>.
+    {% trans "You are about to compute the encrypted tally for election" context "name follows" %} <b>{{election.name}}</b>.
 </p>
 
 <p>
-    Once you do this, voters will no longer be able to cast a ballot.
+    {% trans "Once you do this, voters will no longer be able to cast a ballot." %}
 </p>
 
-<form method="post" action="" onsubmit="alert('ok, tally has begun')" class="pretty">
+<form method="post" action="" onsubmit="alert({% trans "'ok, tally has begun'" %})" class="pretty">
 <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
     
-<input class="button" type="submit" value="compute encrypted tally!" />
-<button onclick="document.location='./view'; return false;">never mind</button>
+<input class="button" type="submit" value="{% trans "compute encrypted tally!" %}" />
+<button onclick="document.location='./view'; return false;">{% trans "never mind" %}</button>
 </form>
 {% else %}
 <p>
-No votes have been cast in this election. At least one vote must be cast before you compute the tally.<br /><br />
-<a href="./view">back to election</a>
+{% trans "No votes have been cast in this election. At least one vote must be cast before you compute the tally." %}<br /><br />
+<a href="./view">{% trans "back to election" %}</a>
 </p>
 {% endif %}
 </div>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_edit.html helios-server-translation/helios/templates/election_edit.html
--- helios-server-original/helios/templates/election_edit.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_edit.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,8 +1,9 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
 
-  <h2 class="title">{{election.name}} &mdash; Update <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">cancel</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Update" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "cancel" %}</a>]</span></h2>
   
 {% if error %}
 <p style="color: red;">
@@ -16,7 +17,7 @@
      {{election_form.as_table}}
      </table>
   <div>
-  <label for="">&nbsp;</label><input type="submit" value="Update" id="update_button" />
+  <label for="">&nbsp;</label><input type="submit" value={% trans "Update" %} id="update_button" />
   </div>
   </form>
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_freeze.html helios-server-translation/helios/templates/election_freeze.html
--- helios-server-original/helios/templates/election_freeze.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_freeze.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,42 +1,43 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Freeze Ballot</h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Freeze Ballot" %}</h2>
 <p>
-Once the ballot is frozen, the questions and options can no longer be modified.<br />
-The list of trustees and their public keys will also be frozen.
+{% trans "Once the ballot is frozen, the questions and options can no longer be modified." %}<br />
+{% trans "The list of trustees and their public keys will also be frozen." %}
 </p>
 
 <p>
 {% if election.openreg %}
-Registration for your election is currently <b>open</b>, which means anyone can vote, even after you freeze the ballot.
+{% trans "Registration for your election is currently <b>open</b>, which means anyone can vote, even after you freeze the ballot." %}
 {% else %}
-Registration for your election is currently <b>closed</b>, which means only the voters you designate will be able to cast a ballot. As the administrator, you will still be able to modify that voter list as the election progresses.
+{% trans "Registration for your election is currently <b>closed</b>, which means only the voters you designate will be able to cast a ballot. As the administrator, you will still be able to modify that voter list as the election progresses." %}
 {% endif %}
 </p>    
 
 {% if helios.VOTERS_EMAIL %}
 <p>
-You must freeze the ballot before you can contact voters.
+{% trans "You must freeze the ballot before you can contact voters." %}
 </p>
 {% endif %}
 
 {% if issues_p %}
 <p>
-    Before you can freeze the election, you will need to:
+    {% trans "Before you can freeze the election, you will need to:" %}
     <ul>
         {% for issue in issues %}
         <li>{{issue.action}}</li>
         {% endfor %}
     </ul>
-    <a href="{% url "helios.views.one_election_view" election.uuid %}">go back to the election</a>
+    <a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "go back to the election" %}</a>
 </p>
 {% else %}
 <form method="post" action="">
 <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
     
-<input class="button" type="submit" value="Freeze the ballot" />
-<button onclick="document.location='./view'; return false;">never mind</button>
+<input class="button" type="submit" value="{% trans "Freeze the ballot" %}" />
+<button onclick="document.location='./view'; return false;">{% trans "never mind" %}</button>
 </form>
 {% endif %}
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_keygenerator.html helios-server-translation/helios/templates/election_keygenerator.html
--- helios-server-original/helios/templates/election_keygenerator.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_keygenerator.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,4 +1,5 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 {% block content %}
 <script language="javascript">
 
@@ -99,79 +100,79 @@
 
 </script>
 
-<h2 class="title">{{election.name}} &mdash; Trustee {{trustee.name}} &mdash; Key Setup</h2>
+<h2 class="title">{{election.name}} &mdash; {% trans "Trustee" %} {{trustee.name}} &mdash; {% trans "Key Setup" %}</h2>
 
 <p>
-  As a trustee, it's time to set up your key for this election.
+  {% trans "As a trustee, it's time to set up your key for this election." %}
 </p>
 
 <p id="waiting_message">
-  Please wait for the generator to load...
+  {% trans "Please wait for the generator to load..." %}
 </p>
 
 <p id="generator">
 
-<span id="buttons"><button onclick="generate_keypair(); return false;" id="generate_button">Generate Election Keys</button></span>
+<span id="buttons"><button onclick="generate_keypair(); return false;" id="generate_button">{% trans "Generate Election Keys" %}</button></span>
 
 <br />
-If you've already generated a keypair, you can <a href="javascript:show_key_reuse()">reuse it</a>.
+{% trans "If you've already generated a keypair, you can" context "action follows" %} <a href="javascript:show_key_reuse()">{% trans "reuse it" context "action" %}</a>.
 </p>
 
 <div id="reuse">
-<h3>Reusing a Key</h3>
+<h3>{% trans "Reusing a Key" %}</h3>
 
 <p>
-Enter your complete secret key below:
+{% trans "Enter your complete secret key below:" %}
 </p>
 <form onsubmit="reuse_key(this.secret_key.value); return false;">
 <textarea cols="80" rows="5" wrap="soft" name="secret_key">
 </textarea>
 <br />
-<input type="submit" value="reuse" />
+<input type="submit" value="{% trans "reuse" %}" />
 </form>
 </div>
 
 <div id="sk_download">
-<h3>Your Secret Key</h3>
+<h3>{% trans "Your Secret Key" %}</h3>
 <span id="clear_button">
-Your key has been generated, but you may choose to<br /><a href="javascript:clear_keys();">clear it from memory and start from scratch</a> if you prefer.<br />
+{% trans "Your key has been generated, but you may choose to" context "action follows" %}<br /><a href="javascript:clear_keys();">{% trans "clear it from memory and start from scratch" context "action" %}</a> {% trans "if you prefer." %}<br />
 </span>
 
 <p>
-    <button style="font-size:16pt;" onclick="download_sk(); $('#pk_link').show();">Show my secret key</button>
+    <button style="font-size:16pt;" onclick="download_sk(); $('#pk_link').show();">{% trans "Show my secret key" %}</button>
 </p>
 </div>
 
 <div style="display:none;" id="pk_content">
-    <p>Bellow is your trustee secret key content. Please copy its content and save it securely. <br>
-       You can also click to dowload it to a file.
-       And please don't lose it! Otherwise it will not be possible to decrypt the election tally.<br>
+    <p>{% trans "Bellow is your trustee secret key content. Please copy its content and save it securely." %} <br>
+       {% trans "You can also click to dowload it to a file." %}
+       {% trans "And please don't lose it! Otherwise it will not be possible to decrypt the election tally." %}<br>
     </p>
     <textarea id="sk_content" rows="5" wrap="soft" cols="50" style="height: 25em;"></textarea>
 </div>
 
 <div style="display:none;" id="pk_link">
 <p>
-<a id="download_to_file" href="javascript:download_sk_to_file('trustee_key_for_{{election.name}}.txt');">download private key to a file</a>
+<a id="download_to_file" href="javascript:download_sk_to_file('trustee_key_for_{{election.name}}.txt');">{% trans "download private key to a file" %}</a>
 </p>
 <p>
-  <a href="javascript:show_pk();">ok, I've saved the key, let's move on</a>
+  <a href="javascript:show_pk();">{% trans "ok, I've saved the key, let's move on" %}</a>
 </p>
 </div>
 
 <form method="post" id="pk_form" action="{% url "helios.views.trustee_upload_pk" election.uuid trustee.uuid %}">
-<h3>Your Public Key</h3>
+<h3>{% trans "Your Public Key" %}</h3>
 <p>
-    It's time to upload the public key to the server.
+    {% trans "It's time to upload the public key to the server." %}
 </p>
 <p>
-    The fingerprint of your public key is: <tt id="pk_hash" style="font-size: 1.5em; font-weight: bold;"></tt>.<br />
-    You may want to save this to confirm that your public key was properly stored by the server.<br />
-    (Your public key is not currently being displayed because you do not need to save it, the fingerprint is sufficient.)
+    {% trans "The fingerprint of your public key is:" %} <tt id="pk_hash" style="font-size: 1.5em; font-weight: bold;"></tt>.<br />
+    {% trans "You may want to save this to confirm that your public key was properly stored by the server." %}<br />
+    {% trans "(Your public key is not currently being displayed because you do not need to save it, the fingerprint is sufficient.)" %}
 </p>
 <textarea id="pk_textarea" name="public_key_json" cols="80" rows="10" style="display:none;">
 </textarea>
-<input type="submit" value="Upload your public key">
+<input type="submit" value="{% trans "Upload your public key" %}">
 </form>
 
 <div id="applet_div"></div>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_new.html helios-server-translation/helios/templates/election_new.html
--- helios-server-original/helios/templates/election_new.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_new.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,8 +1,11 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
 
-  <h2 class="title">Create a New Election</h2>
+{% trans "Next" as next %}
+
+  <h2 class="title">{% trans "Create a New Election" %}</h2>
   
 {% if error %}
 <p style="color: red;">
@@ -16,7 +19,7 @@
      {{election_form.as_table}}
      </table>
   <div>
-  <label for="">&nbsp;</label><input type="submit" value="Next &gt;&gt;" id="next_button" />
+  <label for="">&nbsp;</label><input type="submit" value="{% blocktrans %} {{next}} &gt;&gt;{% endblocktrans %}" id="next_button" />
   </div>
   </form>
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_not_started.html helios-server-translation/helios/templates/election_not_started.html
--- helios-server-original/helios/templates/election_not_started.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_not_started.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,14 +1,15 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
 
-  <h2 class="title">Election {{election.name}} Not Yet Open</h2>
+  <h2 class="title">{% trans "Election" context "name follows" %} {{election.name}} {% trans "Not Yet Open" %}</h2>
 
   <p>
-      This election is not yet open. You probably got here from the Ballot Preview.
+      {% trans "This election is not yet open. You probably got here from the Ballot Preview." %}
   </p>
   
   <p>
-      <a href="{% url "helios.views.one_election_view" election.uuid %}">back to the election</a>
+      <a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to the election" %}</a>
   </p>
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_questions.html helios-server-translation/helios/templates/election_questions.html
--- helios-server-original/helios/templates/election_questions.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_questions.html	2019-10-29 03:15:07.841263284 -0300
@@ -1,8 +1,9 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
-{% block title %}Questions for {{election.name}}{% endblock %}
+{% block title %}{% trans "Questions for" context "election name follows" %} {{election.name}}{% endblock %}
 {% block content %}
-  <h3 class="title">{{election.name}} &mdash; Questions <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h3>
+  <h3 class="title">{{election.name}} &mdash; {% trans "Questions" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h3>
 
   <script language="javascript">
 {% if election.questions %}
@@ -116,7 +117,7 @@
   function add_answer(el) {
     el = $(el);
     var num_answers = el.find('input.answer').length + 1;
-    el.find('tbody').append('<tr><th>&nbsp;&nbsp;&nbsp;</th><th>Answer #' + num_answers + '</th><td><input type="text" class="answer" name="answer_' + num_answers + '" size="70" /><br /><nobr>Link (optional, <tt>http</tt> or <tt>https</tt> only): <input type="url" name="answer_url_' + num_answers + '" width="50%" /></nobr></td></tr>');
+    el.find('tbody').append('<tr><th>&nbsp;&nbsp;&nbsp;</th><th>{% trans "Answer" %} #' + num_answers + '</th><td><input type="text" class="answer" name="answer_' + num_answers + '" size="70" /><br /><nobr>{% trans "Link (optional, <tt>http</tt> or <tt>https</tt> only):" %} <input type="url" name="answer_url_' + num_answers + '" width="50%" /></nobr></td></tr>');
   }
 
   function add_answers(el, num) {
@@ -132,7 +133,22 @@
   }
 
   $(document).ready(function() {
-      $('#questions').setTemplateURL("/static/helios/static_templates/question.html");
+	var lang = navigator.language || navigator.userLanguage;
+	var base = "/static/helios/static_templates/";
+	var file = "/question.html";
+
+	// begin mess; this is for us to check if language specific file exists
+	// this can be a lot better; current situation is work in progress
+	var xhr = new XMLHttpRequest();
+	xhr.open('HEAD', base + lang + file, false);
+	xhr.send();
+
+	if (xhr.status != "404"){
+		// File in desired language exists; use it
+		base += lang;
+	}
+	// end mess; now we know which static file to serve
+	$('#questions').setTemplateURL(base + file);
 
       refresh();
   });
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_tallied.html helios-server-translation/helios/templates/election_tallied.html
--- helios-server-original/helios/templates/election_tallied.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_tallied.html	2019-10-29 03:15:07.842263284 -0300
@@ -1,14 +1,15 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
 
-  <h2 class="title">Election {{election.name}} Already Tallied</h2>
+  <h2 class="title">{% trans "Election" context "election name follows" %} {{election.name}} {% trans "Already Tallied" context "election name before" %}</h2>
 
   <p>
-      This election has already been tallied, you can no longer cast a vote.
+      {% trans "This election has already been tallied, you can no longer cast a vote." %}
   </p>
   
   <p>
-      <a href="{% url "helios.views.one_election_view" election.uuid %}">view the election tally</a>
+      <a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "view the election tally" %}</a>
   </p>
-{% endblock %}
\ No newline at end of file
+{% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/election_view.html helios-server-translation/helios/templates/election_view.html
--- helios-server-original/helios/templates/election_view.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/election_view.html	2019-10-29 03:15:07.842263284 -0300
@@ -1,32 +1,35 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 {% block title %}{{election.name}}{% endblock %}
 {% block content %}
+
 <div style="float: left; margin-right: 50px;">
   <h3 class="title">{{ election.name }}
 {% if admin_p %}
 {% if not election.frozen_at %}
-<small><a class="small button" href="{% url "helios.views.one_election_edit" election.uuid %}">edit</a></small>
+<small><a class="small button" href="{% url "helios.views.one_election_edit" election.uuid %}">{% trans "edit" %}</a></small>
 {% endif %}
 {% endif %}</h3>
 <p style="padding-top:0px; margin-top:0px">
-<em>{% if election.private_p %}private{%else%}public{% endif %}</em> {{ election.election_type }}{% if settings.SHOW_USER_INFO %} created by <u><b>{{election.admin.display_html_small|safe}}</b></u>{% endif %}
+<em>{% if election.private_p %}{% trans "private" context "visibility" %}{%else%}{% trans "public" context "visibility" %}{% endif %}</em> {% trans election.election_type %}{% if settings.SHOW_USER_INFO %} {% trans "created by" %} <u><b>{{election.admin.display_html_small|safe}}</b></u>{% endif %}
 {% if election.is_archived %}
-[archived]
+{% trans "[archived]" %}
 {% endif %}
 {% if admin_p %}
-&nbsp;{% if election.is_archived %}<a class="small button" href="{% url "helios.views.one_election_archive" election_uuid=election.uuid %}?archive_p=0">unarchive it</a>{% else %}<a class="small button" href="{% url "helios.views.one_election_archive" election_uuid=election.uuid %}?archive_p=1">archive it</a>{% endif %}
-<a class="small button" onclick="return window.confirm('Are you sure you want to copy this election?');" href="{% url "helios.views.one_election_copy" election_uuid=election.uuid %}">copy</a>
+&nbsp;{% if election.is_archived %}<a class="small button" href="{% url "helios.views.one_election_archive" election_uuid=election.uuid %}?archive_p=0">{% trans "unarchive it" %}</a>{% else %}<a class="small button" href="{% url "helios.views.one_election_archive" election_uuid=election.uuid %}?archive_p=1">{% trans "archive it" %}</a>{% endif %}
+{% trans "Are you sure you want to copy this election?" as str %}
+<a class="small button" onclick="return window.confirm('{{str}}');" href="{% url "helios.views.one_election_copy" election_uuid=election.uuid %}">{% trans "copy" %}</a>
 {% endif %}
 <br />
 {% if admin_p %}
 {% if not election.private_p %}
 {% if election.featured_p %}
-this {{election.election_type}} is featured on the front page.
+{% trans "this" context "election type" %} {% trans election.election_type %} {% trans "is featured on the front page." %}
 {% if can_feature_p %}
 [<a href="{% url "helios.views.one_election_set_featured" election.uuid %}?featured_p=0">unfeature it</a>]
 {% endif %}
 {% else %}
-this {{election.election_type}} is <u>not</u> featured on the front page.
+{% trans "this" context "election type" %} {% trans election.election_type %} {% trans "is <u>not</u> featured on the front page." %}
 {% if can_feature_p %}
 [<a href="{% url "helios.views.one_election_set_featured" election.uuid %}?featured_p=1">feature it</a>]
 {% endif %}
@@ -51,11 +54,11 @@
 {% endif %}
 
 <p align="center" style="font-size: 1.5em;">
-<a href="{% url "helios.views.one_election_questions" election.uuid %}">questions ({% if election.questions %}{{election.questions|length}}{% else %}0{% endif %})</a>
+<a href="{% url "helios.views.one_election_questions" election.uuid %}">{% trans "questions" %} ({% if election.questions %}{{election.questions|length}}{% else %}0{% endif %})</a>
 &nbsp;&nbsp;|&nbsp;&nbsp;
-<a href="{% url "helios.views.voters_list_pretty" election.uuid %}">voters &amp; ballots</a>
+<a href="{% url "helios.views.voters_list_pretty" election.uuid %}">{% trans "voters &amp; ballots" %}</a>
 &nbsp;&nbsp;|&nbsp;&nbsp;
-<a href="{% url "helios.views.list_trustees_view" election.uuid %}">trustees ({{trustees|length}})</a>
+<a href="{% url "helios.views.list_trustees_view" election.uuid %}">{% trans "trustees" %} ({{trustees|length}})</a>
 </p>
 
 {% if admin_p %}
@@ -79,21 +82,21 @@
 <!-- election complete, no next step -->
 
 {% else %}
-<b>Next Step:</b>
+<b>{% trans "Next Step:" %}</b>
 <span style="font-size: 1.3em;">
 {% if not election.frozen_at %}
 {% if election.issues_before_freeze %}
 {% for issue in election.issues_before_freeze %}
-{{issue.action}}{% if forloop.last %}{% else %}, and{% endif %}<br />
+{{issue.action}}{% if forloop.last %}{% else %}{% trans ", and" context "after each action" %}{% endif %}<br />
 {% endfor %}
 {% else %}
-<a href="{% url "helios.views.one_election_freeze" election.uuid %}">freeze ballot and open election.</a>
+<a href="{% url "helios.views.one_election_freeze" election.uuid %}">{% trans "freeze ballot and open election." %}</a>
 <br />
 {% if election.voting_starts_at %}
-once you do this, the election will be ready for voting and will open automatically<br />
-at {{election.voting_starts_at}}, as per your settings.
+{% trans "once you do this, the election will be ready for voting and will open automatically" %}<br />
+{% trans "at" context "time" %} {{election.voting_starts_at}}, {% trans "as per your settings." %}
 {% else %}
-once you do this, the election will be immediately open for voting.
+{% trans "once you do this, the election will be immediately open for voting." %}
 {% endif %}
 {% endif %}
 
@@ -101,37 +104,37 @@
 
 {% if not election.encrypted_tally %}
 {% if election.tallying_started_at %}
-Tally computation is under way.<br />
-Reload this page in a couple of minutes.
+{% trans "Tally computation is under way." %}<br />
+{% trans "Reload this page in a couple of minutes." %}
 {% else %}
-<a href="{% url "helios.views.one_election_compute_tally" election.uuid %}">compute encrypted tally</a><br />
-The encrypted votes will be combined into an encrypted tally. Once this is done,<br />
-trustees will be asked to provide their share of the decryption.
+<a href="{% url "helios.views.one_election_compute_tally" election.uuid %}">{% trans "compute encrypted tally" %}</a><br />
+{% trans "The encrypted votes will be combined into an encrypted tally. Once this is done,"%}<br />
+{% trans "trustees will be asked to provide their share of the decryption." %}
 {% endif %}
 {% else %}
 
 {% if election.result %}
-<a href="{% url "helios.views.release_result" election.uuid %}">release result</a><br />
-The result displayed below is visible only to you.<br />
-Once you release the result, it will be visible to everyone.
+<a href="{% url "helios.views.release_result" election.uuid %}">{% trans "release result" %}</a><br />
+{% trans "The result displayed below is visible only to you." %}<br />
+{% trans "Once you release the result, it will be visible to everyone." %}
 {% else %}
 
 {% if election.ready_for_decryption_combination %}
 <a href="{% url "helios.views.combine_decryptions" election.uuid %}">
 {% if election.num_trustees == 1 %}
-compute results
+{% trans "compute results" %}
 {% else %}
-combine trustee decryptions and compute results
+{% trans "combine trustee decryptions and compute results" %}
 {% endif %}
 </a><br />
 {% if election.num_trustees == 1 %}
-The result will be computed and shown to you, the administrator, only.
+{% trans "The result will be computed and shown to you, the administrator, only." %}
 {% else %}
-The decryption shares from the trustees will be combined and the tally computed.<br />
-Once you do this, the tally will visible to you, the administrator, only.
+{% trans "The decryption shares from the trustees will be combined and the tally computed." %}<br />
+{% trans "Once you do this, the tally will be visible to you, the administrator, only." %}
 {% endif %}
 {% else %}
-<a href="{% url "helios.views.list_trustees_view" election.uuid %}">trustees (for decryption)</a>
+<a href="{% url "helios.views.list_trustees_view" election.uuid %}">{% trans "trustees (for decryption)" %}</a>
 {% endif %}
 
 {% endif %}
@@ -152,13 +155,13 @@
 {% if show_result %}
 {% if election.result_released_at %}
 <span class="highlight-box round">
-    This election is complete.
+    {% trans "This election is complete." %}
 </span><br /><br /><br />
 {% endif %}
 
-<h3 class="highlight-box">Tally</h3>
+<h3 class="highlight-box">{% trans "Tally" %}</h3>
 {% for question in election.pretty_result %}
-<b><span style="font-size:0.8em;">Question #{{forloop.counter}}</span><br />{{question.question}}</b><br />
+<b><span style="font-size:0.8em;">{% trans "Question" %}#{{forloop.counter}}</span><br />{{question.question}}</b><br />
 <table class="pretty" style="width: auto;">
 {% for answer in question.answers %}
 <tr><td style="padding-right:80px;{% if answer.winner %}font-weight:bold;{% endif %}">{{answer.answer}}</td><td align="right" style="{% if answer.winner %}font-weight:bold;{% endif %}">{{answer.count}}</td></tr>
@@ -170,49 +173,50 @@
 
 {% if election.voting_has_stopped %}
 <span class="highlight-box round">
-    Election closed. Results will be released soon.
+    {% trans "Election closed. Results will be released soon." %}
 </span><br /><br />
 {% else %}
 
 {% if election.voting_has_started %}
-<a class="button" href="{{test_cookie_url}}">Vote in this {{election.election_type}} </a>
+<a class="button" href="{{test_cookie_url}}">{% trans "Vote in this" context "election type"%} {% trans election.election_type %} </a>
 <br />
 <br />
 {% if election.voting_extended_until %}
-This {{election.election_type}} was initially scheduled to end at {{election.voting_ends_at}} (UTC),<br />
-but has been extended until {{ election.voting_extended_until }} (UTC).
+{% trans "This" context "election type" %} {% trans election.election_type %} {% trans "was initially scheduled to end at" context "time" %} {{election.voting_ends_at}} (UTC),<br />
+{% trans "but has been extended until" context "time" %} {{ election.voting_extended_until }} (UTC).
 {% else %}
 {% if election.voting_ends_at %}
 <br />
-This {{election.election_type}} is scheduled to end at {{election.voting_ends_at}} (UTC).
+{% trans "This" context "election type" %} {% trans election.election_type %} {% trans "is scheduled to end at" context "time" %} {{election.voting_ends_at}} (UTC).
 {% else %}
-This {{election.election_type}} ends at the administrator's discretion.
+{% trans "This" context "election type" %} {% trans election.election_type %} {% trans "ends at the administrator's discretion." %}
 {% endif %}
 <br />
 {% endif %}
 
 {% if election.private_p and voter %}
 <br />
-This election is <em>private</em>. You are signed in as eligible voter <em>{{voter.name}}</em>.
+{% trans "This election is <em>private</em>. You are signed in as eligible voter" context "name follows"%} <em>{{voter.name}}</em>.
 {% endif %}
 
 <div class="highlight-box round" style="font-size: 1.2em; margin-right: 400px; display:none;" id="nojava_message">
-  You do not have Java installed in your browser.<br />At this time, Helios requires Java.<br />
-  Visit <a target="_new" href="http://java.sun.com">java.sun.com</a> to install it.
+  {% trans "You do not have Java installed in your browser.<br />
+At this time, Helios requires Java.<br />" %}
+  {% trans "Visit" context "webpage" %} <a target="_new" href="http://java.sun.com">java.sun.com</a> {% trans "to install it." context "prerequisite" %}
 </div>
 <br />
 {% else %}
 <span class="highlight-box round">
-  voting is not yet open
+  {% trans "voting is not yet open" %}
 </span> <br /><br />
 {% endif %}
 
 {% if user %}
 {% if voter %}
 <p style="padding-top:1px;">
-  You are registered to vote in this {{election.election_type}}.
+  {% trans "You are registered to vote in this" context "election type" %} {% trans election.election_type %}.
 {% if election.use_voter_aliases %}
-Your voter alias is {{voter.alias}}.
+{% trans "Your voter alias is" context "name follows" %} {{voter.alias}}.
 {% endif %}
 </p>
 
@@ -221,12 +225,12 @@
 {% else %}
 {% if election.openreg %}
 {% if eligible_p %}
-You are eligible to vote in this election.
+{% trans "You are eligible to vote in this election." %}
 {% else %}
-You are <em>not eligible</em> to vote in this {{election.election_type}}.
+{% trans "You are <em>not eligible</em> to vote in this" %} {% trans election.election_type %}.
 {% endif %}
 {% else %}
-You are <em>not eligible</em> to vote in this {{election.election_type}}.
+{% trans "You are <em>not eligible</em> to vote in this" %} {% trans election.election_type %}.
 <br />
 {% endif %}
 {% endif %}
@@ -235,10 +239,10 @@
 
 {% if election.openreg %}
 {% if election.eligibility %}
-This election is open to: {{election.pretty_eligibility|safe}}
-<a href="{{settings.SECURE_URL_HOST}}{% url "helios_auth.views.index" %}?return_url={{CURRENT_URL}}">Log in</a> to check your eligibility.
+{% trans "This election is open to:" %} {{election.pretty_eligibility|safe}}
+<a href="{{settings.SECURE_URL_HOST}}{% url "helios_auth.views.index" %}?return_url={{CURRENT_URL}}">{% trans "Log in" %}</a> {% trans "to check your eligibility." %}
 {% else %}
-Anyone can vote in this election.
+{% trans "Anyone can vote in this election." %}
 {% endif %}
 {% endif %}
 
@@ -249,47 +253,47 @@
 {% endif %}
 
 {% if admin_p and election.voting_ends_at and not election.tallying_started_at %}
-<br /><a href="{% url "helios.views.one_election_extend" election.uuid %}">extend voting</a><br />
+<br /><a href="{% url "helios.views.one_election_extend" election.uuid %}">{% trans "extend voting" %}</a><br />
 {% endif %}
 
 <div style="background: lightyellow; padding:5px; padding-left: 10px; margin-top: 15px; border: 1px solid #aaa; width: 720px;" class="round">
-<a href="#" onclick="$('#auditbody').slideToggle(250);">Audit Info</a>
+<a href="#" onclick="$('#auditbody').slideToggle(250);">{% trans "Audit Info" %}</a>
 <div id="auditbody" style="display:none;">
-<br />Election URL:<br />
+<br />{% trans "Election URL:" %}<br />
 <tt style="font-size: 1.2em;"><a href="{{election.url}}">{{election.url}}</a></tt>
 
 {% if election.frozen_at %}
 <br />
-<br />Election Fingerprint:<br />
+<br />{% trans "Election Fingerprint:" %}<br />
 <tt style="font-size: 1.3em; font-weight: bold;">{{election.hash}}</tt>
 
 
 {% if votes %}
-<p>Your smart ballot tracker:<br />
+<p>{% trans "Your smart ballot tracker:" %}<br />
 <tt style="font-size:14pt; font-weight: bold;">{{votes.0.vote_hash}}</tt>
 </p>
 {% endif %}
 
 <p style="font-size: 1.3em;">
-<a href="{% url "helios.views.voters_list_pretty" election.uuid %}">Ballot Tracking Center</a> &nbsp;| &nbsp;
-<a href="{% url "helios.views.one_election_audited_ballots" election.uuid %}">Audited Ballots</a>
+<a href="{% url "helios.views.voters_list_pretty" election.uuid %}">{% trans "Ballot Tracking Center" %}</a> &nbsp;| &nbsp;
+<a href="{% url "helios.views.one_election_audited_ballots" election.uuid %}">{% trans "Audited Ballots" %}</a>
 </p>
 
 {% endif %}
 
 {% if not election.voting_has_started %}
 <p style="font-size: 1.2em;">
-  <a href="{{SECURE_URL_HOST}}/booth/vote.html?election_url={% url "helios.views.one_election" election.uuid %}">preview booth</a>
+  <a href="{{SECURE_URL_HOST}}/booth/vote.html?election_url={% url "helios.views.one_election" election.uuid %}">{% trans "preview booth" %}</a>
 </p>
 {% endif %}  
 
 {% if election.voting_has_stopped %}
 <p style="font-size: 1.2em;">
 {% if election.result %}
-verify <a target="_blank" href="/verifier/verify.html?election_url={% url "helios.views.one_election" election.uuid %}">election tally</a>.
+{% trans "verify" context "election tally link follows" %} <a target="_blank" href="/verifier/verify.html?election_url={% url "helios.views.one_election" election.uuid %}">{% trans "election tally" %}</a>.
 {% endif %}
 
-review the <a href="{{vote_url}}">voting booth</a>.
+{% trans "review the" context "voting booth"%} <a href="{{vote_url}}">{% trans "voting booth" %}</a>.
 </p>
 
 {% endif %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/elections_administered.html helios-server-translation/helios/templates/elections_administered.html
--- helios-server-original/helios/templates/elections_administered.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/elections_administered.html	2019-10-29 03:15:07.842263284 -0300
@@ -1,11 +1,12 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">Elections you Administer <span style="font-size:0.7em;">[<a href="/">back to home</a>]</span></h2>
+  <h2 class="title">{% trans "Elections you Administer" %} <span style="font-size:0.7em;">[<a href="/">{% trans "back to home" %}</a>]</span></h2>
 
 <ul>
 {% for election in elections %}
-<li> <a href="{% url "helios.views.one_election_view" election.uuid %}">{{election.name}}</a><em> - {{election.num_voters}} voters / {{election.num_cast_votes}} cast votes</em></li>
+<li> <a href="{% url "helios.views.one_election_view" election.uuid %}">{{election.name}}</a><em> - {{election.num_voters}} {% trans "voters" context "number before" %} / {{election.num_cast_votes}} {% trans "cast votes" context "number before" %}</em></li>
 {% endfor %}
 </ul>
-{% endblock %}
\ No newline at end of file
+{% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/elections_voted.html helios-server-translation/helios/templates/elections_voted.html
--- helios-server-original/helios/templates/elections_voted.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/elections_voted.html	2019-10-29 03:15:07.842263284 -0300
@@ -1,7 +1,8 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">Elections where you voted <span style="font-size:0.7em;">[<a href="/">back to home</a>]</span></h2>
+  <h2 class="title">{% trans "Elections where you voted" %} <span style="font-size:0.7em;">[<a href="/">{% trans "back to home" %}</a>]</span></h2>
 
 <ul>
 {% for election in elections %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/info_body.txt helios-server-translation/helios/templates/email/pt-BR/info_body.txt
--- helios-server-original/helios/templates/email/pt-BR/info_body.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/info_body.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1,8 @@
+Prezado(a) {{voter.name}},
+
+{{custom_message|safe}}
+
+URL da votação: {{election_vote_url}}
+
+--
+Helios/INSCRYPT
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/info_subject.txt helios-server-translation/helios/templates/email/pt-BR/info_subject.txt
--- helios-server-original/helios/templates/email/pt-BR/info_subject.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/info_subject.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1 @@
+Informações - {{custom_subject|safe}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/result_body.txt helios-server-translation/helios/templates/email/pt-BR/result_body.txt
--- helios-server-original/helios/templates/email/pt-BR/result_body.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/result_body.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1,22 @@
+Prezado(a) {{voter.name}},
+
+A apuração da votação {{election.name}} foi concluída.
+
+O resultado já se encontra disponível em:
+
+  {{election_url}}
+
+{{custom_message|safe}}
+
+{% if voter.vote_hash %}O localizador da sua cédula nessa votação foi:
+
+  {{voter.vote_hash}}
+
+Se este localizador não corresponde ao que lhe foi previamente informado
+entre em contato.
+{% else %}
+Nenhuma cédula sua foi encontrada para esta votação.
+Se você acredita que isto é um erro, entre em contato.
+{% endif %}
+--
+Helios/INSCRYPT
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/result_subject.txt helios-server-translation/helios/templates/email/pt-BR/result_subject.txt
--- helios-server-original/helios/templates/email/pt-BR/result_subject.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/result_subject.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1 @@
+Resultados - {{custom_subject|safe}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/simple_body.txt helios-server-translation/helios/templates/email/pt-BR/simple_body.txt
--- helios-server-original/helios/templates/email/pt-BR/simple_body.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/simple_body.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1,16 @@
+Prezado(a) {{voter.name}},
+
+{{custom_message|safe}}
+
+========
+Como Votar
+
+URL da votação:  {{election_vote_url}}
+{% ifequal voter.voter_type "password" %}
+Login: {{voter.voter_login_id}}
+Senha: {{voter.voter_password}}
+{% else %}
+Acesse com sua conta {{voter.voter_type}}.
+{% endifequal %}
+--
+Helios/INSCRYPT
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/simple_subject.txt helios-server-translation/helios/templates/email/pt-BR/simple_subject.txt
--- helios-server-original/helios/templates/email/pt-BR/simple_subject.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/simple_subject.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1 @@
+{{custom_subject|safe}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/vote_body.txt helios-server-translation/helios/templates/email/pt-BR/vote_body.txt
--- helios-server-original/helios/templates/email/pt-BR/vote_body.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/vote_body.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1,29 @@
+Prezado(a) {{voter.name}},
+
+{{custom_message|safe}}
+
+URL da votação:  {{election_vote_url}}
+Identificador da votação:  {{voter.election.hash}}
+{% ifequal voter.voter_type "password" %}
+Login: {{voter.voter_login_id}}
+Senha: {{voter.voter_password}}
+{% else %}
+Acesse com sua conta {{voter.voter_type}}.
+{% endifequal %}{% if voter.vote_hash %}
+Sua cédula foi registrada com o localizador:
+
+  {{voter.vote_hash}}
+
+Você pode votar quantas vezes desejar: apenas a última cédula enviada conta.
+{% endif %}{% if election.use_voter_aliases %}
+Para proteger sua privacidade, esta votação foi configurada para
+nunca mostrar seu login, nome ou endereço de email publicamente.
+No lugar dessas informações, apenas seu pseudônimo será mostrado.
+
+O seu pseudônimo é {{voter.alias}}.
+
+IMPORTANTE: quando solicitado a prover suas credenciais de acesso,
+entre com o seu *login*, e NÃO com o seu pseudônimo.
+{% endif %}
+--
+Helios/INSCRYPT
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/email/pt-BR/vote_subject.txt helios-server-translation/helios/templates/email/pt-BR/vote_subject.txt
--- helios-server-original/helios/templates/email/pt-BR/vote_subject.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/helios/templates/email/pt-BR/vote_subject.txt	2019-10-29 03:15:07.842263284 -0300
@@ -0,0 +1 @@
+Votar - {{custom_subject|safe}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/list_trustees.html helios-server-translation/helios/templates/list_trustees.html
--- helios-server-original/helios/templates/list_trustees.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/list_trustees.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,31 +1,33 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
-{% block title %}Trustees for {{election.name}}{% endblock %}
+{% block title %}{% trans "Trustees for" context "election name follows" %} {{election.name}}{% endblock %}
 
 {% block content %}
-  <h3 class="title">{{election.name}} &mdash; Trustees <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h3>
+  <h3 class="title">{{election.name}} &mdash; {% trans "Trustees" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h3>
 
 <p>
-    Trustees are responsible for decrypting the election result.<br />
-    Each trustee generates a keypair and submits the public portion to Helios.<br />
-    When it's time to decrypt, each trustee needs to provide their secret key.
+    {% trans "Trustees are responsible for decrypting the election result." %}<br />
+    {% trans "Each trustee generates a keypair and submits the public portion to Helios." %}<br />
+    {% trans "When it's time to decrypt, each trustee needs to provide their secret key." %}
 </p>
 
 {% if not election.frozen_at %}
 
 <p>
-  Helios is automatically your first trustee and will handle its keypair generation and decryption automatically.<br />
-  You may add additional trustees if you want, and you can even remove the Helios trustee.<br />
-  However, we recommend you do this only if you have a solid understanding of the trustee's role.
+  {% trans "Helios is automatically your first trustee and will handle its keypair generation and decryption automatically." %}<br />
+  {% trans "You may add additional trustees if you want, and you can even remove the Helios trustee." %}<br />
+  {% trans "However, we recommend you do this only if you have a solid understanding of the trustee's role." %}
 </p>
 
 {% if admin_p %}
+{% trans "Adding your own trustee requires a good bit more work to tally the election.\nYou will need to have trustees generate keypairs and safeguard their secret key.\n\nIf you are not sure what that means, we strongly recommend\nclicking Cancel and letting Helios tally the election for you." as str %}
 <p>
-    [ <a onclick="return(confirm('Adding your own trustee requires a good bit more work to tally the election.\nYou will need to have trustees generate keypairs and safeguard their secret key.\n\nIf you are not sure what that means, we strongly recommend\nclicking Cancel and letting Helios tally the election for you.'));" href="{% url "helios.views.new_trustee" election.uuid %}">add a trustee</a> ]
+    [ <a onclick="return(confirm('{{str}}'));" href="{% url "helios.views.new_trustee" election.uuid %}">{% trans "add a trustee" %}</a> ]
 </p>
 {% if not election.has_helios_trustee %}
 <p>
-    <a href="{% url "helios.views.new_trustee_helios" election.uuid %}">add Helios as a trustee</a>
+    <a href="{% url "helios.views.new_trustee_helios" election.uuid %}">{% trans "add Helios as a trustee" %}</a>
 </p>
 {% endif %}
 {% endif %}
@@ -36,31 +38,36 @@
 
 {% else %}
 {% for t in trustees %}
-<h5> Trustee #{{forloop.counter}}: {{t.name}} 
+<h5> {% trans "Trustee" %} #{{forloop.counter}}: {{t.name}} 
 {% if admin_p %}
 {% if t.secret_key %}
-{% if not election.frozen_at %}[<a onclick="return confirm('Are you sure you want to remove Helios as a trustee?');" href="{% url "helios.views.delete_trustee" election.uuid %}?uuid={{t.uuid}}">x</a>]{% endif %}
+{% if not election.frozen_at %}
+{% trans "Are you sure you want to remove Helios as a trustee?" as remove_helios_str %}
+[<a onclick="return confirm('{{remove_helios_str}}');" href="{% url "helios.views.delete_trustee" election.uuid %}?uuid={{t.uuid}}">x</a>]{% endif %}
 {% else %}
 ({{t.email}})
-{% if not election.frozen_at %}[<a onclick="return confirm('Are you sure you want to remove this Trustee?');" href="{% url "helios.views.delete_trustee" election.uuid %}?uuid={{t.uuid}}">x</a>]{% endif %}
-[<a onclick="return confirm('Are you sure you want to send this trustee his/her admin URL?');" href="{% url "helios.views.trustee_send_url" election.uuid t.uuid %}">send login</a>]
+{% if not election.frozen_at %}
+{% trans "Are you sure you want to remove this Trustee?" as remove_t_str %}
+[<a onclick="return confirm('{{remove_t_str}}');" href="{% url "helios.views.delete_trustee" election.uuid %}?uuid={{t.uuid}}">x</a>]{% endif %}
+{% trans "Are you sure you want to send this trustee his/her admin URL?" as send_trustee_url_confirm %}
+[<a onclick="return confirm('{{send_trustee_url_confirm}}');" href="{% url "helios.views.trustee_send_url" election.uuid t.uuid %}">{% trans "send login" %}</a>]
 {% endif %}
 {% endif %}
 </h5>
 
 <p>
 {% if t.public_key_hash %}
-  Public Key Fingerprint: <tt style="font-size:1.5em;">{{t.public_key_hash}}</tt>
+  {% trans "Public Key Fingerprint:" %} <tt style="font-size:1.5em;">{{t.public_key_hash}}</tt>
 {% else %}
-No public key uploaded yet.
+{% trans "No public key uploaded yet." %}
 {% endif %}
 </p>
 
 {% if election.encrypted_tally %}
 {% if t.decryption_factors %}
-<b>tally recorded for this trustee.</b>
+<b>{% trans "tally recorded for this trustee." %}</b>
 {% else %}
-<em>waiting for this trustee's tally</em>
+<em>{% trans "waiting for this trustee's tally" %}</em>
 {% endif %}
 {% endif %}
 </li>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/new_trustee.html helios-server-translation/helios/templates/new_trustee.html
--- helios-server-original/helios/templates/new_trustee.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/new_trustee.html	2019-10-29 03:15:07.843263284 -0300
@@ -1,14 +1,15 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; New Trustee <span style="font-size:0.7em;">[<a href="{% url "helios.views.list_trustees_view" election.uuid %}">cancel</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "New Trustee" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.list_trustees_view" election.uuid %}">{% trans "cancel" %}</a>]</span></h2>
   
 <form method="post" action="">
 <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
-Name: <input type="text" name="name" size="60" /><br /><br />
-Email: <input type="text" name="email" size="60" /><br /><br />
+{% trans "Name" %}: <input type="text" name="name" size="60" /><br /><br />
+{% trans "Email" %}: <input type="text" name="email" size="60" /><br /><br />
 
-<input type="submit" value="Add Trustee" />
+<input type="submit" value={% trans "Add Trustee" %} />
 </form>
 
-{% endblock %}
\ No newline at end of file
+{% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/release_result.html helios-server-translation/helios/templates/release_result.html
--- helios-server-original/helios/templates/release_result.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/release_result.html	2019-10-29 03:15:07.843263284 -0300
@@ -1,19 +1,20 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Release Result &mdash; {{election.name}}{% endblock %}
+{% block title %}{% trans "Release Result" %} &mdash; {{election.name}}{% endblock %}
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Release Result <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">cancel</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Release Result" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "cancel" %}</a>]</span></h2>
 
   <p>
-    You are about to release the result for this election.
+    {% trans "You are about to release the result for this election." %}
   </p>
 
   <form method="POST" action="">
     <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
     <p>
-    <input type="checkbox" name="send_email" value="send_email" checked="checked"/> Send email to voters reporting release of result.
+    <input type="checkbox" name="send_email" value="send_email" checked="checked"/> {% trans "Send email to voters reporting release of result." %}
     </p>
-    <input type="submit" class="button" value="release result!" />
+    <input type="submit" class="button" value="{% trans "release result!" %}" />
   </form>
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/trustee_check_sk.html helios-server-translation/helios/templates/trustee_check_sk.html
--- helios-server-original/helios/templates/trustee_check_sk.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/trustee_check_sk.html	2019-10-29 03:15:07.843263284 -0300
@@ -1,4 +1,5 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
 {% block content %}
 <script>
@@ -35,39 +36,39 @@
     
     $('#processing').hide();
 
-    var reset_link = "<br /><a href='javascript:reset();'>try again</a>";
+    var reset_link = "<br /><a href='javascript:reset();'>" + '{% trans "try again" %}' + "</a>";
     if (key_ok_p) {
-      $('#result').html("your secret key matches!");
+      $('#result').html('{% trans "your secret key matches!" %}');
     } else {
-      $('#result').html("OH OH, your key is bad." + reset_link);
+      $('#result').html('{% trans "OH OH, your key is bad." %}' + reset_link);
     }
 }
 </script>
-  <h2 class="title">{{election.name}} &mdash; Trustee {{trustee.name}} &mdash; Check Secret Key <span style="font-size:0.7em;">[<a href="./home">back to trustee home</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Trustee" context "trustee name follows" %} {{trustee.name}} &mdash; {% trans "Check Secret Key" %} <span style="font-size:0.7em;">[<a href="./home">{% trans "back to trustee home" %}</a>]</span></h2>
 
 <p>
-Your public key fingerprint is: <b>{{trustee.public_key_hash}}</b>
+{% trans "Your public key fingerprint is:" %} <b>{{trustee.public_key_hash}}</b>
 </p>
 
 <div id="loading">
-loading crypto functions...
+{% trans "loading crypto functions..." %}
 </div>
 
 <div id="input" style="display:none;">
 <p>
-To verify that you have the right secret key, paste it here:
+{% trans "To verify that you have the right secret key, paste it here:" %}
 <p>
 
 <form onsubmit="check_sk(this.secret_key.value); this.secret_key.value=''; return false;">
 <textarea name="secret_key" cols="60" rows ="5" wrap="soft" style="height: 25em;">
 </textarea>
 <br />
-<input type="submit" value="check" />
+<input type="submit" value="{% trans "check" %}" />
 </form>
 </div>
 
 <div id="processing" style="display:none;">
-checking your secret key...
+{% trans "checking your secret key..." %}
 </div>
 
 <div id="result">
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/trustee_decrypt_and_prove.html helios-server-translation/helios/templates/trustee_decrypt_and_prove.html
--- helios-server-original/helios/templates/trustee_decrypt_and_prove.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/trustee_decrypt_and_prove.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,4 +1,5 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
 {% block content %}
 <script language="javascript">
@@ -131,75 +132,75 @@
 }
 
 </script>
-  <h2 class="title">Trustee {{trustee.name}} &mdash; Decrypt Result for {{election.name}}</h2>
+  <h2 class="title">{% trans "Trustee" context "name follows" %} {{trustee.name}} &mdash; {% trans "Decrypt Result for" context "election name follows" %} {{election.name}}</h2>
 
 <p>
-    <b>Trustee:</b> <tt>{{trustee.email}}</tt><br />
-    <b>Public Key Fingerprint:</b> <tt>{{trustee.public_key_hash}}</tt><br />
-    <b>Encrypted Tally Fingerprint:</b> <tt>{{election.encrypted_tally_hash}}</tt>
+    <b>{% trans "Trustee:" %}</b> <tt>{{trustee.email}}</tt><br />
+    <b>{% trans "Public Key Fingerprint:" %}</b> <tt>{{trustee.public_key_hash}}</tt><br />
+    <b>{% trans "Encrypted Tally Fingerprint:" %}</b> <tt>{{election.encrypted_tally_hash}}</tt>
 </p>
 
     <p>
-      The encrypted tally for your election has been computed.<br />
-      Now it's time to compute and submit your partial decryption.
+      {% trans "The encrypted tally for your election has been computed." %}<br />
+      {% trans "Now it's time to compute and submit your partial decryption." %}
     </p>
 
     <p>
-      This process is performed in two steps.<br /><br />
-      <u>First</u>, your secret key is used to decrypt the tally <em>inside</em> your browser, without connecting to the network.<br />
-      You can choose to take your browser "offline" for this step, if you'd like.<br /><br />
-      <u>Second</u>, once your decryption factors have been computed, your browser will need to be "online" to submit them to the server.<br />
-      If you'd like, you can compute your decryption factors, copy them to your clipboard, restart your browser, and<br />
-      skip to the second step so that your browser is never online when you enter your secret key.
+      {% trans "This process is performed in two steps." %}<br /><br />
+      {% blocktrans %}<u>First</u>, your secret key is used to decrypt the tally <em>inside</em> your browser, without connecting to the network.{% endblocktrans %}<br />
+      {% blocktrans %}You can choose to take your browser "offline" for this step, if you'd like.{% endblocktrans %}<br /><br />
+      {% blocktrans %}<u>Second</u>, once your decryption factors have been computed, your browser will need to be "online" to submit them to the server.{% endblocktrans %}<br />
+      {% blocktrans %}If you'd like, you can compute your decryption factors, copy them to your clipboard, restart your browser, and{% endblocktrans %}<br />
+      {% blocktrans %}skip to the second step so that your browser is never online when you enter your secret key.{% endblocktrans %}
     </p>
     
   <div id="sk_section" style="display:none;">
 
       <form onsubmit="return false;">
-          <h3>FIRST STEP: enter your secret key</h3>
+          <h3>{% trans "FIRST STEP: enter your secret key" %}</h3>
           <textarea id="sk_textarea" cols="60" rows="5" style="height: 25em;"></textarea>
       </form>
       <p id="tally_section">
-          <button onclick="do_tally();">Generate partial decryption</button>
+          <button onclick="do_tally();">{% trans "Generate partial decryption" context "button" %}</button>
       </p>
 
       <p id="skip_to_second_step_instructions">
-        <a href="javascript:skip_to_second_step();">skip to the second step</a><br />
-        (you need to have already computed your decryption factors.)
+        <a href="javascript:skip_to_second_step();">{% trans "skip to the second step" %}</a><br />
+        {% trans "(you need to have already computed your decryption factors.)" %}
       </p>
   </div>
   
   <div id="waiting_div">
-      Generating partial decryption factors and proofs...<br />
+      {% trans "Generating partial decryption factors and proofs..." %}<br />
       <img src="/static/helios/loading.gif" />
   </div>
 
   <div id="waiting_submit_div">
-      Submitting and checking your decryption factors and proofs...<br />
+      {% trans "Submitting and checking your decryption factors and proofs..." %}<br />
       <img src="/static/helios/loading.gif" />
   </div>
   
   <div id="result_div">
-      <h3>SECOND STEP: upload your partial decryption</h3>
+      <h3>{% trans "SECOND STEP: upload your partial decryption" %}</h3>
       <p id="first-step-success" style="display: none;">
-          Your partial decryption factors and proofs have been generated below.<br />
-          Your secret key has been cleared from memory.<br />
-          When you're ready, you can submit this result to the server.
+          {% trans "Your partial decryption factors and proofs have been generated below." %}<br />
+          {% trans "Your secret key has been cleared from memory." %}<br />
+          {% trans "When you're ready, you can submit this result to the server." %}
       </p>
-      Your partial decryption:<br />
+      {% trans "Your partial decryption:" %}<br />
       <p>
           <textarea id="result_textarea" cols="60" rows="5" wrap="soft" style="height: 25em;"></textarea>
-          <button onclick="submit_result();">Upload decryption factors to server</button>
+          <button onclick="submit_result();">{% trans "Upload decryption factors to server" %}</button>
       </p>
-      <p><a href="javascript:reset()">reset and restart decryption process</a></p>
+      <p><a href="javascript:reset()">{% trans "reset and restart decryption process" %}</a></p>
   </div>
   
   <div id="done_div">
-      Done! <a href="{% url "helios.views.one_election_view" election.uuid %}">Back to election</a>
+      {% trans "Done!" %} <a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "Back to election" %}</a>
   </div>
   
   <div id="error_div">
-      Error: your secret key was likely incorrect.
+      {% trans "Error: your secret key was likely incorrect." %}
   </div>
 
   <div id="applet_div">
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/trustee_home.html helios-server-translation/helios/templates/trustee_home.html
--- helios-server-original/helios/templates/trustee_home.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/trustee_home.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,29 +1,30 @@
 {% extends "helios/templates/cryptobase.html" %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Trustee {{trustee.name}} Home</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Trustee" context "trustee name follows" %} {{trustee.name}} {% trans "Home" context "trustee name before" %}</span></h2>
 
 <p>
 {% if trustee.public_key_hash %}
-You have successfully uploaded your public key.<br />
-Your public key fingerprint is: <b>{{trustee.public_key_hash}}</b>.<br />
-You can <a href="{% url "helios.views.trustee_check_sk" election.uuid trustee.uuid %}">verify that you have the right secret key</a>.
+{% trans "You have successfully uploaded your public key." %}<br />
+{% trans "Your public key fingerprint is:" %} <b>{{trustee.public_key_hash}}</b>.<br />
+{% trans "You can" context "action follows" %} <a href="{% url "helios.views.trustee_check_sk" election.uuid trustee.uuid %}">{% trans "verify that you have the right secret key" context "action link" %}</a>.
 {% else %}
-<a href="{% url "helios.views.trustee_keygenerator" election.uuid trustee.uuid %}">setup your key</a>
+<a href="{% url "helios.views.trustee_keygenerator" election.uuid trustee.uuid %}">{% trans "setup your key" context "action" %}</a>
 {% endif %}
 </p>
 
 <p>
 {% if election.encrypted_tally %}
 {% if trustee.decryption_factors %}
-You have successfully uploaded your decryption.
+{% trans "You have successfully uploaded your decryption." %}
 {% else %}
-    The encrypted tally for this election is ready.<br />
-    <a href="{% url "helios.views.trustee_decrypt_and_prove" election.uuid trustee.uuid %}">decrypt with your key</a>
+    {% trans "The encrypted tally for this election is ready." %}<br />
+    <a href="{% url "helios.views.trustee_decrypt_and_prove" election.uuid trustee.uuid %}">{% trans "decrypt with your key" context "action" %}</a>
 {% endif %}
 {% else %}
-Once the tally is computed, come back here to provide your secret key for decryption purposes.<br />
-You should keep the email with your trustee homepage link, which contains the credentials needed to get back here.
+{% trans "Once the tally is computed, come back here to provide your secret key for decryption purposes." %}<br />
+{% trans "You should keep the email with your trustee homepage link, which contains the credentials needed to get back here." %}
 {% endif %}
 </p>
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/voters_email.html helios-server-translation/helios/templates/voters_email.html
--- helios-server-original/helios/templates/voters_email.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/voters_email.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,6 +1,7 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Contact Voters for {{election.name}}{% endblock %}
+{% block title %}{% trans "Contact Voters for" context "name follows" %} {{election.name}}{% endblock %}
 {% block content %}
 <script>
 var voter_id = null;
@@ -9,16 +10,16 @@
 {% endif %}
 </script>
 
-  <h2 class="title">{{election.name}} &mdash; Contact Voters <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Contact Voters" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h2>
 
 {% if voter %}  
   <p>
-    You are sending this email to a specific user: <b>{{voter.name}} ({{voter.voter_id}})</b>
+    {% trans "You are sending this email to a specific user:" context "name follows" %} <b>{{voter.name}} ({{voter.voter_id}})</b>
   </p>
 {% endif %}
 
 <p>
-<b>Templates</b>:
+<b>{% trans "Templates" context "email" %}</b>:
 
 {% for template_option in templates %}
 {% if template_option.0 == template %}
@@ -30,13 +31,13 @@
 {% endfor %}
 
 <pre style="margin:10px; border: 1px solid #888; padding:20px">
-Subject: {{default_subject}}
+{% trans "Subject:" context "email" %} {{default_subject}}
 
 {{default_body|safe}}
 </pre>
 
 <p>
-You may tweak the subject and add a custom message using the form below.
+{% trans "You may tweak the subject and add a custom message using the form below." %}
 </p>
 
   <form class="prettyform" action="" method="POST" id="email_form">
@@ -47,7 +48,7 @@
      {{email_form.as_table}}
      </table>
   <div>
-  <label for="">&nbsp;</label><input type="submit" value="Send" id="send_button" class="button" />
+  <label for="">&nbsp;</label><input type="submit" value="{% trans "Send" %}" id="send_button" class="button" />
   </div>
   </form>
   
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/voters_list.html helios-server-translation/helios/templates/voters_list.html
--- helios-server-original/helios/templates/voters_list.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/voters_list.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,29 +1,30 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
-{% block title %}Voters &amp; Ballot Tracking Center for {{election.name}}{% endblock %}
+{% block title %}{% trans "Voters &amp; Ballot Tracking Center for" context "election name follows" %} {{election.name}}{% endblock %}
 {% block content %}
-  <h3 class="title">{{election.name}} &mdash; Voters and Ballot Tracking Center <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h3>
+  <h3 class="title">{{election.name}} &mdash; {% trans "Voters and Ballot Tracking Center" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h3>
 
 <p>
-<b>Who can vote?</b>
+<b>{% trans "Who can vote?" %}</b>
 {% if election.openreg %}
 {{election.pretty_eligibility|safe}}
 {% else %}
-<em>Only the voters listed here</em>.
+<em>{% trans "Only the voters listed here" %}</em>.
 {% endif %}
 </p>
 
 {% if admin_p and not election.frozen_at %}
 {% if election.private_p %}
-<em>Your election is marked private, which means you cannot open registration up more widely</em>.<br />
+<em>{% trans "Your election is marked private, which means you cannot open registration up more widely" %}</em>.<br />
 {% else %}
-You can change this setting:
+{% trans "You can change this setting:" %}
 <form method="post" action="{% url "helios.views.voters_eligibility" election.uuid %}">
 <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
-<input type="radio" name="eligibility" value="openreg" {% if election.openreg and not election.eligibility %}CHECKED{% endif %} /> anyone can vote<br />
-<input type="radio" name="eligibility" value="closedreg" {% if not election.openreg %}CHECKED{% endif %} /> only voters listed explicitly below can vote<br />
+<input type="radio" name="eligibility" value="openreg" {% if election.openreg and not election.eligibility %}CHECKED{% endif %} /> {% trans "anyone can vote" %}<br />
+<input type="radio" name="eligibility" value="closedreg" {% if not election.openreg %}CHECKED{% endif %} /> {% trans "only voters listed explicitly below can vote" %}<br />
 {% if categories %}
-<input type="radio" name="eligibility" value="limitedreg" {% if election.eligibility %}CHECKED{% endif %} /> only voters who are members of 
+<input type="radio" name="eligibility" value="limitedreg" {% if election.eligibility %}CHECKED{% endif %} /> {% trans "only voters who are members of" context "categories follow" %} 
 <select name="category_id">
 {% for category in categories %}
 <option value="{{category.id}}" {% if eligibility_category_id == category.id %}SELECTED{% endif %}> {{category.name}}</option>
@@ -31,21 +32,21 @@
 </select>
 <br />
 {% endif %}
-<input type="submit" value="update" />
+<input type="submit" value={% trans "update" %} />
 </form>
 {% endif %}
 {% endif %}
 
 {% if email_voters and election.frozen_at and admin_p %}
-<p><a class="button" href="{% url "helios.views.voters_email" election.uuid %}">email voters</a></p>
+<p><a class="button" href="{% url "helios.views.voters_email" election.uuid %}">{% trans "email voters" %}</a></p>
 {% endif %}
 
 {% if election.num_voters > 20 %}
 <p>
 {% if q %}
-<p><em>searching for <u>{{q}}</u>.</em> [<a href="?">clear search</a>]</p>
+<p><em>{% trans "searching for" context "voter name follows" %} <u>{{q}}</u>.</em> [<a href="?">{% trans "clear search" %}</a>]</p>
 {% else %}
-<form method="get" action="{% url "helios.views.voters_list_pretty" election.uuid %}"><b>search</b>: <input type="text" name="q" /> <input type="submit" value="search" /></form>
+<form method="get" action="{% url "helios.views.voters_list_pretty" election.uuid %}"><b>{% trans "search" %}</b>: <input type="text" name="q" /> <input type="submit" value="{% trans "search" %}" /></form>
 {% endif %}
 </p>
 {% endif %}
@@ -54,11 +55,11 @@
 <!-- Add a Voter: WORK HERE-->
 {% if upload_p and not election.openreg %}
 <p>
-<a class="button" href="{% url "helios.views.voters_upload" election_uuid=election.uuid %}">bulk upload voters</a>
+<a class="button" href="{% url "helios.views.voters_upload" election_uuid=election.uuid %}">{% trans "bulk upload voters" %}</a>
 </p>
 
 {% if voter_files %}
-Prior Bulk Uploads:
+{% trans "Prior Bulk Uploads:" %}
 <ul>
 {% for vf in voter_files %}
 <li>
@@ -67,15 +68,15 @@
 {% else %}
 {{vf.voter_file_content|length}}
 {% endif %}
- bytes, at {{vf.uploaded_at}}:
+ bytes, {% trans "at" context "date" %} {{vf.uploaded_at}}:
 {% if vf.processing_finished_at %}
-<em>done processing: {{vf.num_voters}} voters loaded</em>
+<em>{% trans "done processing:" %} {{vf.num_voters}} {% trans "voters loaded" context "number before" %}</em>
 {% else %}
 
 {% if vf.processing_started_at %}
-<em>currently processing</em>
+<em>{% trans "currently processing" %}</em>
 {% else %}
-<em>not yet processed</em>
+<em>{% trans "not yet processed" %}</em>
 {% endif %}
 
 {% endif %}
@@ -91,39 +92,39 @@
 <p>
 <b>
 {% if election.num_cast_votes %}
-{{election.num_cast_votes}} cast vote{% ifequal election.num_cast_votes 1 %}{% else %}s{% endifequal %}
+{{election.num_cast_votes}} {% trans "cast vote" %}{% ifequal election.num_cast_votes 1 %}{% else %}s{% endifequal %}
 {% else %}
-no votes yet
+{% trans "no votes yet" %}
 {% endif %}
 </b>
 </p>
 
 {% if voters_page.has_previous %}
-<a href="./list?page={{voters_page.previous_page_number}}&limit={{limit}}&q={{q|urlencode}}">previous {{limit}}</a> &nbsp;&nbsp;
+<a href="./list?page={{voters_page.previous_page_number}}&limit={{limit}}&q={{q|urlencode}}">{% trans "previous" %} {{limit}}</a> &nbsp;&nbsp;
 {% endif %}
 
 
-Voters {{voters_page.start_index}} - {{voters_page.end_index}} (of {{total_voters}})&nbsp;&nbsp;
+{% trans "Voters" %} {{voters_page.start_index}} - {{voters_page.end_index}} {% trans "(of" context "total number follows" %} {{total_voters}})&nbsp;&nbsp;
 
 {% if voters_page.has_next %}
-<a href="./list?page={{voters_page.next_page_number}}&limit={{limit}}&q={{q|urlencode}}">next {{limit}}</a> &nbsp;&nbsp;
+<a href="./list?page={{voters_page.next_page_number}}&limit={{limit}}&q={{q|urlencode}}">{% trans "next" context "feminine" %} {{limit}}</a> &nbsp;&nbsp;
 {% endif %}
 
 <table class="pretty">
 <tr>
 {% if admin_p or not election.use_voter_aliases %}
 {% if admin_p %}
-<th style="width: 80px;">Actions</th>
-<th>Login</th>
-<th>Email Address</th>
+<th style="width: 80px;">{% trans "Actions" %}</th>
+<th>{% trans "Login" context "username" %}</th>
+<th>{% trans "Email Address" %}</th>
 {% endif %}
-<th>Name</th>
+<th>{% trans "Name" %}</th>
 {% endif %}
 
 {% if election.use_voter_aliases %}
-<th>Alias</th>
+<th>{% trans "Alias" %}</th>
 {% endif %}
-<th>Smart Ballot Tracker</th>
+<th>{% trans "Smart Ballot Tracker" %}</th>
 </tr>
 {% for voter in voters %}
 <tr>
@@ -131,9 +132,10 @@
 {% if admin_p %}
 <td style="white-space: nowrap;">
 {% if election.frozen_at %}
-[<a href="{% url "helios.views.voters_email" election.uuid %}?voter_id={{voter.voter_login_id}}">email</a>]
+[<a href="{% url "helios.views.voters_email" election.uuid %}?voter_id={{voter.voter_login_id}}">{% trans "email" %}</a>]
 {% endif %}
-[<a onclick="return confirm('are you sure you want to remove {{voter.name}} ?');" href="{% url "helios.views.voter_delete" election.uuid voter.uuid %}">x</a>]
+{% trans "are you sure you want to remove" as str context "voter name follows" %}
+[<a onclick="return confirm('{{str}} {{voter.name}} ?');" href="{% url "helios.views.voter_delete" election.uuid voter.uuid %}">x</a>]
 </td>
 <td>{{voter.voter_login_id}}</td>
 <td>{{voter.voter_email}}</td>
@@ -143,14 +145,14 @@
 {% if election.use_voter_aliases %}
 <td>{{voter.alias}}</td>
 {% endif %}
-<td><tt style="font-size: 1.4em;">{% if voter.vote_hash %}{{voter.vote_hash}} <span style="font-size:0.8em;">[<a href="{% url "helios.views.castvote_shortcut" vote_tinyhash=voter.vote_tinyhash %}">view</a>]</span>{% else %}&mdash;{% endif %}</tt></td>
+<td><tt style="font-size: 1.4em;">{% if voter.vote_hash %}{{voter.vote_hash}} <span style="font-size:0.8em;">[<a href="{% url "helios.views.castvote_shortcut" vote_tinyhash=voter.vote_tinyhash %}">{% trans "view" %}</a>]</span>{% else %}&mdash;{% endif %}</tt></td>
 </tr>
 {% endfor %}
 </table>
 
 {% else %}
 <br /><br />
-<em>no voters.</em>
+<em>{% trans "no voters" %}.</em>
 {% endif %}
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/voters_upload.html helios-server-translation/helios/templates/voters_upload.html
--- helios-server-original/helios/templates/voters_upload.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/voters_upload.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,30 +1,31 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Bulk Upload Voters <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Bulk Upload Voters" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h2>
 
 <form method="post" action="" id="upload_form" enctype="multipart/form-data">
   <p>
-    If you would like to specify your list of voters by name and email address,<br />
-    you can bulk upload a list of such voters here.<br /><br />
+    {% trans "If you would like to specify your list of voters by name and email address," %}<br />
+    {% trans "you can bulk upload a list of such voters here." %}<br /><br />
 
-    Please prepare a text file of comma-separated values with the fields:
+    {% trans "Please prepare a text file of comma-separated values with the fields:" %}
 </p>
 <pre>
-   &lt;unique_id&gt;,&lt;email&gt,&lt;full name&gt;
+   {% trans "&lt;unique_id&gt;,&lt;email&gt,&lt;full name&gt;" %}
 </pre>
 
 <p>
-For example:
+{% trans "For example:" %}
   </p>
   <pre>
-      benadida,ben@adida.net,Ben Adida
-      bobsmith,bob@acme.org,Bob Smith
+      {% trans "benadida,ben@adida.net,Ben Adida" context "example" %}
+      {% trans "bobsmith,bob@acme.org,Bob Smith" context "example" %}
       ...
   </pre> 
 
   <p>
-    The easiest way to prepare such a file is to use a spreadsheet program and to export as "CSV".
+    {% trans "The easiest way to prepare such a file is to use a spreadsheet program and to export as &quot;CSV&quot;." %}
   </p>
   {% if error %}
   <div style="color: red;">{{error}}</div>
@@ -32,15 +33,15 @@
     <input type="hidden" name="csrf_token" value="{{csrf_token}}" />
     
     <input type="file" name="voters_file" />
-    <input type="submit" value="go" />
+    <input type="submit" value={% trans "go" context "label bulk upload" %} />
 </form>
 
 <div id="processing">
 </div>
 
 <div id="done" style="display:none;">
-  OK, done uploading.<br />
-  You can now <a href="./manage">view the list of voters</a>.
+  {% trans "OK, done uploading." context "voter upload" %}<br />
+  {% trans "You can now" context "action follows" %} <a href="./manage">{% trans "view the list of voters" %}</a>.
 </div>
 
 {% endblock %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/templates/voters_upload_confirm.html helios-server-translation/helios/templates/voters_upload_confirm.html
--- helios-server-original/helios/templates/voters_upload_confirm.html	2019-10-28 03:15:16.932087172 -0300
+++ helios-server-translation/helios/templates/voters_upload_confirm.html	2019-10-29 03:15:07.844263284 -0300
@@ -1,14 +1,15 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block content %}
-  <h2 class="title">{{election.name}} &mdash; Bulk Upload Voters &mdash; Confirm<span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">back to election</a>]</span></h2>
+  <h2 class="title">{{election.name}} &mdash; {% trans "Bulk Upload Voters &mdash; Confirm" %} <span style="font-size:0.7em;">[<a href="{% url "helios.views.one_election_view" election.uuid %}">{% trans "back to election" %}</a>]</span></h2>
 
 <p>
-You have uploaded a file of voters. The first few rows of this file are:
+{% trans "You have uploaded a file of voters. The first few rows of this file are:" %}
 </p>
 
 <table>
-<tr><th>Voter Login</th><th>Email Address</th><th>Name</th></tr>
+<tr><th>{% trans "Voter Login" context "voter listing" %}</th><th>{% trans "Email Address" context "voter listing" %}</th><th>{% trans "Name" context "voter listing" %}</th></tr>
 {% for v in voters %}
 <tr><td>{{v.voter_id}}</td><td>{{v.email}}</td><td>{{v.name}}</td></tr>
 {% endfor %}
@@ -23,18 +24,18 @@
 </p>
 <br />
 
-<a href="{% url "helios.views.voters_upload_cancel" election.uuid %}">never mind, upload a different file</a>
+<a href="{% url "helios.views.voters_upload_cancel" election.uuid %}">{% trans "never mind, upload a different file" %}</a>
 </p>
 
 {% else %}
 <p></p>
 <form method="post" action="" id="upload_form">
-  Does this look right to you?
+  {% trans "Does this look right to you?" %}
   <input type="hidden" name="confirm_p" value="1" />
-  <input type="submit" value="Yes, let's go" />
+  <input type="submit" value={% trans "Yes, let's go" %} />
 </form>
 
-<a href="{% url "helios.views.voters_upload_cancel" election.uuid %}">no, let me upload a different file</a>
+<a href="{% url "helios.views.voters_upload_cancel" election.uuid %}">{% trans "no, let me upload a different file" %}</a>
 
 {% endif %}
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios/views.py helios-server-translation/helios/views.py
--- helios-server-original/helios/views.py	2019-10-28 03:15:16.931087172 -0300
+++ helios-server-translation/helios/views.py	2019-10-29 03:15:07.837263284 -0300
@@ -5,6 +5,7 @@
 Ben Adida (ben@adida.net)
 """
 
+from django.utils.translation import ugettext_lazy as _
 from django.core.urlresolvers import reverse
 from django.core.paginator import Paginator
 from django.core.exceptions import PermissionDenied
@@ -90,10 +91,10 @@
   
   users = User.objects.filter(admin_p=True)
   if len(users) == 0:
-    return HttpResponse("no admin users!")
+    return HttpResponse(_("no admin users!"))
 
   if len(users) == 0:
-    return HttpResponse("no users!")
+    return HttpResponse(_("no users!"))
 
   user = users[0]
   request.session['user'] = {'type' : user.user_type, 'user_id' : user.user_id}
@@ -150,9 +151,9 @@
 
 @trustee_check
 def trustee_keygenerator(request, election, trustee):
-  """
+  _("""
   A key generator with the current params, like the trustee home but without a specific election.
-  """
+  """)
   eg_params_json = utils.to_json(ELGAMAL_PARAMS_LD_OBJECT.toJSONDict())
 
   return render_template(request, "election_keygenerator", {'eg_params_json': eg_params_json, 'election': election, 'trustee': trustee})
@@ -160,7 +161,7 @@
 @login_required
 def elections_administered(request):
   if not can_create_election(request):
-    return HttpResponseForbidden('only an administrator has elections to administer')
+    return HttpResponseForbidden(_('only an administrator has elections to administer'))
   
   user = get_user(request)
   elections = Election.get_by_user_as_admin(user)
@@ -178,7 +179,7 @@
 @login_required
 def election_new(request):
   if not can_create_election(request):
-    return HttpResponseForbidden('only an administrator can create an election')
+    return HttpResponseForbidden(_('only an administrator can create an election'))
     
   error = None
   
@@ -210,9 +211,9 @@
           election.generate_trustee(ELGAMAL_PARAMS)
           return HttpResponseRedirect(settings.SECURE_URL_HOST + reverse(one_election_view, args=[election.uuid]))
         except IntegrityError:
-          error = "An election with short name %s already exists" % election_params['short_name']
+          error = _("An election with short name %s already exists") % election_params['short_name']
       else:
-        error = "No special characters allowed in the short name."
+        error = _("No special characters allowed in the short name.")
     
   return render_template(request, "election_new", {'election_form': election_form, 'error': error})
   
@@ -243,7 +244,7 @@
         election.save()
         return HttpResponseRedirect(settings.SECURE_URL_HOST + reverse(one_election_view, args=[election.uuid]))
       except IntegrityError:
-        error = "An election with short name %s already exists" % clean_data['short_name']
+        error = _("An election with short name %s already exists") % clean_data['short_name']
 
   return render_template(request, "election_edit", {'election_form' : election_form, 'election' : election, 'error': error})
 
@@ -328,13 +329,13 @@
   # status update message?
   if election.openreg:
     if election.voting_has_started:
-      status_update_message = u"Vote in %s" % election.name
+      status_update_message = _(u"Vote in %s") % election.name
     else:
-      status_update_message = u"Register to vote in %s" % election.name
+      status_update_message = _(u"Register to vote in %s") % election.name
 
   # result!
   if election.result:
-    status_update_message = u"Results are in for %s" % election.name
+    status_update_message = _(u"Results are in for %s") % election.name
   
   trustees = Trustee.get_by_election(election)
 
@@ -403,9 +404,9 @@
 
 @election_admin(frozen=False)
 def new_trustee_helios(request, election):
-  """
+  _("""
   Make Helios a trustee of the election
-  """
+  """)
   election.generate_trustee(ELGAMAL_PARAMS)
   return HttpResponseRedirect(settings.SECURE_URL_HOST + reverse(list_trustees_view, args=[election.uuid]))
   
@@ -434,7 +435,7 @@
   
   url = settings.SECURE_URL_HOST + reverse(trustee_login, args=[election.short_name, trustee.email, trustee.secret])
   
-  body = """
+  body = _("""
 
 You are a trustee for %s.
 
@@ -444,9 +445,9 @@
   
 --
 Helios  
-""" % (election.name, url)
+""") % (election.name, url)
 
-  utils.send_email(settings.SERVER_EMAIL, ["%s <%s>" % (trustee.name, trustee.email)], 'your trustee homepage for %s' % election.name, body)
+  utils.send_email(settings.SERVER_EMAIL, ["%s <%s>" % (trustee.name, trustee.email)], _('your trustee homepage for %s') % election.name, body)
 
   logging.info("URL %s " % url)
   return HttpResponseRedirect(settings.SECURE_URL_HOST + reverse(list_trustees_view, args = [election.uuid]))
@@ -735,6 +736,7 @@
       status_update_message = None
 
     # launch the verification task
+
     tasks.cast_vote_verify_and_store.delay(
       cast_vote_id = cast_vote.id,
       status_update_message = status_update_message)
@@ -1129,7 +1131,7 @@
     election.save()
 
     if request.POST.get('send_email', ''):
-      return HttpResponseRedirect("%s?%s" % (settings.SECURE_URL_HOST + reverse(voters_email, args=[election.uuid]),urllib.urlencode({'template': 'result'})))
+      return HttpResponseRedirect("%s?%s" % (settings.SECURE_URL_HOST + reverse(voters_email, args=[election.uuid]),urllib.urlencode({'template': _('result')})))
     else:
       return HttpResponseRedirect("%s" % (settings.SECURE_URL_HOST + reverse(one_election_view, args=[election.uuid])))
 
@@ -1313,7 +1315,7 @@
 
         return render_template(request, 'voters_upload_confirm', {'election': election, 'voters': voters, 'problems': problems})
       else:
-        return HttpResponseRedirect("%s?%s" % (settings.SECURE_URL_HOST + reverse(voters_upload, args=[election.uuid]), urllib.urlencode({'e':'no voter file specified, try again'})))
+        return HttpResponseRedirect("%s?%s" % (settings.SECURE_URL_HOST + reverse(voters_upload, args=[election.uuid]), urllib.urlencode({'e':_('no voter file specified, try again')})))
 
 @election_admin()
 def voters_upload_cancel(request, election):
@@ -1333,13 +1335,18 @@
   if not VOTERS_EMAIL:
     return HttpResponseRedirect(settings.SECURE_URL_HOST + reverse(one_election_view, args=[election.uuid]))
   TEMPLATES = [
-    ('vote', 'Time to Vote'),
-    ('simple', 'Simple'),
-    ('info', 'Additional Info'),
-    ('result', 'Election Result')
+    (_('vote'), _('Time to Vote')),
+    (_('simple'), _('Simple')),
+    (_('info'), _('Additional Info')),
+    (_('result'), _('Election Result'))
     ]
 
-  template = request.GET.get('template', 'vote')
+  template = request.GET.get('template', _('vote'))
+
+  print("requested template: " + str(template))
+  for t in TEMPLATES:
+    print("template in TEMPLATES: " + str(t[0]))
+
   if not template in [t[0] for t in TEMPLATES]:
     raise Exception("bad template")
 
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios_auth/auth_systems/password.py helios-server-translation/helios_auth/auth_systems/password.py
--- helios-server-original/helios_auth/auth_systems/password.py	2019-10-28 03:15:16.933087172 -0300
+++ helios-server-translation/helios_auth/auth_systems/password.py	2019-10-29 03:15:08.028263282 -0300
@@ -7,6 +7,7 @@
 from django.core.mail import send_mail
 from django.conf import settings
 from django.http import HttpResponseRedirect
+from django.utils.translation import ugettext_lazy as _
 
 import logging
 
@@ -26,8 +27,8 @@
   user.save()
 
 class LoginForm(forms.Form):
-  username = forms.CharField(max_length=50)
-  password = forms.CharField(widget=forms.PasswordInput(), max_length=100)
+  username = forms.CharField(max_length=50, label=_("Username"))
+  password = forms.CharField(widget=forms.PasswordInput(), max_length=100, label=_("Password"))
 
 def password_check(user, password):
   return (user and user.info['password'] == password)
@@ -61,7 +62,7 @@
           return HttpResponseRedirect(reverse(after))
       except User.DoesNotExist:
         pass
-      error = 'Bad Username or Password'
+      error = _("Bad Username or Password")
   
   return render_template(request, 'password/login', {'form': form, 'error': error})
     
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/helios_auth/templates/password/login.html helios-server-translation/helios_auth/templates/password/login.html
--- helios-server-original/helios_auth/templates/password/login.html	2019-10-28 03:15:16.934087172 -0300
+++ helios-server-translation/helios_auth/templates/password/login.html	2019-10-29 03:15:08.029263282 -0300
@@ -1,4 +1,5 @@
 {% extends TEMPLATE_BASE %}
+{% load i18n %}
 
 {% block header %}
 <h2>{{ settings.SITE_TITLE }} &mdash; Login</h2>
@@ -20,7 +21,7 @@
     {{form.as_table}}
 </table>
 <div>
-<label for="">&nbsp;</label><input type="submit" value="Login" />
+<label for="">&nbsp;</label><input type="submit" value={% trans "Login"%} />
 </div>
 </form>
 </p>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/default-sbv.html helios-server-translation/heliosbooth/default-sbv.html
--- helios-server-original/heliosbooth/default-sbv.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/default-sbv.html	2019-10-29 03:15:08.029263282 -0300
@@ -0,0 +1,157 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
+  <title>Helios Voting System</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script>
+  <script language="javascript" src="js/jquery-1.2.6.min.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+
+  <script language="javascript">
+    // required for jscrypto library
+    var JSCRYPTO_HOME = document.location.pathname.replace("single-ballot-verify.html", "js/jscrypto");
+  </script>
+
+  <script language="javascript" src="js/jscrypto/class.js"></script>  
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>
+  <script language="javascript" src="verifier.js"></script>
+</head>
+<body>
+<script language="javascript">
+$(document).ready(function() {
+    BigInt.setup(function() {
+       $('#verifier_loading').hide();
+
+       if (BigInt.is_dummy) {
+          $('#dummy_bigint').show();
+          return;
+       }
+
+       $('#verifier').show();
+       var election_url = $.query.get('election_url');
+       $('#election_url').val(election_url); 
+    }, function() {
+       $('#dummy_bigint').show();
+    });
+});    
+
+function result_append(str) {
+    $('#results').append(str).append("<br />");
+}
+
+function verify_single_ballot(election_url, audit_trail) {
+  var encrypted_vote_json = jQuery.secureEvalJSON(audit_trail);
+  
+  result_append("loading election...");
+
+  // quick and dirty detection of cast ballot
+  if (encrypted_vote_json['cast_at']) {
+    result_append("\n\nIt looks like you are trying to verify a cast ballot. That can't be done, only audited ballots can be verified.");
+    return;
+  }
+  
+  $('#loading').show();
+  
+  var after_computation = function(overall_result) {
+    result_append("<br />");
+    
+    $('#loading').hide();
+    
+    if (overall_result) {
+       result_append('SUCCESSFUL VERIFICATION, DONE!');
+    } else {
+       result_append('PROBLEM - THIS BALLOT DOES NOT VERIFY.');        
+    }
+  };
+
+  // the hash will be computed within the setup function call now
+  $.ajax({url: election_url, success: function(raw_json) {
+    if (window.Worker) {
+      var verifier_worker = new window.Worker("verifierworker.js");
+      verifier_worker.onmessage = function(event) {
+        if (event.data.type == 'log')
+          return console.log(event.data.msg);
+       
+        if (event.data.type == 'status')
+          return result_append(event.data.msg);
+
+        if (event.data.type == 'result')
+          return after_computation(event.data.result);
+      };
+
+      verifier_worker.postMessage({'type':'verify', 'election':raw_json, 'vote':encrypted_vote_json});
+    } else {
+      var overall_result = verify_ballot(raw_json, encrypted_vote_json, result_append);
+      after_computation(overall_result);
+    }
+  }, error: function() {
+      result_append('PROBLEM LOADING election. Are you sure you have the right election URL?<br />');
+
+      $('#loading').hide();
+      
+      result_append('PROBLEM - THIS BALLOT DOES NOT VERIFY.');        
+  }
+  });
+}
+
+</script>
+</head>
+<body>
+<div id="wrapper">
+<div id="banner">
+    Helios Single-Ballot Verifier
+</div>
+<div id="content">
+
+<div id="verifier_loading">
+Loading verifier...
+</div>
+
+<div id="dummy_bigint" style="display:none;">
+Your browser does not have the Java plugin installed.<br /><br />
+At this time, the Java plugin is required for browser-based ballot auditing, although it is not required for ballot preparation.
+</div>
+
+<div id="verifier" style="display:none;">
+<p style="font-size: 16pt;">
+    This single-ballot verifier lets you enter an audited ballot<br />and verify that it was prepared correctly.
+</p>
+
+<form onsubmit="try {verify_single_ballot(this.election_url.value, this.audit_trail.value);} catch (e) {E = e;} return false;">
+Enter the Election URL:
+    <input type="text" size="50" name="election_url" id="election_url" /><br />
+
+<p>
+Your Ballot:
+</p>
+
+<textarea name="audit_trail" cols="80" rows="7">
+</textarea><br /><br />
+<input type="submit" class="pretty" value="Verify" />
+</form>
+
+<br /><br />
+<div id="results">
+</div>
+<img id="loading" src="loading.gif" style="display:none;" />
+</div>
+</div>
+
+</div>
+
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/default-vote.html helios-server-translation/heliosbooth/default-vote.html
--- helios-server-original/heliosbooth/default-vote.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/default-vote.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,660 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" />
+  <title>Helios Voting Booth</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+
+<!--
+  <script language="javascript" src="js/jscrypto/class.js"></script>
+  <script language="javascript" src="js/jscrypto/bigint.dummy.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script>
+  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>-->
+  
+  <script language="javascript" src="js/20160507-helios-booth-compressed.js"></script>
+</head>
+<body>
+<div id="wrapper">
+<span style="float:right; padding: 7px 15px 5px 10px;">[<a href="javascript:BOOTH.exit();">exit</a>]</span>
+<div id="banner">Helios Voting Booth</div>
+<div id="content">
+
+<div id="header">
+</div>
+
+<script language="javascript">
+// utils
+BOOTH = {};
+
+BOOTH.setup_templates = function() {
+    if (BOOTH.templates_setup_p)
+        return;
+
+    var cache_bust = "?cb=" + new Date().getTime();
+
+    // Start static HTML language decision block
+    var lang = navigator.language || navigator.userLanguage;
+    var base = "templates/";
+    var file = "/election.html"; // we check only one file
+
+    var xhr = new XMLHttpRequest();
+    xhr.open('HEAD', base + lang + file, false);
+    xhr.send();
+
+    if (xhr.status != "404"){
+            // File in desired language exists; assume all others exist
+            // and make use of them
+            base += lang;
+    }
+
+    $('#header').setTemplateURL(base + "/header.html" + cache_bust);
+    $('#election_div').setTemplateURL(base + "/election.html" + cache_bust);
+    $('#question_div').setTemplateURL(base + "/question.html" + cache_bust);
+    $('#seal_div').setTemplateURL(base + "/seal.html" + cache_bust);
+    $('#audit_div').setTemplateURL(base + "/audit.html" + cache_bust);
+    $('#footer').setTemplateURL(base + "/footer.html" + cache_bust);
+
+    BOOTH.templates_setup_p = true;
+};
+
+// set up the message when navigating away
+BOOTH.started_p = false;
+
+window.onbeforeunload = function(evt) {
+  if (!BOOTH.started_p)
+    return;
+
+  if (typeof evt == 'undefined') {
+    evt = window.event;
+  }
+
+  var message = "If you leave this page with an in-progress ballot, your ballot will be lost.";
+
+  if (evt) {
+    evt.returnValue = message;
+  }
+
+  return message;
+};
+
+BOOTH.exit = function() {
+    if (confirm("Are you sure you want to exit the booth and lose all information about your current ballot?")) {
+        BOOTH.started_p = false;
+        window.location = BOOTH.election.cast_url;
+    }
+};
+
+BOOTH.setup_ballot = function(election) {
+  BOOTH.ballot = {};
+
+  // dirty markers for encryption (mostly for async)
+  BOOTH.dirty = [];
+
+  // each question starts out with an empty array answer
+  // and a dirty bit to make sure we encrypt
+  BOOTH.ballot.answers = [];
+  $(BOOTH.election.questions).each(function(i,x){
+    BOOTH.ballot.answers[i] = [];
+    BOOTH.dirty[i] = true;
+  });
+};
+
+// all ciphertexts to null
+BOOTH.reset_ciphertexts = function() {
+  _(BOOTH.encrypted_answers).each(function(enc_answer, ea_num) {
+    BOOTH.launch_async_encryption_answer(ea_num);
+  });
+};
+
+BOOTH.log = function(msg) {
+  if (typeof(console) != undefined)
+    console.log(msg);
+};
+
+BOOTH.setup_workers = function(election_raw_json) {
+  // async?
+  if (!BOOTH.synchronous) {
+      // prepare spots for encrypted answers
+      // and one worker per question
+      BOOTH.encrypted_answers = [];
+      BOOTH.answer_timestamps = [];
+      BOOTH.worker = new window.Worker("boothworker-single.js");
+      BOOTH.worker.postMessage({
+        'type': 'setup',
+        'election' : election_raw_json
+      });
+
+      BOOTH.worker.onmessage = function(event) {
+        // logging
+        if (event.data.type == 'log')
+          return BOOTH.log(event.data.msg);
+
+        // result of encryption
+        if (event.data.type == 'result') {
+          // this check ensures that race conditions
+          // don't screw up votes.
+          if (event.data.id == BOOTH.answer_timestamps[event.data.q_num]) {
+            BOOTH.encrypted_answers[event.data.q_num] = HELIOS.EncryptedAnswer.fromJSONObject(event.data.encrypted_answer, BOOTH.election);
+            BOOTH.log("got encrypted answer " + event.data.q_num);
+          } else {
+            BOOTH.log("no way jose");
+          }
+        }
+      };
+
+      $(BOOTH.election.questions).each(function(q_num, q) {
+        BOOTH.encrypted_answers[q_num] = null;
+      });
+  }
+};
+
+function escape_html(content) {
+  return $('<div/>').text(content).html();
+}
+
+BOOTH.setup_election = function(raw_json, election_metadata) {
+  // IMPORTANT: we use the raw JSON for safer hash computation
+  // so that we are using the JSON serialization of the SERVER
+  // to compute the hash, not the JSON serialization in JavaScript.
+  // the main reason for this is unicode representation: the Python approach
+  // appears to be safer.
+  BOOTH.election = HELIOS.Election.fromJSONString(raw_json);
+
+  // FIXME: we shouldn't need to set both, but right now we are doing so
+  // because different code uses each one. Bah. Need fixing.
+  BOOTH.election.hash = b64_sha256(raw_json);
+  BOOTH.election.election_hash = BOOTH.election.hash
+
+  // async?
+  BOOTH.setup_workers(raw_json);
+
+  document.title += ' - ' + BOOTH.election.name;
+
+  // escape election fields
+  $(['description', 'name']).each(function(i, field) {
+    BOOTH.election[field] = escape_html(BOOTH.election[field]);
+  });
+
+  // TODO: escape question and answers
+
+  // whether the election wants candidate order randomization or not
+  // we set up an ordering array so that the rest of the code is
+  // less error-prone. 
+  BOOTH.election.question_answer_orderings = [];
+  $(BOOTH.election.questions).each(function(i, question) {
+    var ordering = new Array(question.answers.length);
+
+    // initialize array so it is the identity permutation
+    $(ordering).each(function(j, answer) {ordering[j]=j;});
+
+    // if we want reordering, then we shuffle the array
+    if (election_metadata && election_metadata.randomize_answer_order) {
+      shuffleArray(ordering);
+    }
+
+    BOOTH.election.question_answer_orderings[i] = ordering;
+  });
+
+  $('#header').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
+  $('#footer').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
+  BOOTH.setup_ballot();
+};
+
+BOOTH.show = function(el) {
+  $('.panel').hide();
+  el.show();
+  return el;
+};
+
+BOOTH.show_election = function() {
+  BOOTH.show($('#election_div')).processTemplate({'election' : BOOTH.election});
+};
+
+BOOTH.launch_async_encryption_answer = function(question_num) {
+   BOOTH.answer_timestamps[question_num] = new Date().getTime();
+   BOOTH.encrypted_answers[question_num] = null;
+   BOOTH.dirty[question_num] = false;
+   BOOTH.worker.postMessage({
+      'type' : 'encrypt',
+      'q_num': question_num,
+      'answer' : BOOTH.ballot.answers[question_num],
+      'id' : BOOTH.answer_timestamps[question_num]
+   });
+};
+
+// check if the current question is ok
+BOOTH.validate_question = function(question_num) {
+    // check if enough answers are checked
+    if (BOOTH.ballot.answers[question_num].length < BOOTH.election.questions[question_num].min) {
+        alert('You need to select at least ' + BOOTH.election.questions[question_num].min + ' answer(s).');
+        return false;
+    }
+
+    // if we need to launch the worker, let's do it
+    if (!BOOTH.synchronous) {
+       // we need a unique ID for this to ensure that old results
+       // don't mess things up. Using timestamp.
+       // check if dirty
+       if (BOOTH.dirty[question_num]) {
+         BOOTH.launch_async_encryption_answer(question_num);
+       }
+    }
+    return true;
+};
+
+BOOTH.validate_and_confirm = function(question_num) {
+  if (BOOTH.validate_question(question_num)) {
+      BOOTH.seal_ballot();
+  }
+};
+
+BOOTH.next = function(question_num) {
+    if (BOOTH.validate_question(question_num)) {
+        BOOTH.show_question(question_num + 1);
+    }
+};
+
+BOOTH.previous = function(question_num) {
+    if (BOOTH.validate_question(question_num)) {
+        BOOTH.show_question(question_num - 1);
+    }
+};
+
+// http://stackoverflow.com/questions/2450954/how-to-randomize-shuffle-a-javascript-array
+function shuffleArray(array) {
+  var currentIndex = array.length
+    , temporaryValue
+    , randomIndex
+    ;
+
+  // While there remain elements to shuffle...
+  while (0 !== currentIndex) {
+     // Pick a remaining element...
+    randomIndex = Math.floor(Math.random() * currentIndex);
+    currentIndex -= 1;
+
+    // And swap it with the current element.
+    temporaryValue = array[currentIndex];
+    array[currentIndex] = array[randomIndex];
+    array[randomIndex] = temporaryValue;
+  }
+
+  return array;
+}
+
+BOOTH.show_question = function(question_num) {
+  BOOTH.started_p = true;
+
+  // the first time we hit the last question, we enable the review all button
+  if (question_num == BOOTH.election.questions.length -1)
+    BOOTH.all_questions_seen = true;
+
+  BOOTH.show_progress('1');
+  BOOTH.show($('#question_div')).processTemplate({'question_num' : question_num,
+                      'last_question_num' : BOOTH.election.questions.length - 1,
+                      'question' : BOOTH.election.questions[question_num], 'show_reviewall' : BOOTH.all_questions_seen,
+		      'answer_ordering': BOOTH.election.question_answer_orderings[question_num]
+                });
+
+  // fake clicking through the answers, to trigger the disabling if need be
+  // first we remove the answers array
+  var answer_array = BOOTH.ballot.answers[question_num];
+  BOOTH.ballot.answers[question_num] = [];
+
+  // we should not set the dirty bit here, so we save it away
+  var old_dirty = BOOTH.dirty[question_num];
+  $(answer_array).each(function(i, ans) {
+    BOOTH.click_checkbox_script(question_num, ans, true);
+  });
+  BOOTH.dirty[question_num] = old_dirty;
+};
+
+
+
+BOOTH.click_checkbox_script = function(question_num, answer_num) {
+  document.forms['answer_form']['answer_'+question_num+'_'+answer_num].click();
+};
+
+BOOTH.click_checkbox = function(question_num, answer_num, checked_p) {
+  // keep track of dirty answers that need encrypting
+  BOOTH.dirty[question_num] = true;
+
+  if (checked_p) {
+     // multiple click events shouldn't screw this up
+     if ($(BOOTH.ballot.answers[question_num]).index(answer_num) == -1)
+        BOOTH.ballot.answers[question_num].push(answer_num);
+
+     $('#answer_label_' + question_num + "_" + answer_num).addClass('selected');
+  } else {
+     BOOTH.ballot.answers[question_num] = UTILS.array_remove_value(BOOTH.ballot.answers[question_num], answer_num);
+     $('#answer_label_' + question_num + "_" + answer_num).removeClass('selected');
+  }
+
+  if (BOOTH.election.questions[question_num].max != null && BOOTH.ballot.answers[question_num].length >= BOOTH.election.questions[question_num].max) {
+     // disable the other checkboxes
+     $('.ballot_answer').each(function(i, checkbox) {
+        if (!checkbox.checked)
+            checkbox.disabled = true;
+     });
+
+     // do the warning only if the question allows more than one option, otherwise it's confusing
+     if (BOOTH.election.questions[question_num].max > 1) {
+        $('#warning_box').html("Maximum number of options selected.<br />To change your selection, please de-select a current selection first.");
+     }
+  } else {
+     // enable the other checkboxes
+     $('.ballot_answer').each(function(i, checkbox) {
+       checkbox.disabled = false;
+     });
+     $('#warning_box').html("");
+  }
+};
+
+BOOTH.show_processing_before = function(str_to_execute) {
+    $('#processing_div').html("<h3 align='center'>Processing...</h3>");
+    BOOTH.show($('#processing_div'));
+
+    // add a timeout so browsers like Safari actually display the processing message
+    setTimeout(str_to_execute, 250);
+};
+
+BOOTH.show_encryption_message_before = function(func_to_execute) {
+    BOOTH.show_progress('2');
+    BOOTH.show($('#encrypting_div'));
+
+    func_to_execute();
+};
+
+BOOTH.load_and_setup_election = function(election_url) {
+    // the hash will be computed within the setup function call now
+    $.get(election_url, function(raw_json) {
+        // let's also get the metadata
+        $.getJSON(election_url + "/meta", {}, function(election_metadata) {
+          BOOTH.election_metadata = election_metadata;
+          BOOTH.setup_election(raw_json, election_metadata);
+          BOOTH.show_election();
+          BOOTH.election_url = election_url;
+        });
+    });
+
+
+    if (USE_SJCL) {
+      // get more randomness from server
+      $.get(election_url + "/get-randomness", {}, function(raw_json) {
+        sjcl.random.addEntropy(JSON.parse(raw_json).randomness);
+      });
+    }
+};
+
+BOOTH.hide_progress = function() {
+  $('#progress_div').hide();
+};
+
+BOOTH.show_progress = function(step_num) {
+    $('#progress_div').show();
+    $(['1','2','3','4']).each(function(n, step) {
+        if (step == step_num)
+            $('#progress_' + step).attr('class', 'selected');
+        else
+            $('#progress_' + step).attr('class', 'unselected');
+    });
+};
+
+BOOTH.so_lets_go = function () {
+    BOOTH.hide_progress();
+
+    BOOTH.setup_templates();
+
+    // election URL
+    var election_url = $.query.get('election_url');
+    BOOTH.load_and_setup_election(election_url);
+};
+
+BOOTH.nojava = function() {
+  // in the case of Chrome, we get here when Java
+  // is disabled, instead of detecting the problem earlier.
+  // because navigator.javaEnabled() still returns true.
+  // $('#election_div').hide();
+  // $('#error_div').show();
+    USE_SJCL = true;
+    sjcl.random.startCollectors();
+    BigInt.setup(BOOTH.so_lets_go);
+};
+
+BOOTH.ready_p = false;
+
+$(document).ready(function() {
+    if (USE_SJCL) {
+      sjcl.random.startCollectors();
+    }
+
+    // we're asynchronous if we have SJCL and Worker
+    BOOTH.synchronous = !(USE_SJCL && window.Worker);
+
+    // we do in the browser only if it's asynchronous
+    BigInt.in_browser = !BOOTH.synchronous;
+
+    // set up dummy bigint for fast parsing and serialization
+    if (!BigInt.in_browser)
+      BigInt = BigIntDummy;
+
+    BigInt.setup(BOOTH.so_lets_go, BOOTH.nojava);
+});
+
+
+BOOTH.check_encryption_status = function() {
+  var progress = BOOTH.progress.progress();
+  if (progress == "" || progress == null)
+    progress = "0";
+
+  $('#percent_done').html(progress);
+};
+
+BOOTH._after_ballot_encryption = function() {
+    // if already serialized, use that, otherwise serialize
+    BOOTH.encrypted_vote_json = BOOTH.encrypted_ballot_serialized || JSON.stringify(BOOTH.encrypted_ballot.toJSONObject());
+
+    var do_hash = function() {
+       BOOTH.encrypted_ballot_hash = b64_sha256(BOOTH.encrypted_vote_json); // BOOTH.encrypted_ballot.get_hash();
+       window.setTimeout(show_cast, 0);
+    };
+
+    var show_cast = function() {
+      $('#seal_div').processTemplate({'cast_url': BOOTH.election.cast_url,
+        'encrypted_vote_json': BOOTH.encrypted_vote_json,
+        'encrypted_vote_hash' : BOOTH.encrypted_ballot_hash,
+        'election_uuid' : BOOTH.election.uuid,
+        'election_hash' : BOOTH.election_hash,
+        'election': BOOTH.election,
+        'election_metadata': BOOTH.election_metadata,
+        'questions' : BOOTH.election.questions,
+        'choices' : BALLOT.pretty_choices(BOOTH.election, BOOTH.ballot)});
+      BOOTH.show($('#seal_div'));
+      BOOTH.encrypted_vote_json = null;
+    };
+
+    window.setTimeout(do_hash, 0);
+};
+
+BOOTH.total_cycles_waited = 0;
+
+// wait for all workers to be done
+BOOTH.wait_for_ciphertexts = function() {
+    BOOTH.total_cycles_waited += 1;
+
+    var answers_done = _.reject(BOOTH.encrypted_answers, _.isNull);
+    var percentage_done = Math.round((100 * answers_done.length) / BOOTH.encrypted_answers.length);
+
+    if (BOOTH.total_cycles_waited > 250) {
+      alert('there appears to be a problem with the encryption process.\nPlease email help@heliosvoting.org and indicate that your encryption process froze at ' + percentage_done + '%');
+      return;
+    }
+
+    if (percentage_done < 100) {
+      setTimeout(BOOTH.wait_for_ciphertexts, 500);
+      $('#percent_done').html(percentage_done + '');
+      return;
+    }
+
+    BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromEncryptedAnswers(BOOTH.election, BOOTH.encrypted_answers);
+
+    BOOTH._after_ballot_encryption();
+};
+
+BOOTH.seal_ballot_raw = function() {
+    if (BOOTH.synchronous) {
+      BOOTH.progress = new UTILS.PROGRESS();
+      var progress_interval = setInterval("BOOTH.check_encryption_status()", 500);
+      BOOTH.encrypted_ballot = new HELIOS.EncryptedVote(BOOTH.election, BOOTH.ballot.answers, BOOTH.progress);
+      clearInterval(progress_interval);
+      BOOTH._after_ballot_encryption();
+    } else {
+      BOOTH.total_cycles_waited = 0;
+      BOOTH.wait_for_ciphertexts();
+    }
+};
+
+BOOTH.request_ballot_encryption = function() {
+    $.post(BOOTH.election_url + "/encrypt-ballot", {'answers_json': $.toJSON(BOOTH.ballot.answers)}, function(result) {
+      //BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromJSONObject($.secureEvalJSON(result), BOOTH.election);
+      // rather than deserialize and reserialize, which is inherently slow on browsers
+      // that already need to do network requests, just remove the plaintexts
+
+      BOOTH.encrypted_ballot_with_plaintexts_serialized = result;
+      var ballot_json_obj = $.secureEvalJSON(BOOTH.encrypted_ballot_with_plaintexts_serialized);
+      var answers = ballot_json_obj.answers;
+      for (var i=0; i<answers.length; i++) {
+         delete answers[i]['answer'];
+         delete answers[i]['randomness'];
+      }
+
+      BOOTH.encrypted_ballot_serialized = JSON.stringify(ballot_json_obj);
+
+      window.setTimeout(BOOTH._after_ballot_encryption, 0);
+    });
+};
+
+BOOTH.seal_ballot = function() {
+    BOOTH.show_progress('2');
+
+    // if we don't have the ability to do crypto in the browser,
+    // we use the server
+    if (!BigInt.in_browser) {
+      BOOTH.show_encryption_message_before(BOOTH.request_ballot_encryption, true);
+    } else {
+      BOOTH.show_encryption_message_before(BOOTH.seal_ballot_raw, true);
+      $('#percent_done_container').show();
+    }
+};
+
+BOOTH.audit_ballot = function() {
+    BOOTH.audit_trail = BOOTH.encrypted_ballot_with_plaintexts_serialized || $.toJSON(BOOTH.encrypted_ballot.get_audit_trail());
+
+    BOOTH.show($('#audit_div')).processTemplate({'audit_trail' : BOOTH.audit_trail, 'election_url' : BOOTH.election_url});
+};
+
+BOOTH.post_audited_ballot = function() {
+  $.post(BOOTH.election_url + "/post-audited-ballot", {'audited_ballot': BOOTH.audit_trail}, function(result) {
+    alert('This audited ballot has been posted.\nRemember, this vote will only be used for auditing and will not be tallied.\nClick "back to voting" and cast a new ballot to make sure your vote counts.');
+  });
+};
+
+BOOTH.cast_ballot = function() {
+    // show progress spinner
+    $('#loading_div').show();
+    $('#proceed_button').attr('disabled', 'disabled');
+
+    // at this point, we delete the plaintexts by resetting the ballot
+    BOOTH.setup_ballot(BOOTH.election);
+
+    // clear the plaintext from the encrypted
+    if (BOOTH.encrypted_ballot)
+       BOOTH.encrypted_ballot.clearPlaintexts();
+
+    BOOTH.encrypted_ballot_serialized = null;
+    BOOTH.encrypted_ballot_with_plaintexts_serialized = null;
+
+    // remove audit trail
+    BOOTH.audit_trail = null;
+
+    // we're ready to leave the site
+    BOOTH.started_p = false;
+
+    // submit the form
+    $('#send_ballot_form').submit();
+};
+
+BOOTH.show_receipt = function() {
+    UTILS.open_window_with_content("Your smart ballot tracker for " + BOOTH.election.name + ": " + BOOTH.encrypted_ballot_hash);
+};
+
+BOOTH.do_done = function() {
+  BOOTH.started_p = false;
+};
+
+</script>
+<div id="page">
+  <div id="progress_div" style="display:none; width: 500px; margin:auto;">
+      <table width="100%">
+          <tr><td id="progress_1">(1) Select</td><td id="progress_2">(2) Review</td><td id="progress_3">(3) Submit</td></tr>
+      </table>
+  </div>
+  <div id="election_div" class="panel">
+    <h3>Checking capabilities and loading election booth...</h3>
+    <div align="center"><img src="loading.gif" /><br />This may take up to 10 seconds</div>
+  </div>
+
+  <div id="error_div" class="panel" style="display: none;">
+    <h3>There's a problem</h3>
+    <p>
+      It appears that your browser does not have Java enabled. Helios needs Java to perform encryption within the browser.
+    </p>
+    <p>
+      You may be able to install Java by visiting <a target="_new" href="http://java.com">java.com</a>.
+    </p>
+  </div>
+
+  <div id="question_div" class="panel">
+  </div>
+
+  <div id="processing_div" class="panel" style="display:none;">
+      <h3 align="center">Processing....</h3>
+  </div>
+
+  <div id="encrypting_div" class="panel" style="display:none;">
+      <h3 align="center">Helios is now encrypting your ballot<br />
+          <img src="encrypting.gif" /> <span style="font-size:0.7em; display:none;" id="percent_done_container">(<span id="percent_done">0</span>%)</span></h3>
+
+      <p align="center"><b>This may take up to two minutes.</b>
+  </div>
+
+  <div id="seal_div" class="panel">
+  </div>
+
+  <div id="audit_div" class="panel">
+  </div>
+
+</div>
+
+<br clear="both" />
+</div>
+<div id="footer">&nbsp;</div>
+</div>
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/pt-BR-sbv.html helios-server-translation/heliosbooth/pt-BR-sbv.html
--- helios-server-original/heliosbooth/pt-BR-sbv.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/pt-BR-sbv.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,157 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="pt-BR" lang="pt-BR">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
+  <title>Sistema Helios</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script>
+  <script language="javascript" src="js/jquery-1.2.6.min.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+
+  <script language="javascript">
+    // required for jscrypto library
+    var JSCRYPTO_HOME = document.location.pathname.replace("single-ballot-verify.html", "js/jscrypto");
+  </script>
+
+  <script language="javascript" src="js/jscrypto/class.js"></script>  
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>
+  <script language="javascript" src="pt-BR-verifier.js"></script>
+</head>
+<body>
+<script language="javascript">
+$(document).ready(function() {
+    BigInt.setup(function() {
+       $('#verifier_loading').hide();
+
+       if (BigInt.is_dummy) {
+          $('#dummy_bigint').show();
+          return;
+       }
+
+       $('#verifier').show();
+       var election_url = $.query.get('election_url');
+       $('#election_url').val(election_url); 
+    }, function() {
+       $('#dummy_bigint').show();
+    });
+});    
+
+function result_append(str) {
+    $('#results').append(str).append("<br />");
+}
+
+function verify_single_ballot(election_url, audit_trail) {
+  var encrypted_vote_json = jQuery.secureEvalJSON(audit_trail);
+  
+  result_append("carregando eleição...");
+
+  // quick and dirty detection of cast ballot
+  if (encrypted_vote_json['cast_at']) {
+    result_append("\n\nParece que você está tentando verificar uma cédula depositada. Apenas cédulas auditadas podem ser verificadas.");
+    return;
+  }
+  
+  $('#loading').show();
+  
+  var after_computation = function(overall_result) {
+    result_append("<br />");
+    
+    $('#loading').hide();
+    
+    if (overall_result) {
+       result_append('SUCESSO - A CÉDULA CONFERE');
+    } else {
+       result_append('ERRO - A CÉDULA NÃO CONFERE.');        
+    }
+  };
+
+  // the hash will be computed within the setup function call now
+  $.ajax({url: election_url, success: function(raw_json) {
+    if (window.Worker) {
+      var verifier_worker = new window.Worker("pt-BR-verifierworker.js");
+      verifier_worker.onmessage = function(event) {
+        if (event.data.type == 'log')
+          return console.log(event.data.msg);
+       
+        if (event.data.type == 'status')
+          return result_append(event.data.msg);
+
+        if (event.data.type == 'result')
+          return after_computation(event.data.result);
+      };
+
+      verifier_worker.postMessage({'type':'verify', 'election':raw_json, 'vote':encrypted_vote_json});
+    } else {
+      var overall_result = verify_ballot(raw_json, encrypted_vote_json, result_append);
+      after_computation(overall_result);
+    }
+  }, error: function() {
+      result_append('Um problema ocorreu ao carregar a eleição. A URL da eleição está correta?<br />');
+
+      $('#loading').hide();
+      
+      result_append('ERRO - ESTA CÉDULA NÃO CONFERE.');        
+  }
+  });
+}
+
+</script>
+</head>
+<body>
+<div id="wrapper">
+<div id="banner">
+    Verificador de Cédulas do Helios
+</div>
+<div id="content">
+
+<div id="verifier_loading">
+Carregando verificador...
+</div>
+
+<div id="dummy_bigint" style="display:none;">
+O seu navegador não possui o plugin Java instalado.<br /><br />
+O plugin Java é necessário para auditoria local de cédulas.
+</div>
+
+<div id="verifier" style="display:none;">
+<p style="font-size: 16pt;">
+    Este verificador de cédulas permite que você informe uma cédula auditada<br />e verifique se ela foi preparada corretamente.
+</p>
+
+<form onsubmit="try {verify_single_ballot(this.election_url.value, this.audit_trail.value);} catch (e) {E = e;} return false;">
+URL da eleição:
+    <input type="text" size="50" name="election_url" id="election_url" /><br />
+
+<p>
+Sua cédula:
+</p>
+
+<textarea name="audit_trail" cols="80" rows="7">
+</textarea><br /><br />
+<input type="submit" class="pretty" value="Verificar" />
+</form>
+
+<br /><br />
+<div id="results">
+</div>
+<img id="loading" src="loading.gif" style="display:none;" />
+</div>
+</div>
+
+</div>
+
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/pt-BR-verifier.js helios-server-translation/heliosbooth/pt-BR-verifier.js
--- helios-server-original/heliosbooth/pt-BR-verifier.js	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/pt-BR-verifier.js	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,58 @@
+// helper functions for verifying a ballot
+// assumes all of Helios machinery is loaded
+
+function verify_ballot(election_raw_json, encrypted_vote_json, status_cb) {
+    var overall_result = true;
+    try {
+	election = HELIOS.Election.fromJSONString(election_raw_json);
+	var election_hash = election.get_hash();
+	status_cb("o código da eleição é " + election_hash);
+	
+	// display ballot fingerprint
+	encrypted_vote = HELIOS.EncryptedVote.fromJSONObject(encrypted_vote_json, election);
+	status_cb("número de rastreio da cédula é " + encrypted_vote.get_hash());
+	
+      // check the hash
+      if (election_hash == encrypted_vote.election_hash) {
+          status_cb("cédula e eleição correspondem");
+      } else {
+          overall_result = false;
+          status_cb("ERRO = cédula e eleição não correspondem");          
+      }
+      
+      // display the ballot as it is claimed to be
+      status_cb("Conteúdo da Cédula:");
+      _(election.questions).each(function(q, qnum) {
+	      if (q.tally_type != "homomorphic") {
+		  status_cb("AVISO: a totalização não é homomórfica. Como este verificador lida apenas com cédulas homomórficas é possível que a verificação venha a falhar.");
+	      }
+        
+	      var answer_pretty_list = _(encrypted_vote.encrypted_answers[qnum].answer).map(function(aindex, anum) {
+		      return q.answers[aindex];
+		  });
+	      status_cb("Pleito #" + (qnum+1) + " - " + q.short_name + " : " + answer_pretty_list.join(", "));
+      });
+      
+      // verify the encryption
+      if (encrypted_vote.verifyEncryption(election.questions, election.public_key)) {
+          status_cb("Cifração Verificada");
+      } else {
+          overall_result = false;
+          status_cb("ERRO = Cifração não corresponde.");
+      }
+      
+      // verify the proofs
+      if (encrypted_vote.verifyProofs(election.public_key, function(ea_num, choice_num, result) {
+      })) {
+          status_cb("Provas conferem.");
+      } else {
+          overall_result = false;
+          status_cb("ERRO = Provas não conferem.");
+      }
+    } catch (e) {
+      status_cb('erro ao verificar estruturas de dados da eleição ou da cédula, entradas mal-formatadas: ' + e.toString());
+      overall_result=false;
+    }
+
+    return overall_result;
+}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/pt-BR-verifierworker.js helios-server-translation/heliosbooth/pt-BR-verifierworker.js
--- helios-server-original/heliosbooth/pt-BR-verifierworker.js	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/pt-BR-verifierworker.js	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,61 @@
+/*
+ * JavaScript HTML 5 Worker for BOOTH
+ */
+
+// import needed resources
+importScripts("js/underscore-min.js");
+
+importScripts("js/jscrypto/jsbn.js",
+	      "js/jscrypto/jsbn2.js",
+	      "js/jscrypto/sjcl.js",
+	      "js/jscrypto/class.js",
+	      "js/jscrypto/bigint.js",
+	      "js/jscrypto/random.js",
+	      "js/jscrypto/elgamal.js",
+	      "js/jscrypto/sha1.js",
+	      "js/jscrypto/sha2.js",
+	      "js/jscrypto/helios.js",
+	      "pt-BR-verifier.js");
+
+var console = {
+    'log' : function(msg) {
+	self.postMessage({'type':'log','msg':msg});
+    }
+};
+
+var status_update = function(msg) {
+    self.postMessage({'type' : 'status', 'msg' : msg});
+};
+
+var ELECTION = null;
+var VOTE = null;
+
+function do_verify(message) {
+    console.log("verifying!");
+
+    // json string
+    ELECTION = message.election;
+
+    // json object
+    VOTE = message.vote;
+
+    var result = verify_ballot(ELECTION, VOTE, status_update);
+
+    // send the result back
+    self.postMessage({
+	    'type': 'result',
+		'result': result});
+}
+
+// receive either
+// a) an election and an integer position of the question
+// that this worker will be used to encrypt
+// {'type': 'setup', 'question_num' : 2, 'election' : election_json}
+//
+// b) an answer that needs encrypting
+// {'type': 'encrypt', 'answer' : answer_json}
+//
+self.onmessage = function(event) {
+    // dispatch to method
+    self['do_' + event.data.type](event.data);
+}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/pt-BR-vote.html helios-server-translation/heliosbooth/pt-BR-vote.html
--- helios-server-original/heliosbooth/pt-BR-vote.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/pt-BR-vote.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,660 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="pt-BR" lang="pt-BR">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" />
+  <title>Cabine de Votação</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+
+<!--
+  <script language="javascript" src="js/jscrypto/class.js"></script>
+  <script language="javascript" src="js/jscrypto/bigint.dummy.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script>
+  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>-->
+  
+  <script language="javascript" src="js/20160507-helios-booth-compressed.js"></script>
+</head>
+<body>
+<div id="wrapper">
+<span style="float:right; padding: 7px 15px 5px 10px;">[<a href="javascript:BOOTH.exit();">sair</a>]</span>
+<div id="banner">Cabine de Votação do Helios</div>
+<div id="content">
+
+<div id="header">
+</div>
+
+<script language="javascript">
+// utils
+BOOTH = {};
+
+BOOTH.setup_templates = function() {
+    if (BOOTH.templates_setup_p)
+        return;
+
+    var cache_bust = "?cb=" + new Date().getTime();
+
+    // Start static HTML language decision block
+    var lang = navigator.language || navigator.userLanguage;
+    var base = "templates/";
+    var file = "/election.html"; // we check only one file
+
+    var xhr = new XMLHttpRequest();
+    xhr.open('HEAD', base + lang + file, false);
+    xhr.send();
+
+    if (xhr.status != "404"){
+            // File in desired language exists; assume all others exist
+            // and make use of them
+            base += lang;
+    }
+
+    $('#header').setTemplateURL(base + "/header.html" + cache_bust);
+    $('#election_div').setTemplateURL(base + "/election.html" + cache_bust);
+    $('#question_div').setTemplateURL(base + "/question.html" + cache_bust);
+    $('#seal_div').setTemplateURL(base + "/seal.html" + cache_bust);
+    $('#audit_div').setTemplateURL(base + "/audit.html" + cache_bust);
+    $('#footer').setTemplateURL(base + "/footer.html" + cache_bust);
+
+    BOOTH.templates_setup_p = true;
+};
+
+// set up the message when navigating away
+BOOTH.started_p = false;
+
+window.onbeforeunload = function(evt) {
+  if (!BOOTH.started_p)
+    return;
+
+  if (typeof evt == 'undefined') {
+    evt = window.event;
+  }
+
+  var message = "Ao deixar esta página durante o processo de votação você irá perder a cédula de votação atual.";
+
+  if (evt) {
+    evt.returnValue = message;
+  }
+
+  return message;
+};
+
+BOOTH.exit = function() {
+    if (confirm("Deseja realmente sair da cabine de votação e perder toda a informação da cédula atual?")) {
+        BOOTH.started_p = false;
+        window.location = BOOTH.election.cast_url;
+    }
+};
+
+BOOTH.setup_ballot = function(election) {
+  BOOTH.ballot = {};
+
+  // dirty markers for encryption (mostly for async)
+  BOOTH.dirty = [];
+
+  // each question starts out with an empty array answer
+  // and a dirty bit to make sure we encrypt
+  BOOTH.ballot.answers = [];
+  $(BOOTH.election.questions).each(function(i,x){
+    BOOTH.ballot.answers[i] = [];
+    BOOTH.dirty[i] = true;
+  });
+};
+
+// all ciphertexts to null
+BOOTH.reset_ciphertexts = function() {
+  _(BOOTH.encrypted_answers).each(function(enc_answer, ea_num) {
+    BOOTH.launch_async_encryption_answer(ea_num);
+  });
+};
+
+BOOTH.log = function(msg) {
+  if (typeof(console) != undefined)
+    console.log(msg);
+};
+
+BOOTH.setup_workers = function(election_raw_json) {
+  // async?
+  if (!BOOTH.synchronous) {
+      // prepare spots for encrypted answers
+      // and one worker per question
+      BOOTH.encrypted_answers = [];
+      BOOTH.answer_timestamps = [];
+      BOOTH.worker = new window.Worker("boothworker-single.js");
+      BOOTH.worker.postMessage({
+        'type': 'setup',
+        'election' : election_raw_json
+      });
+
+      BOOTH.worker.onmessage = function(event) {
+        // logging
+        if (event.data.type == 'log')
+          return BOOTH.log(event.data.msg);
+
+        // result of encryption
+        if (event.data.type == 'result') {
+          // this check ensures that race conditions
+          // don't screw up votes.
+          if (event.data.id == BOOTH.answer_timestamps[event.data.q_num]) {
+            BOOTH.encrypted_answers[event.data.q_num] = HELIOS.EncryptedAnswer.fromJSONObject(event.data.encrypted_answer, BOOTH.election);
+            BOOTH.log("got encrypted answer " + event.data.q_num);
+          } else {
+            BOOTH.log("no way jose");
+          }
+        }
+      };
+
+      $(BOOTH.election.questions).each(function(q_num, q) {
+        BOOTH.encrypted_answers[q_num] = null;
+      });
+  }
+};
+
+function escape_html(content) {
+  return $('<div/>').text(content).html();
+}
+
+BOOTH.setup_election = function(raw_json, election_metadata) {
+  // IMPORTANT: we use the raw JSON for safer hash computation
+  // so that we are using the JSON serialization of the SERVER
+  // to compute the hash, not the JSON serialization in JavaScript.
+  // the main reason for this is unicode representation: the Python approach
+  // appears to be safer.
+  BOOTH.election = HELIOS.Election.fromJSONString(raw_json);
+
+  // FIXME: we shouldn't need to set both, but right now we are doing so
+  // because different code uses each one. Bah. Need fixing.
+  BOOTH.election.hash = b64_sha256(raw_json);
+  BOOTH.election.election_hash = BOOTH.election.hash
+
+  // async?
+  BOOTH.setup_workers(raw_json);
+
+  document.title += ' - ' + BOOTH.election.name;
+
+  // escape election fields
+  $(['description', 'name']).each(function(i, field) {
+    BOOTH.election[field] = escape_html(BOOTH.election[field]);
+  });
+
+  // TODO: escape question and answers
+
+  // whether the election wants candidate order randomization or not
+  // we set up an ordering array so that the rest of the code is
+  // less error-prone. 
+  BOOTH.election.question_answer_orderings = [];
+  $(BOOTH.election.questions).each(function(i, question) {
+    var ordering = new Array(question.answers.length);
+
+    // initialize array so it is the identity permutation
+    $(ordering).each(function(j, answer) {ordering[j]=j;});
+
+    // if we want reordering, then we shuffle the array
+    if (election_metadata && election_metadata.randomize_answer_order) {
+      shuffleArray(ordering);
+    }
+
+    BOOTH.election.question_answer_orderings[i] = ordering;
+  });
+
+  $('#header').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
+  $('#footer').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
+  BOOTH.setup_ballot();
+};
+
+BOOTH.show = function(el) {
+  $('.panel').hide();
+  el.show();
+  return el;
+};
+
+BOOTH.show_election = function() {
+  BOOTH.show($('#election_div')).processTemplate({'election' : BOOTH.election});
+};
+
+BOOTH.launch_async_encryption_answer = function(question_num) {
+   BOOTH.answer_timestamps[question_num] = new Date().getTime();
+   BOOTH.encrypted_answers[question_num] = null;
+   BOOTH.dirty[question_num] = false;
+   BOOTH.worker.postMessage({
+      'type' : 'encrypt',
+      'q_num': question_num,
+      'answer' : BOOTH.ballot.answers[question_num],
+      'id' : BOOTH.answer_timestamps[question_num]
+   });
+};
+
+// check if the current question is ok
+BOOTH.validate_question = function(question_num) {
+    // check if enough answers are checked
+    if (BOOTH.ballot.answers[question_num].length < BOOTH.election.questions[question_num].min) {
+        alert('Você precisa selecionar pelo menos ' + BOOTH.election.questions[question_num].min + '.');
+        return false;
+    }
+
+    // if we need to launch the worker, let's do it
+    if (!BOOTH.synchronous) {
+       // we need a unique ID for this to ensure that old results
+       // don't mess things up. Using timestamp.
+       // check if dirty
+       if (BOOTH.dirty[question_num]) {
+         BOOTH.launch_async_encryption_answer(question_num);
+       }
+    }
+    return true;
+};
+
+BOOTH.validate_and_confirm = function(question_num) {
+  if (BOOTH.validate_question(question_num)) {
+      BOOTH.seal_ballot();
+  }
+};
+
+BOOTH.next = function(question_num) {
+    if (BOOTH.validate_question(question_num)) {
+        BOOTH.show_question(question_num + 1);
+    }
+};
+
+BOOTH.previous = function(question_num) {
+    if (BOOTH.validate_question(question_num)) {
+        BOOTH.show_question(question_num - 1);
+    }
+};
+
+// http://stackoverflow.com/questions/2450954/how-to-randomize-shuffle-a-javascript-array
+function shuffleArray(array) {
+  var currentIndex = array.length
+    , temporaryValue
+    , randomIndex
+    ;
+
+  // While there remain elements to shuffle...
+  while (0 !== currentIndex) {
+     // Pick a remaining element...
+    randomIndex = Math.floor(Math.random() * currentIndex);
+    currentIndex -= 1;
+
+    // And swap it with the current element.
+    temporaryValue = array[currentIndex];
+    array[currentIndex] = array[randomIndex];
+    array[randomIndex] = temporaryValue;
+  }
+
+  return array;
+}
+
+BOOTH.show_question = function(question_num) {
+  BOOTH.started_p = true;
+
+  // the first time we hit the last question, we enable the review all button
+  if (question_num == BOOTH.election.questions.length -1)
+    BOOTH.all_questions_seen = true;
+
+  BOOTH.show_progress('1');
+  BOOTH.show($('#question_div')).processTemplate({'question_num' : question_num,
+                      'last_question_num' : BOOTH.election.questions.length - 1,
+                      'question' : BOOTH.election.questions[question_num], 'show_reviewall' : BOOTH.all_questions_seen,
+		      'answer_ordering': BOOTH.election.question_answer_orderings[question_num]
+                });
+
+  // fake clicking through the answers, to trigger the disabling if need be
+  // first we remove the answers array
+  var answer_array = BOOTH.ballot.answers[question_num];
+  BOOTH.ballot.answers[question_num] = [];
+
+  // we should not set the dirty bit here, so we save it away
+  var old_dirty = BOOTH.dirty[question_num];
+  $(answer_array).each(function(i, ans) {
+    BOOTH.click_checkbox_script(question_num, ans, true);
+  });
+  BOOTH.dirty[question_num] = old_dirty;
+};
+
+
+
+BOOTH.click_checkbox_script = function(question_num, answer_num) {
+  document.forms['answer_form']['answer_'+question_num+'_'+answer_num].click();
+};
+
+BOOTH.click_checkbox = function(question_num, answer_num, checked_p) {
+  // keep track of dirty answers that need encrypting
+  BOOTH.dirty[question_num] = true;
+
+  if (checked_p) {
+     // multiple click events shouldn't screw this up
+     if ($(BOOTH.ballot.answers[question_num]).index(answer_num) == -1)
+        BOOTH.ballot.answers[question_num].push(answer_num);
+
+     $('#answer_label_' + question_num + "_" + answer_num).addClass('selected');
+  } else {
+     BOOTH.ballot.answers[question_num] = UTILS.array_remove_value(BOOTH.ballot.answers[question_num], answer_num);
+     $('#answer_label_' + question_num + "_" + answer_num).removeClass('selected');
+  }
+
+  if (BOOTH.election.questions[question_num].max != null && BOOTH.ballot.answers[question_num].length >= BOOTH.election.questions[question_num].max) {
+     // disable the other checkboxes
+     $('.ballot_answer').each(function(i, checkbox) {
+        if (!checkbox.checked)
+            checkbox.disabled = true;
+     });
+
+     // do the warning only if the question allows more than one option, otherwise it's confusing
+     if (BOOTH.election.questions[question_num].max > 1) {
+        $('#warning_box').html("O número máximo de alternativas já se encontra selecionado.<br />Para alterar sua seleção atual, remova ao menos uma das alternativas selecionadas.");
+     }
+  } else {
+     // enable the other checkboxes
+     $('.ballot_answer').each(function(i, checkbox) {
+       checkbox.disabled = false;
+     });
+     $('#warning_box').html("");
+  }
+};
+
+BOOTH.show_processing_before = function(str_to_execute) {
+    $('#processing_div').html("<h3 align='center'>Processando...</h3>");
+    BOOTH.show($('#processing_div'));
+
+    // add a timeout so browsers like Safari actually display the processing message
+    setTimeout(str_to_execute, 250);
+};
+
+BOOTH.show_encryption_message_before = function(func_to_execute) {
+    BOOTH.show_progress('2');
+    BOOTH.show($('#encrypting_div'));
+
+    func_to_execute();
+};
+
+BOOTH.load_and_setup_election = function(election_url) {
+    // the hash will be computed within the setup function call now
+    $.get(election_url, function(raw_json) {
+        // let's also get the metadata
+        $.getJSON(election_url + "/meta", {}, function(election_metadata) {
+          BOOTH.election_metadata = election_metadata;
+          BOOTH.setup_election(raw_json, election_metadata);
+          BOOTH.show_election();
+          BOOTH.election_url = election_url;
+        });
+    });
+
+
+    if (USE_SJCL) {
+      // get more randomness from server
+      $.get(election_url + "/get-randomness", {}, function(raw_json) {
+        sjcl.random.addEntropy(JSON.parse(raw_json).randomness);
+      });
+    }
+};
+
+BOOTH.hide_progress = function() {
+  $('#progress_div').hide();
+};
+
+BOOTH.show_progress = function(step_num) {
+    $('#progress_div').show();
+    $(['1','2','3','4']).each(function(n, step) {
+        if (step == step_num)
+            $('#progress_' + step).attr('class', 'selected');
+        else
+            $('#progress_' + step).attr('class', 'unselected');
+    });
+};
+
+BOOTH.so_lets_go = function () {
+    BOOTH.hide_progress();
+
+    BOOTH.setup_templates();
+
+    // election URL
+    var election_url = $.query.get('election_url');
+    BOOTH.load_and_setup_election(election_url);
+};
+
+BOOTH.nojava = function() {
+  // in the case of Chrome, we get here when Java
+  // is disabled, instead of detecting the problem earlier.
+  // because navigator.javaEnabled() still returns true.
+  // $('#election_div').hide();
+  // $('#error_div').show();
+    USE_SJCL = true;
+    sjcl.random.startCollectors();
+    BigInt.setup(BOOTH.so_lets_go);
+};
+
+BOOTH.ready_p = false;
+
+$(document).ready(function() {
+    if (USE_SJCL) {
+      sjcl.random.startCollectors();
+    }
+
+    // we're asynchronous if we have SJCL and Worker
+    BOOTH.synchronous = !(USE_SJCL && window.Worker);
+
+    // we do in the browser only if it's asynchronous
+    BigInt.in_browser = !BOOTH.synchronous;
+
+    // set up dummy bigint for fast parsing and serialization
+    if (!BigInt.in_browser)
+      BigInt = BigIntDummy;
+
+    BigInt.setup(BOOTH.so_lets_go, BOOTH.nojava);
+});
+
+
+BOOTH.check_encryption_status = function() {
+  var progress = BOOTH.progress.progress();
+  if (progress == "" || progress == null)
+    progress = "0";
+
+  $('#percent_done').html(progress);
+};
+
+BOOTH._after_ballot_encryption = function() {
+    // if already serialized, use that, otherwise serialize
+    BOOTH.encrypted_vote_json = BOOTH.encrypted_ballot_serialized || JSON.stringify(BOOTH.encrypted_ballot.toJSONObject());
+
+    var do_hash = function() {
+       BOOTH.encrypted_ballot_hash = b64_sha256(BOOTH.encrypted_vote_json); // BOOTH.encrypted_ballot.get_hash();
+       window.setTimeout(show_cast, 0);
+    };
+
+    var show_cast = function() {
+      $('#seal_div').processTemplate({'cast_url': BOOTH.election.cast_url,
+        'encrypted_vote_json': BOOTH.encrypted_vote_json,
+        'encrypted_vote_hash' : BOOTH.encrypted_ballot_hash,
+        'election_uuid' : BOOTH.election.uuid,
+        'election_hash' : BOOTH.election_hash,
+        'election': BOOTH.election,
+        'election_metadata': BOOTH.election_metadata,
+        'questions' : BOOTH.election.questions,
+        'choices' : BALLOT.pretty_choices(BOOTH.election, BOOTH.ballot)});
+      BOOTH.show($('#seal_div'));
+      BOOTH.encrypted_vote_json = null;
+    };
+
+    window.setTimeout(do_hash, 0);
+};
+
+BOOTH.total_cycles_waited = 0;
+
+// wait for all workers to be done
+BOOTH.wait_for_ciphertexts = function() {
+    BOOTH.total_cycles_waited += 1;
+
+    var answers_done = _.reject(BOOTH.encrypted_answers, _.isNull);
+    var percentage_done = Math.round((100 * answers_done.length) / BOOTH.encrypted_answers.length);
+
+    if (BOOTH.total_cycles_waited > 250) {
+      alert('parece haver um problema no processo de cifragem.\nEnvie um email para help@heliosvoting.org informando que o processo de votação foi interrompido em ' + percentage_done + '%');
+      return;
+    }
+
+    if (percentage_done < 100) {
+      setTimeout(BOOTH.wait_for_ciphertexts, 500);
+      $('#percent_done').html(percentage_done + '');
+      return;
+    }
+
+    BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromEncryptedAnswers(BOOTH.election, BOOTH.encrypted_answers);
+
+    BOOTH._after_ballot_encryption();
+};
+
+BOOTH.seal_ballot_raw = function() {
+    if (BOOTH.synchronous) {
+      BOOTH.progress = new UTILS.PROGRESS();
+      var progress_interval = setInterval("BOOTH.check_encryption_status()", 500);
+      BOOTH.encrypted_ballot = new HELIOS.EncryptedVote(BOOTH.election, BOOTH.ballot.answers, BOOTH.progress);
+      clearInterval(progress_interval);
+      BOOTH._after_ballot_encryption();
+    } else {
+      BOOTH.total_cycles_waited = 0;
+      BOOTH.wait_for_ciphertexts();
+    }
+};
+
+BOOTH.request_ballot_encryption = function() {
+    $.post(BOOTH.election_url + "/encrypt-ballot", {'answers_json': $.toJSON(BOOTH.ballot.answers)}, function(result) {
+      //BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromJSONObject($.secureEvalJSON(result), BOOTH.election);
+      // rather than deserialize and reserialize, which is inherently slow on browsers
+      // that already need to do network requests, just remove the plaintexts
+
+      BOOTH.encrypted_ballot_with_plaintexts_serialized = result;
+      var ballot_json_obj = $.secureEvalJSON(BOOTH.encrypted_ballot_with_plaintexts_serialized);
+      var answers = ballot_json_obj.answers;
+      for (var i=0; i<answers.length; i++) {
+         delete answers[i]['answer'];
+         delete answers[i]['randomness'];
+      }
+
+      BOOTH.encrypted_ballot_serialized = JSON.stringify(ballot_json_obj);
+
+      window.setTimeout(BOOTH._after_ballot_encryption, 0);
+    });
+};
+
+BOOTH.seal_ballot = function() {
+    BOOTH.show_progress('2');
+
+    // if we don't have the ability to do crypto in the browser,
+    // we use the server
+    if (!BigInt.in_browser) {
+      BOOTH.show_encryption_message_before(BOOTH.request_ballot_encryption, true);
+    } else {
+      BOOTH.show_encryption_message_before(BOOTH.seal_ballot_raw, true);
+      $('#percent_done_container').show();
+    }
+};
+
+BOOTH.audit_ballot = function() {
+    BOOTH.audit_trail = BOOTH.encrypted_ballot_with_plaintexts_serialized || $.toJSON(BOOTH.encrypted_ballot.get_audit_trail());
+
+    BOOTH.show($('#audit_div')).processTemplate({'audit_trail' : BOOTH.audit_trail, 'election_url' : BOOTH.election_url});
+};
+
+BOOTH.post_audited_ballot = function() {
+  $.post(BOOTH.election_url + "/post-audited-ballot", {'audited_ballot': BOOTH.audit_trail}, function(result) {
+    alert('Esta cédula auditada foi postada.\nLembre-se, o voto nela contido será usado apenas para fins de auditoria e não participará da totalização.\nClique em "voltar para a votação" e assegure-se de enviar seu voto para que ele seja contabilizado.');
+  });
+};
+
+BOOTH.cast_ballot = function() {
+    // show progress spinner
+    $('#loading_div').show();
+    $('#proceed_button').attr('disabled', 'disabled');
+
+    // at this point, we delete the plaintexts by resetting the ballot
+    BOOTH.setup_ballot(BOOTH.election);
+
+    // clear the plaintext from the encrypted
+    if (BOOTH.encrypted_ballot)
+       BOOTH.encrypted_ballot.clearPlaintexts();
+
+    BOOTH.encrypted_ballot_serialized = null;
+    BOOTH.encrypted_ballot_with_plaintexts_serialized = null;
+
+    // remove audit trail
+    BOOTH.audit_trail = null;
+
+    // we're ready to leave the site
+    BOOTH.started_p = false;
+
+    // submit the form
+    $('#send_ballot_form').submit();
+};
+
+BOOTH.show_receipt = function() {
+    UTILS.open_window_with_content("O número de rastreio de sua cédula para a votação " + BOOTH.election.name + ": " + BOOTH.encrypted_ballot_hash);
+};
+
+BOOTH.do_done = function() {
+  BOOTH.started_p = false;
+};
+
+</script>
+<div id="page">
+  <div id="progress_div" style="display:none; width: 500px; margin:auto;">
+      <table width="100%">
+          <tr><td id="progress_1">(1) Escolha</td><td id="progress_2">(2) Revise</td><td id="progress_3">(3) Envie</td></tr>
+      </table>
+  </div>
+  <div id="election_div" class="panel">
+    <h3>Verificando capacidades e carregando a cabine de votação...</h3>
+    <div align="center"><img src="loading.gif" /><br />Este processo pode demorar alguns segundos</div>
+  </div>
+
+  <div id="error_div" class="panel" style="display: none;">
+    <h3>Um problema ocorreu</h3>
+    <p>
+      Aparentemente o seu navegador não possui o Java habilitado. O Helios requer Java para realizar as cifragens necessárias a partir do seu navegador.
+    </p>
+    <p>
+      Você pode instalar o Java visitando <a target="_new" href="http://java.com">java.com</a>.
+    </p>
+  </div>
+
+  <div id="question_div" class="panel">
+  </div>
+
+  <div id="processing_div" class="panel" style="display:none;">
+      <h3 align="center">Processando....</h3>
+  </div>
+
+  <div id="encrypting_div" class="panel" style="display:none;">
+      <h3 align="center">O Helios está agora cifrando sua cédula<br />
+          <img src="encrypting.gif" /> <span style="font-size:0.7em; display:none;" id="percent_done_container">(<span id="percent_done">0</span>%)</span></h3>
+
+      <p align="center"><b>Este processo pode demorar alguns minutos.</b>
+  </div>
+
+  <div id="seal_div" class="panel">
+  </div>
+
+  <div id="audit_div" class="panel">
+  </div>
+
+</div>
+
+<br clear="both" />
+</div>
+<div id="footer">&nbsp;</div>
+</div>
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/single-ballot-verify.html helios-server-translation/heliosbooth/single-ballot-verify.html
--- helios-server-original/heliosbooth/single-ballot-verify.html	2019-10-28 03:15:16.935087172 -0300
+++ helios-server-translation/heliosbooth/single-ballot-verify.html	2019-10-29 03:15:08.030263282 -0300
@@ -1,157 +1,30 @@
-<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
-<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
-<head>
-  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
-  <title>Helios Voting System</title>
-  <link rel="stylesheet" type="text/css" href="css/booth.css" />
-  <link rel="stylesheet" type="text/css" href="css/forms.css" />
-  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
-  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
-  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
-  <script language="javascript" src="js/underscore-min.js"></script>
-  <script language="javascript" src="js/jquery-1.2.6.min.js"></script>
-  <script language="javascript" src="js/jquery-jtemplates.js"></script>
-  <script language="javascript" src="js/jquery.json.min.js"></script>
-  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+<!doctype html>
 
-  <script language="javascript">
-    // required for jscrypto library
-    var JSCRYPTO_HOME = document.location.pathname.replace("single-ballot-verify.html", "js/jscrypto");
-  </script>
-
-  <script language="javascript" src="js/jscrypto/class.js"></script>  
-  <script language="javascript" src="js/jscrypto/bigint.js"></script>
-  <script language="javascript" src="js/jscrypto/random.js"></script>
-  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
-  <script language="javascript" src="js/jscrypto/sha1.js"></script>
-  <script language="javascript" src="js/jscrypto/sha2.js"></script>
-  <script language="javascript" src="js/jscrypto/helios.js"></script>
-  <script language="javascript" src="verifier.js"></script>
-</head>
-<body>
 <script language="javascript">
-$(document).ready(function() {
-    BigInt.setup(function() {
-       $('#verifier_loading').hide();
-
-       if (BigInt.is_dummy) {
-          $('#dummy_bigint').show();
-          return;
-       }
-
-       $('#verifier').show();
-       var election_url = $.query.get('election_url');
-       $('#election_url').val(election_url); 
-    }, function() {
-       $('#dummy_bigint').show();
-    });
-});    
 
-function result_append(str) {
-    $('#results').append(str).append("<br />");
-}
-
-function verify_single_ballot(election_url, audit_trail) {
-  var encrypted_vote_json = jQuery.secureEvalJSON(audit_trail);
+  // Fetch URL parameters
+  var parameters = window.location.search;
   
-  result_append("loading election...");
-
-  // quick and dirty detection of cast ballot
-  if (encrypted_vote_json['cast_at']) {
-    result_append("\n\nIt looks like you are trying to verify a cast ballot. That can't be done, only audited ballots can be verified.");
-    return;
+  // Start static HTML language decision block
+  var lang = navigator.language || navigator.userLanguage;
+  var base = "sbv.html";
+  var file = "./" + lang + "-" + base;
+
+  var xhr = new XMLHttpRequest();
+  xhr.open('HEAD', file, false);
+
+  try{
+  xhr.send();
+  if (xhr.status == "200"){
+  window.location = file + parameters;
+  }
+  else{
+  window.location = "./default-" + base + parameters;
+  }
   }
   
-  $('#loading').show();
-  
-  var after_computation = function(overall_result) {
-    result_append("<br />");
-    
-    $('#loading').hide();
-    
-    if (overall_result) {
-       result_append('SUCCESSFUL VERIFICATION, DONE!');
-    } else {
-       result_append('PROBLEM - THIS BALLOT DOES NOT VERIFY.');        
-    }
-  };
-
-  // the hash will be computed within the setup function call now
-  $.ajax({url: election_url, success: function(raw_json) {
-    if (window.Worker) {
-      var verifier_worker = new window.Worker("verifierworker.js");
-      verifier_worker.onmessage = function(event) {
-        if (event.data.type == 'log')
-          return console.log(event.data.msg);
-       
-        if (event.data.type == 'status')
-          return result_append(event.data.msg);
-
-        if (event.data.type == 'result')
-          return after_computation(event.data.result);
-      };
-
-      verifier_worker.postMessage({'type':'verify', 'election':raw_json, 'vote':encrypted_vote_json});
-    } else {
-      var overall_result = verify_ballot(raw_json, encrypted_vote_json, result_append);
-      after_computation(overall_result);
-    }
-  }, error: function() {
-      result_append('PROBLEM LOADING election. Are you sure you have the right election URL?<br />');
-
-      $('#loading').hide();
-      
-      result_append('PROBLEM - THIS BALLOT DOES NOT VERIFY.');        
+  catch (e){
+  window.location = "./default-" + base + parameters;
   }
-  });
-}
-
+  
 </script>
-</head>
-<body>
-<div id="wrapper">
-<div id="banner">
-    Helios Single-Ballot Verifier
-</div>
-<div id="content">
-
-<div id="verifier_loading">
-Loading verifier...
-</div>
-
-<div id="dummy_bigint" style="display:none;">
-Your browser does not have the Java plugin installed.<br /><br />
-At this time, the Java plugin is required for browser-based ballot auditing, although it is not required for ballot preparation.
-</div>
-
-<div id="verifier" style="display:none;">
-<p style="font-size: 16pt;">
-    This single-ballot verifier lets you enter an audited ballot<br />and verify that it was prepared correctly.
-</p>
-
-<form onsubmit="try {verify_single_ballot(this.election_url.value, this.audit_trail.value);} catch (e) {E = e;} return false;">
-Enter the Election URL:
-    <input type="text" size="50" name="election_url" id="election_url" /><br />
-
-<p>
-Your Ballot:
-</p>
-
-<textarea name="audit_trail" cols="80" rows="7">
-</textarea><br /><br />
-<input type="submit" class="pretty" value="Verify" />
-</form>
-
-<br /><br />
-<div id="results">
-</div>
-<img id="loading" src="loading.gif" style="display:none;" />
-</div>
-</div>
-
-</div>
-
-<div id="applet_div">
-</div>
-</body>
-</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/audit.html helios-server-translation/heliosbooth/templates/pt-BR/audit.html
--- helios-server-original/heliosbooth/templates/pt-BR/audit.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/audit.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,35 @@
+
+<h3>Sua cédula auditada</h3>
+
+<p>
+<b><u>IMPORTANTE</u></b>: agora que sua cédula foi auditada ela <em>não será computada na totalização</em>.<br />
+Para enviar sua cédula, você deve clicar no botão "voltar para a votação" abaixo para obter uma nova cifração de sua cédula e, então, escolher "Enviar meu voto".
+</p>
+
+<p>
+<b>Por que?</b> O Helios impede que você envie uma cédula auditada no intuito de prover alguma proteção contra tentativas de coerção.
+</p>
+
+<p>
+<b>E agora?</b> <a onclick="$('#audit_trail').select(); return false;" href="#">Selecione a informação de auditoria de sua cédula</a>, copie e cole no <a target="_blank" href="single-ballot-verify.html?election_url={$T.election_url}">verificador de cédulas</a> para verificar a cifração.<br />
+Ao terminar, não se esqueça de clicar em "voltar para a votação" para obter uma nova cifração de sua cédula e então enviar seu voto para que seja contabilizado.
+</p>
+
+<form action="#">
+<textarea name="audit_trail" id="audit_trail" cols="80" rows="10" wrap="soft">
+{$T.audit_trail}
+</textarea>
+<br /><br />
+<b>Antes de voltar para a votação</b>, você pode postar esta cédula auditada no centro de rastreio do Helios para que os demais eleitores possam também checar a informação de auditoria desta cédula.
+<br /><br />
+<b>Mesmo que você poste esta cédula no centro de rastreio do Helios você deve retornar à votação e enviar seu voto para que ele possa de fato ser contabilizado.</b>
+<br /><br />
+<input type="button" value="voltar para a votação" onclick="BOOTH.reset_ciphertexts();BOOTH.seal_ballot();" class="pretty" />
+&nbsp; &nbsp;&nbsp;
+<input type="button" value="postar cédula auditada na central de rastreio" onclick="$(this).attr('disabled', 'disabled');BOOTH.post_audited_ballot();" id="post_audited_ballot_button" class="pretty" style="font-size:0.8em;"/>
+
+</form>
+
+
+
+
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/done.html helios-server-translation/heliosbooth/templates/pt-BR/done.html
--- helios-server-original/heliosbooth/templates/pt-BR/done.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/done.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,9 @@
+
+<h3>Seu voto está sendo enviado</h3>
+
+<p>
+Aguarde alguns segundos enquanto o seu voto é enviado para o servidor....
+</p>
+
+
+
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/election.html helios-server-translation/heliosbooth/templates/pt-BR/election.html
--- helios-server-original/heliosbooth/templates/pt-BR/election.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/election.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,29 @@
+
+<div id="questions_div">
+<p>
+    Para depositar seu voto você será guiado pelos passos a seguir.<br />
+    Caso ainda não tenha feito seu acesso, você será solicitado a fazê-lo ao final do processo.
+<p>
+    
+<ol>
+    <li> <b>Escolha</b> as opções de sua preferência.<br />
+<span style="font-size: 14pt;">Você pode avançar ou retornar pelas páginas livremente.</span></li>
+<br />
+
+    <li> <b>Revise &amp; Confirme</b> suas escolhas.<br />
+<span style="font-size: 14pt;">
+  Suas escolhas são cifradas em segurança no ambiente do seu navegador e você receberá um número para rastreio de sua cédula.<br />
+</span>
+</li>
+<br />
+
+    <li> <b>Envie</b> sua cédula cifrada.<br />
+<span style="font-size: 14pt;">
+Você será solicitado a fazer seu acesso no sistema para poder enviar sua cédula cifrada para a totalização.
+</span>
+</li>
+</ol>
+
+<div align="center"><button onclick="BOOTH.show_question(0);">Começar</button></div>
+</div>
+
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/footer.html helios-server-translation/heliosbooth/templates/pt-BR/footer.html
--- helios-server-original/heliosbooth/templates/pt-BR/footer.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/footer.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,9 @@
+<span style="float:right; padding-right:20px;">
+<a target="_new" href="mailto:{$T.election_metadata.help_email}?subject=help%20with%20election%20{$T.election.name}&body=I%20need%20help%20with%20election%20{$T.election.uuid}">ajuda</a>
+</span>
+{#if $T.election.BOGUS_P}
+A chave pública para esta eleição ainda não está disponível. Este é apenas o modo de pré-visualização.
+{#else}
+Identificador da eleição: <span id="election_hash" style="font-weight:bold;">{$T.election.hash}</span>
+{#/if}
+
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/header.html helios-server-translation/heliosbooth/templates/pt-BR/header.html
--- helios-server-original/heliosbooth/templates/pt-BR/header.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/header.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,2 @@
+<h1 id="election_name">{$T.election.name}</h1>
+<p>&nbsp;</p>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/question.html helios-server-translation/heliosbooth/templates/pt-BR/question.html
--- helios-server-original/heliosbooth/templates/pt-BR/question.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/question.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,70 @@
+
+<form onsubmit="return false;" class="prettyform" id="answer_form">
+<input type="hidden" name="question_num" value="{$T.question_num}" />
+
+<p>
+<br /> 
+<b>{$T.question.question}</b>
+<br />
+<span style="font-size: 0.6em;">#{$T.question_num + 1} de {$T.last_question_num + 1} &mdash;
+ escolha 
+{#if $T.question.min == 1 && $T.question.max == 1}
+necessariamente 1
+{#else}
+{#if $T.question.min && $T.question.min > 0}
+{#if $T.question.max}
+{$T.question.min} a {$T.question.max}
+{#else}
+pelo menos {$T.question.min}
+{#/if}
+{#else}
+{#if $T.question.max}
+{#if $T.question.max > 1}até {#/if}{$T.question.max}
+{#else}
+quantas desejar
+{#/if} 
+{#/if}
+{#/if}
+</span>
+</p>
+
+{#foreach $T.question.answers as answer}
+<div id="answer_label_{$T.question_num}_{$T.answer_ordering[$T.answer$index]}">
+  <input type="checkbox" class="ballot_answer" id="answer_{$T.question_num}_{$T.answer_ordering[$T.answer$index]}" name="answer_{$T.question_num}_{$T.answer_ordering[$T.answer$index]}" value="yes" onclick="BOOTH.click_checkbox({$T.question_num}, {$T.answer_ordering[$T.answer$index]}, this.checked);" />
+
+  <label class="answer" for="answer_{$T.question_num}_{$T.answer_ordering[$T.answer$index]}">
+    {$T.question.answers[$T.answer_ordering[$T.answer$index]]}
+
+    {#if $T.question.answer_urls && $T.question.answer_urls[$T.answer_ordering[$T.answer$index]] && $T.question.answer_urls[$T.answer_ordering[$T.answer$index]] != ""}
+      &nbsp;&nbsp;
+      <span style="font-size: 12pt;">
+        [<a target="_blank" href="{$T.question.answer_urls[$T.answer_ordering[$T.answer$index]]}" rel="noopener noreferrer">more info</a>]
+      </span>
+    {#/if}
+  </label>
+</div>
+{#/for}
+
+<div id="warning_box" style="color: green; text-align:center; font-size: 0.8em; padding-top:10px; padding-bottom: 10px; height:50px;">
+</div>
+
+
+{#if $T.show_reviewall}
+<div style="float: right;">
+<input type="button" onclick="BOOTH.validate_and_confirm({$T.question_num});" value="Prosseguir" />
+</div>
+{#/if}
+
+{#if $T.question_num != 0}
+<input type="button" onclick="BOOTH.previous({$T.question_num})" value="Anterior" />
+&nbsp;
+{#/if}
+
+{#if $T.question_num < $T.last_question_num}
+<input type="button" onclick="BOOTH.next({$T.question_num})" value="Próxima" />
+&nbsp;
+{#/if}
+
+<br clear="both" />
+
+</form>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/seal.html helios-server-translation/heliosbooth/templates/pt-BR/seal.html
--- helios-server-original/heliosbooth/templates/pt-BR/seal.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/seal.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,56 @@
+
+{#if $T.election_metadata.use_advanced_audit_features}
+<div style="float: right; background: lightyellow; margin-left: 20px; padding: 0px 10px 10px 10px; border: 1px solid #ddd; width:200px;">
+<h4><a onclick="$('#auditbody').slideToggle(250);" href="#">Auditar</a> <span style="font-size: 0.8em; color: #444">[opcional]</span></h4>
+<div id="auditbody" style="display:none;">
+<p>
+Caso deseje, você pode auditar sua cédula para verificar como suas escolhas foram cifradas.
+</p>
+<p>
+Isto feito, sua cédula será cifrada novamente para que seu voto possa ser depositado.
+</p>
+<input type="button" value="Verificar Cifração" onclick="BOOTH.audit_ballot();" class="pretty" />
+</p>
+</div>
+</div>
+{#/if}
+
+<h3>Revise sua Cédula</h3>
+
+
+<div style="padding: 10px; margin-bottom: 10px; background-color: #eee; border: 1px #ddd solid; max-width: 340px;">
+{#foreach $T.questions as question}
+
+<b>Pleito #{$T.question$index + 1}: {$T.question.short_name}</b><br>
+{#if $T.choices[$T.question$index].length == 0}
+<div style="margin-left: 15px;">&#x2610; <i>Nenhuma opção selecionada</i></div>
+{#/if}
+{#foreach $T.choices[$T.question$index] as choice}
+<div style="margin-left: 15px;">&#x2713; {$T.choice}</div>
+{#/for}
+{#if $T.choices[$T.question$index].length < $T.question.max}
+[você pode escolher até {$T.question.max}]
+{#/if}
+[<a onclick="BOOTH.show_question({$T.question$index}); return false;" href="#">editar minha escolha</a>]
+{#if !$T.question$last}<br><br>{#/if}
+{#/for}
+</div>
+
+
+<p><p>O número de rastreio da sua cédula é <b><tt style="font-size: 11pt;">{$T.encrypted_vote_hash}</tt></b>. Caso queira, você pode <a onclick="BOOTH.show_receipt(); return false;" href="#">imprimir o número de rastreio</a>.<br /><br /></p>
+
+<p>
+Após clicar em "Enviar meu voto", a versão decifrada de sua cédula será destruída e a versão cifrada será enviada ao servidor Helios.</p>
+
+<button id="proceed_button" onclick="BOOTH.cast_ballot();">Enviar meu voto</button><br />
+<div id="loading_div"><img src="loading.gif" id="proceed_loading_img" /></div>
+
+
+
+<form method="POST" action="{$T.cast_url}" id="send_ballot_form" class="prettyform">
+<input type="hidden" name="election_uuid" value="{$T.election_uuid}" />
+<input type="hidden" name="election_hash" value="{$T.election_hash}" />
+<textarea name="encrypted_vote" style="display: none;">
+{$T.encrypted_vote_json}
+</textarea>
+</form>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/templates/pt-BR/submit.html helios-server-translation/heliosbooth/templates/pt-BR/submit.html
--- helios-server-original/heliosbooth/templates/pt-BR/submit.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosbooth/templates/pt-BR/submit.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,35 @@
+
+<h3>Submit Your Encrypted Ballot</h3>
+
+<p>
+All information, other than your encrypted ballot,<br />
+has been removed from memory.
+</p>
+
+<p>
+As a reminder, your ballot tracking number is:<br />
+<b><tt style="font-size: 16pt;">{$T.encrypted_vote_hash}</tt></b>
+</p>
+
+<p>
+According to the election definition file, this ballot will be submitted to:
+</p>
+
+<p><b><tt style="font-size:1.3em;">{$T.cast_url}</tt></b></p>
+
+<p>
+    where you will log in to validate your eligibility to vote.
+</p>
+
+<form method="POST" onsubmit="BOOTH.do_done();" action="{$T.cast_url}" id="send_ballot_form" class="prettyform">
+<input type="hidden" name="election_uuid" value="{$T.election_uuid}" />
+<input type="hidden" name="election_hash" value="{$T.election_hash}" />
+<textarea name="encrypted_vote" style="display: none;">
+{$T.encrypted_vote_json}
+</textarea>
+<div id="submit_button">
+  <label for="submit">&nbsp;</label>
+  <input type="submit" value="Submit Ballot" class="pretty" />
+</div>
+</form>
+
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosbooth/vote.html helios-server-translation/heliosbooth/vote.html
--- helios-server-original/heliosbooth/vote.html	2019-10-28 03:15:16.935087172 -0300
+++ helios-server-translation/heliosbooth/vote.html	2019-10-29 03:15:08.030263282 -0300
@@ -1,644 +1,30 @@
-<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
-<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
-<head>
-  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" />
-  <title>Helios Voting Booth</title>
-  <link rel="stylesheet" type="text/css" href="css/booth.css" />
-  <link rel="stylesheet" type="text/css" href="css/forms.css" />
-
-<!--
-  <script language="javascript" src="js/jscrypto/class.js"></script>
-  <script language="javascript" src="js/jscrypto/bigint.dummy.js"></script>
-  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
-  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
-  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
-  <script language="javascript" src="js/underscore-min.js"></script>
-  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
-  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
-  <script language="javascript" src="js/jquery-jtemplates.js"></script>
-  <script language="javascript" src="js/jquery.json.min.js"></script>
-  <script language="javascript" src="js/jscrypto/bigint.js"></script>
-  <script language="javascript" src="js/jscrypto/random.js"></script>
-  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
-  <script language="javascript" src="js/jscrypto/sha1.js"></script>
-  <script language="javascript" src="js/jscrypto/sha2.js"></script>
-  <script language="javascript" src="js/jscrypto/helios.js"></script>-->
-  
-  <script language="javascript" src="js/20160507-helios-booth-compressed.js"></script>
-</head>
-<body>
-<div id="wrapper">
-<span style="float:right; padding: 7px 15px 5px 10px;">[<a href="javascript:BOOTH.exit();">exit</a>]</span>
-<div id="banner">Helios Voting Booth</div>
-<div id="content">
-
-<div id="header">
-</div>
+<!doctype html>
 
 <script language="javascript">
-// utils
-BOOTH = {};
-
-BOOTH.setup_templates = function() {
-    if (BOOTH.templates_setup_p)
-        return;
-
-    var cache_bust = "?cb=" + new Date().getTime();
-
-    $('#header').setTemplateURL("templates/header.html" + cache_bust);
-    $('#election_div').setTemplateURL("templates/election.html" + cache_bust);
-    $('#question_div').setTemplateURL("templates/question.html" + cache_bust);
-    $('#seal_div').setTemplateURL("templates/seal.html" + cache_bust);
-    $('#audit_div').setTemplateURL("templates/audit.html" + cache_bust);
-    $('#footer').setTemplateURL("templates/footer.html" + cache_bust);
-
-    BOOTH.templates_setup_p = true;
-};
-
-// set up the message when navigating away
-BOOTH.started_p = false;
-
-window.onbeforeunload = function(evt) {
-  if (!BOOTH.started_p)
-    return;
-
-  if (typeof evt == 'undefined') {
-    evt = window.event;
-  }
-
-  var message = "If you leave this page with an in-progress ballot, your ballot will be lost.";
-
-  if (evt) {
-    evt.returnValue = message;
-  }
-
-  return message;
-};
-
-BOOTH.exit = function() {
-    if (confirm("Are you sure you want to exit the booth and lose all information about your current ballot?")) {
-        BOOTH.started_p = false;
-        window.location = BOOTH.election.cast_url;
-    }
-};
-
-BOOTH.setup_ballot = function(election) {
-  BOOTH.ballot = {};
-
-  // dirty markers for encryption (mostly for async)
-  BOOTH.dirty = [];
-
-  // each question starts out with an empty array answer
-  // and a dirty bit to make sure we encrypt
-  BOOTH.ballot.answers = [];
-  $(BOOTH.election.questions).each(function(i,x){
-    BOOTH.ballot.answers[i] = [];
-    BOOTH.dirty[i] = true;
-  });
-};
-
-// all ciphertexts to null
-BOOTH.reset_ciphertexts = function() {
-  _(BOOTH.encrypted_answers).each(function(enc_answer, ea_num) {
-    BOOTH.launch_async_encryption_answer(ea_num);
-  });
-};
-
-BOOTH.log = function(msg) {
-  if (typeof(console) != undefined)
-    console.log(msg);
-};
-
-BOOTH.setup_workers = function(election_raw_json) {
-  // async?
-  if (!BOOTH.synchronous) {
-      // prepare spots for encrypted answers
-      // and one worker per question
-      BOOTH.encrypted_answers = [];
-      BOOTH.answer_timestamps = [];
-      BOOTH.worker = new window.Worker("boothworker-single.js");
-      BOOTH.worker.postMessage({
-        'type': 'setup',
-        'election' : election_raw_json
-      });
-
-      BOOTH.worker.onmessage = function(event) {
-        // logging
-        if (event.data.type == 'log')
-          return BOOTH.log(event.data.msg);
-
-        // result of encryption
-        if (event.data.type == 'result') {
-          // this check ensures that race conditions
-          // don't screw up votes.
-          if (event.data.id == BOOTH.answer_timestamps[event.data.q_num]) {
-            BOOTH.encrypted_answers[event.data.q_num] = HELIOS.EncryptedAnswer.fromJSONObject(event.data.encrypted_answer, BOOTH.election);
-            BOOTH.log("got encrypted answer " + event.data.q_num);
-          } else {
-            BOOTH.log("no way jose");
-          }
-        }
-      };
-
-      $(BOOTH.election.questions).each(function(q_num, q) {
-        BOOTH.encrypted_answers[q_num] = null;
-      });
-  }
-};
-
-function escape_html(content) {
-  return $('<div/>').text(content).html();
-}
-
-BOOTH.setup_election = function(raw_json, election_metadata) {
-  // IMPORTANT: we use the raw JSON for safer hash computation
-  // so that we are using the JSON serialization of the SERVER
-  // to compute the hash, not the JSON serialization in JavaScript.
-  // the main reason for this is unicode representation: the Python approach
-  // appears to be safer.
-  BOOTH.election = HELIOS.Election.fromJSONString(raw_json);
-
-  // FIXME: we shouldn't need to set both, but right now we are doing so
-  // because different code uses each one. Bah. Need fixing.
-  BOOTH.election.hash = b64_sha256(raw_json);
-  BOOTH.election.election_hash = BOOTH.election.hash
-
-  // async?
-  BOOTH.setup_workers(raw_json);
-
-  document.title += ' - ' + BOOTH.election.name;
-
-  // escape election fields
-  $(['description', 'name']).each(function(i, field) {
-    BOOTH.election[field] = escape_html(BOOTH.election[field]);
-  });
 
-  // TODO: escape question and answers
-
-  // whether the election wants candidate order randomization or not
-  // we set up an ordering array so that the rest of the code is
-  // less error-prone. 
-  BOOTH.election.question_answer_orderings = [];
-  $(BOOTH.election.questions).each(function(i, question) {
-    var ordering = new Array(question.answers.length);
-
-    // initialize array so it is the identity permutation
-    $(ordering).each(function(j, answer) {ordering[j]=j;});
-
-    // if we want reordering, then we shuffle the array
-    if (election_metadata && election_metadata.randomize_answer_order) {
-      shuffleArray(ordering);
-    }
-
-    BOOTH.election.question_answer_orderings[i] = ordering;
-  });
-
-  $('#header').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
-  $('#footer').processTemplate({'election' : BOOTH.election, 'election_metadata': BOOTH.election_metadata});
-  BOOTH.setup_ballot();
-};
-
-BOOTH.show = function(el) {
-  $('.panel').hide();
-  el.show();
-  return el;
-};
-
-BOOTH.show_election = function() {
-  BOOTH.show($('#election_div')).processTemplate({'election' : BOOTH.election});
-};
-
-BOOTH.launch_async_encryption_answer = function(question_num) {
-   BOOTH.answer_timestamps[question_num] = new Date().getTime();
-   BOOTH.encrypted_answers[question_num] = null;
-   BOOTH.dirty[question_num] = false;
-   BOOTH.worker.postMessage({
-      'type' : 'encrypt',
-      'q_num': question_num,
-      'answer' : BOOTH.ballot.answers[question_num],
-      'id' : BOOTH.answer_timestamps[question_num]
-   });
-};
-
-// check if the current question is ok
-BOOTH.validate_question = function(question_num) {
-    // check if enough answers are checked
-    if (BOOTH.ballot.answers[question_num].length < BOOTH.election.questions[question_num].min) {
-        alert('You need to select at least ' + BOOTH.election.questions[question_num].min + ' answer(s).');
-        return false;
-    }
-
-    // if we need to launch the worker, let's do it
-    if (!BOOTH.synchronous) {
-       // we need a unique ID for this to ensure that old results
-       // don't mess things up. Using timestamp.
-       // check if dirty
-       if (BOOTH.dirty[question_num]) {
-         BOOTH.launch_async_encryption_answer(question_num);
-       }
-    }
-    return true;
-};
-
-BOOTH.validate_and_confirm = function(question_num) {
-  if (BOOTH.validate_question(question_num)) {
-      BOOTH.seal_ballot();
+  // Fetch URL parameters
+  var parameters = window.location.search;
+  
+  // Start static HTML language decision block
+  var lang = navigator.language || navigator.userLanguage;
+  var base = "vote.html";
+  var file = "./" + lang + "-" + base;
+
+  var xhr = new XMLHttpRequest();
+  xhr.open('HEAD', file, false);
+
+  try{
+  xhr.send();
+  if (xhr.status == "200"){
+  window.location = file + parameters;
   }
-};
-
-BOOTH.next = function(question_num) {
-    if (BOOTH.validate_question(question_num)) {
-        BOOTH.show_question(question_num + 1);
-    }
-};
-
-BOOTH.previous = function(question_num) {
-    if (BOOTH.validate_question(question_num)) {
-        BOOTH.show_question(question_num - 1);
-    }
-};
-
-// http://stackoverflow.com/questions/2450954/how-to-randomize-shuffle-a-javascript-array
-function shuffleArray(array) {
-  var currentIndex = array.length
-    , temporaryValue
-    , randomIndex
-    ;
-
-  // While there remain elements to shuffle...
-  while (0 !== currentIndex) {
-     // Pick a remaining element...
-    randomIndex = Math.floor(Math.random() * currentIndex);
-    currentIndex -= 1;
-
-    // And swap it with the current element.
-    temporaryValue = array[currentIndex];
-    array[currentIndex] = array[randomIndex];
-    array[randomIndex] = temporaryValue;
+  else{
+  window.location = "./default-" + base + parameters;
   }
-
-  return array;
-}
-
-BOOTH.show_question = function(question_num) {
-  BOOTH.started_p = true;
-
-  // the first time we hit the last question, we enable the review all button
-  if (question_num == BOOTH.election.questions.length -1)
-    BOOTH.all_questions_seen = true;
-
-  BOOTH.show_progress('1');
-  BOOTH.show($('#question_div')).processTemplate({'question_num' : question_num,
-                      'last_question_num' : BOOTH.election.questions.length - 1,
-                      'question' : BOOTH.election.questions[question_num], 'show_reviewall' : BOOTH.all_questions_seen,
-		      'answer_ordering': BOOTH.election.question_answer_orderings[question_num]
-                });
-
-  // fake clicking through the answers, to trigger the disabling if need be
-  // first we remove the answers array
-  var answer_array = BOOTH.ballot.answers[question_num];
-  BOOTH.ballot.answers[question_num] = [];
-
-  // we should not set the dirty bit here, so we save it away
-  var old_dirty = BOOTH.dirty[question_num];
-  $(answer_array).each(function(i, ans) {
-    BOOTH.click_checkbox_script(question_num, ans, true);
-  });
-  BOOTH.dirty[question_num] = old_dirty;
-};
-
-
-
-BOOTH.click_checkbox_script = function(question_num, answer_num) {
-  document.forms['answer_form']['answer_'+question_num+'_'+answer_num].click();
-};
-
-BOOTH.click_checkbox = function(question_num, answer_num, checked_p) {
-  // keep track of dirty answers that need encrypting
-  BOOTH.dirty[question_num] = true;
-
-  if (checked_p) {
-     // multiple click events shouldn't screw this up
-     if ($(BOOTH.ballot.answers[question_num]).index(answer_num) == -1)
-        BOOTH.ballot.answers[question_num].push(answer_num);
-
-     $('#answer_label_' + question_num + "_" + answer_num).addClass('selected');
-  } else {
-     BOOTH.ballot.answers[question_num] = UTILS.array_remove_value(BOOTH.ballot.answers[question_num], answer_num);
-     $('#answer_label_' + question_num + "_" + answer_num).removeClass('selected');
   }
-
-  if (BOOTH.election.questions[question_num].max != null && BOOTH.ballot.answers[question_num].length >= BOOTH.election.questions[question_num].max) {
-     // disable the other checkboxes
-     $('.ballot_answer').each(function(i, checkbox) {
-        if (!checkbox.checked)
-            checkbox.disabled = true;
-     });
-
-     // do the warning only if the question allows more than one option, otherwise it's confusing
-     if (BOOTH.election.questions[question_num].max > 1) {
-        $('#warning_box').html("Maximum number of options selected.<br />To change your selection, please de-select a current selection first.");
-     }
-  } else {
-     // enable the other checkboxes
-     $('.ballot_answer').each(function(i, checkbox) {
-       checkbox.disabled = false;
-     });
-     $('#warning_box').html("");
+  
+  catch (e){
+  window.location = "./default-" + base + parameters;
   }
-};
-
-BOOTH.show_processing_before = function(str_to_execute) {
-    $('#processing_div').html("<h3 align='center'>Processing...</h3>");
-    BOOTH.show($('#processing_div'));
-
-    // add a timeout so browsers like Safari actually display the processing message
-    setTimeout(str_to_execute, 250);
-};
-
-BOOTH.show_encryption_message_before = function(func_to_execute) {
-    BOOTH.show_progress('2');
-    BOOTH.show($('#encrypting_div'));
-
-    func_to_execute();
-};
-
-BOOTH.load_and_setup_election = function(election_url) {
-    // the hash will be computed within the setup function call now
-    $.get(election_url, function(raw_json) {
-        // let's also get the metadata
-        $.getJSON(election_url + "/meta", {}, function(election_metadata) {
-          BOOTH.election_metadata = election_metadata;
-          BOOTH.setup_election(raw_json, election_metadata);
-          BOOTH.show_election();
-          BOOTH.election_url = election_url;
-        });
-    });
-
-
-    if (USE_SJCL) {
-      // get more randomness from server
-      $.get(election_url + "/get-randomness", {}, function(raw_json) {
-        sjcl.random.addEntropy(JSON.parse(raw_json).randomness);
-      });
-    }
-};
-
-BOOTH.hide_progress = function() {
-  $('#progress_div').hide();
-};
-
-BOOTH.show_progress = function(step_num) {
-    $('#progress_div').show();
-    $(['1','2','3','4']).each(function(n, step) {
-        if (step == step_num)
-            $('#progress_' + step).attr('class', 'selected');
-        else
-            $('#progress_' + step).attr('class', 'unselected');
-    });
-};
-
-BOOTH.so_lets_go = function () {
-    BOOTH.hide_progress();
-
-    BOOTH.setup_templates();
-
-    // election URL
-    var election_url = $.query.get('election_url');
-    BOOTH.load_and_setup_election(election_url);
-};
-
-BOOTH.nojava = function() {
-  // in the case of Chrome, we get here when Java
-  // is disabled, instead of detecting the problem earlier.
-  // because navigator.javaEnabled() still returns true.
-  // $('#election_div').hide();
-  // $('#error_div').show();
-    USE_SJCL = true;
-    sjcl.random.startCollectors();
-    BigInt.setup(BOOTH.so_lets_go);
-};
-
-BOOTH.ready_p = false;
-
-$(document).ready(function() {
-    if (USE_SJCL) {
-      sjcl.random.startCollectors();
-    }
-
-    // we're asynchronous if we have SJCL and Worker
-    BOOTH.synchronous = !(USE_SJCL && window.Worker);
-
-    // we do in the browser only if it's asynchronous
-    BigInt.in_browser = !BOOTH.synchronous;
-
-    // set up dummy bigint for fast parsing and serialization
-    if (!BigInt.in_browser)
-      BigInt = BigIntDummy;
-
-    BigInt.setup(BOOTH.so_lets_go, BOOTH.nojava);
-});
-
-
-BOOTH.check_encryption_status = function() {
-  var progress = BOOTH.progress.progress();
-  if (progress == "" || progress == null)
-    progress = "0";
-
-  $('#percent_done').html(progress);
-};
-
-BOOTH._after_ballot_encryption = function() {
-    // if already serialized, use that, otherwise serialize
-    BOOTH.encrypted_vote_json = BOOTH.encrypted_ballot_serialized || JSON.stringify(BOOTH.encrypted_ballot.toJSONObject());
-
-    var do_hash = function() {
-       BOOTH.encrypted_ballot_hash = b64_sha256(BOOTH.encrypted_vote_json); // BOOTH.encrypted_ballot.get_hash();
-       window.setTimeout(show_cast, 0);
-    };
-
-    var show_cast = function() {
-      $('#seal_div').processTemplate({'cast_url': BOOTH.election.cast_url,
-        'encrypted_vote_json': BOOTH.encrypted_vote_json,
-        'encrypted_vote_hash' : BOOTH.encrypted_ballot_hash,
-        'election_uuid' : BOOTH.election.uuid,
-        'election_hash' : BOOTH.election_hash,
-        'election': BOOTH.election,
-        'election_metadata': BOOTH.election_metadata,
-        'questions' : BOOTH.election.questions,
-        'choices' : BALLOT.pretty_choices(BOOTH.election, BOOTH.ballot)});
-      BOOTH.show($('#seal_div'));
-      BOOTH.encrypted_vote_json = null;
-    };
-
-    window.setTimeout(do_hash, 0);
-};
-
-BOOTH.total_cycles_waited = 0;
-
-// wait for all workers to be done
-BOOTH.wait_for_ciphertexts = function() {
-    BOOTH.total_cycles_waited += 1;
-
-    var answers_done = _.reject(BOOTH.encrypted_answers, _.isNull);
-    var percentage_done = Math.round((100 * answers_done.length) / BOOTH.encrypted_answers.length);
-
-    if (BOOTH.total_cycles_waited > 250) {
-      alert('there appears to be a problem with the encryption process.\nPlease email help@heliosvoting.org and indicate that your encryption process froze at ' + percentage_done + '%');
-      return;
-    }
-
-    if (percentage_done < 100) {
-      setTimeout(BOOTH.wait_for_ciphertexts, 500);
-      $('#percent_done').html(percentage_done + '');
-      return;
-    }
-
-    BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromEncryptedAnswers(BOOTH.election, BOOTH.encrypted_answers);
-
-    BOOTH._after_ballot_encryption();
-};
-
-BOOTH.seal_ballot_raw = function() {
-    if (BOOTH.synchronous) {
-      BOOTH.progress = new UTILS.PROGRESS();
-      var progress_interval = setInterval("BOOTH.check_encryption_status()", 500);
-      BOOTH.encrypted_ballot = new HELIOS.EncryptedVote(BOOTH.election, BOOTH.ballot.answers, BOOTH.progress);
-      clearInterval(progress_interval);
-      BOOTH._after_ballot_encryption();
-    } else {
-      BOOTH.total_cycles_waited = 0;
-      BOOTH.wait_for_ciphertexts();
-    }
-};
-
-BOOTH.request_ballot_encryption = function() {
-    $.post(BOOTH.election_url + "/encrypt-ballot", {'answers_json': $.toJSON(BOOTH.ballot.answers)}, function(result) {
-      //BOOTH.encrypted_ballot = HELIOS.EncryptedVote.fromJSONObject($.secureEvalJSON(result), BOOTH.election);
-      // rather than deserialize and reserialize, which is inherently slow on browsers
-      // that already need to do network requests, just remove the plaintexts
-
-      BOOTH.encrypted_ballot_with_plaintexts_serialized = result;
-      var ballot_json_obj = $.secureEvalJSON(BOOTH.encrypted_ballot_with_plaintexts_serialized);
-      var answers = ballot_json_obj.answers;
-      for (var i=0; i<answers.length; i++) {
-         delete answers[i]['answer'];
-         delete answers[i]['randomness'];
-      }
-
-      BOOTH.encrypted_ballot_serialized = JSON.stringify(ballot_json_obj);
-
-      window.setTimeout(BOOTH._after_ballot_encryption, 0);
-    });
-};
-
-BOOTH.seal_ballot = function() {
-    BOOTH.show_progress('2');
-
-    // if we don't have the ability to do crypto in the browser,
-    // we use the server
-    if (!BigInt.in_browser) {
-      BOOTH.show_encryption_message_before(BOOTH.request_ballot_encryption, true);
-    } else {
-      BOOTH.show_encryption_message_before(BOOTH.seal_ballot_raw, true);
-      $('#percent_done_container').show();
-    }
-};
-
-BOOTH.audit_ballot = function() {
-    BOOTH.audit_trail = BOOTH.encrypted_ballot_with_plaintexts_serialized || $.toJSON(BOOTH.encrypted_ballot.get_audit_trail());
-
-    BOOTH.show($('#audit_div')).processTemplate({'audit_trail' : BOOTH.audit_trail, 'election_url' : BOOTH.election_url});
-};
-
-BOOTH.post_audited_ballot = function() {
-  $.post(BOOTH.election_url + "/post-audited-ballot", {'audited_ballot': BOOTH.audit_trail}, function(result) {
-    alert('This audited ballot has been posted.\nRemember, this vote will only be used for auditing and will not be tallied.\nClick "back to voting" and cast a new ballot to make sure your vote counts.');
-  });
-};
-
-BOOTH.cast_ballot = function() {
-    // show progress spinner
-    $('#loading_div').show();
-    $('#proceed_button').attr('disabled', 'disabled');
-
-    // at this point, we delete the plaintexts by resetting the ballot
-    BOOTH.setup_ballot(BOOTH.election);
-
-    // clear the plaintext from the encrypted
-    if (BOOTH.encrypted_ballot)
-       BOOTH.encrypted_ballot.clearPlaintexts();
-
-    BOOTH.encrypted_ballot_serialized = null;
-    BOOTH.encrypted_ballot_with_plaintexts_serialized = null;
-
-    // remove audit trail
-    BOOTH.audit_trail = null;
-
-    // we're ready to leave the site
-    BOOTH.started_p = false;
-
-    // submit the form
-    $('#send_ballot_form').submit();
-};
-
-BOOTH.show_receipt = function() {
-    UTILS.open_window_with_content("Your smart ballot tracker for " + BOOTH.election.name + ": " + BOOTH.encrypted_ballot_hash);
-};
-
-BOOTH.do_done = function() {
-  BOOTH.started_p = false;
-};
-
+  
 </script>
-<div id="page">
-  <div id="progress_div" style="display:none; width: 500px; margin:auto;">
-      <table width="100%">
-          <tr><td id="progress_1">(1) Select</td><td id="progress_2">(2) Review</td><td id="progress_3">(3) Submit</td></tr>
-      </table>
-  </div>
-  <div id="election_div" class="panel">
-    <h3>Checking capabilities and loading election booth...</h3>
-    <div align="center"><img src="loading.gif" /><br />This may take up to 10 seconds</div>
-  </div>
-
-  <div id="error_div" class="panel" style="display: none;">
-    <h3>There's a problem</h3>
-    <p>
-      It appears that your browser does not have Java enabled. Helios needs Java to perform encryption within the browser.
-    </p>
-    <p>
-      You may be able to install Java by visiting <a target="_new" href="http://java.com">java.com</a>.
-    </p>
-  </div>
-
-  <div id="question_div" class="panel">
-  </div>
-
-  <div id="processing_div" class="panel" style="display:none;">
-      <h3 align="center">Processing....</h3>
-  </div>
-
-  <div id="encrypting_div" class="panel" style="display:none;">
-      <h3 align="center">Helios is now encrypting your ballot<br />
-          <img src="encrypting.gif" /> <span style="font-size:0.7em; display:none;" id="percent_done_container">(<span id="percent_done">0</span>%)</span></h3>
-
-      <p align="center"><b>This may take up to two minutes.</b>
-  </div>
-
-  <div id="seal_div" class="panel">
-  </div>
-
-  <div id="audit_div" class="panel">
-  </div>
-
-</div>
-
-<br clear="both" />
-</div>
-<div id="footer">&nbsp;</div>
-</div>
-<div id="applet_div">
-</div>
-</body>
-</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosverifier/default-verify.html helios-server-translation/heliosverifier/default-verify.html
--- helios-server-original/heliosverifier/default-verify.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosverifier/default-verify.html	2019-10-29 03:15:08.030263282 -0300
@@ -0,0 +1,289 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
+  <title>Helios Voting System -- Verifier</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script> 
+  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+
+  <script language="javascript">
+    // required for jscrypto library
+    var JSCRYPTO_HOME = document.location.pathname.replace("verify.html", "js/jscrypto");
+  </script>
+
+  <script language="javascript" src="js/jscrypto/class.js"></script>  
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>
+
+<script language="javascript">
+$(document).ready(function() {
+    BigInt.setup(function() {
+       $('#verifier_loading').hide();
+       $('#verifier').show();
+    }, function() {
+       alert('sorry, in-browser verification requires Java Support at this time.');
+    });
+});    
+
+function result_append(str) {
+    $('#results').append(str).append("<br />");
+}
+
+function pretty_result(result) {return result? "VERIFIED" : "FAIL";}
+
+function load_ballots(election_url, ballot_list, ballots, final_callback) {
+    // the ballots array is the place where we build up the list of ballots
+
+    // end of the iteration?
+    if (ballot_list.length == ballots.length) {
+        final_callback(ballots);
+        return;
+    }
+    
+    result_append("loading ballot for voter #" + (ballots.length + 1));
+        
+    // get the next ballot
+    $.get(election_url + "/ballots/" + ballot_list[ballots.length].uuid + '/last', function(result) {
+        var new_ballot = jQuery.secureEvalJSON(result);
+        ballots.push(new_ballot);
+        if (new_ballot.vote == null)
+          result_append("no ballot for this voter #" + ballots.length);
+        else
+          result_append("FOUND a ballot for voter #" + ballots.length);
+        load_ballots(election_url, ballot_list, ballots, final_callback);
+    });
+}
+
+// load the ballot list in increments of 50, for long ballots
+function load_ballot_list(election_url, ballot_list, after, final_callback) {
+    var url = election_url + "/voters/?limit=50";
+    if (after)
+        url+= "&after=" + after;
+        
+    $.get(url, function(result) {
+        var new_ballot_list = jQuery.secureEvalJSON(result);
+
+        // done, no more ballots?
+        if (new_ballot_list.length == 0)
+            return final_callback(ballot_list);
+            
+        // not done, add to the list
+        ballot_list = ballot_list.concat(new_ballot_list);
+        after = ballot_list[ballot_list.length - 1].uuid;
+        
+        // and iterate
+        load_ballot_list(election_url, ballot_list, after, final_callback);
+    });
+}
+
+function load_election_and_ballots(election_url) {
+        
+    result_append("<h3>Election</h3>");
+    result_append("loading election...");
+
+    var overall_result = true;
+    
+    // the hash will be computed within the setup function call now
+    $.get(election_url, function(raw_json) {
+        try {
+            election = HELIOS.Election.fromJSONString(raw_json);
+            result_append("loaded election: " + election.name);
+            result_append("election fingerprint: " + election.get_hash());
+
+            var tally = [];
+
+            $(election.questions).each(function(qnum, q) {
+                if (q.tally_type != "homomorphic") {
+                  result_append("PROBLEM: this election is not a straight-forward homomorphic-tally election. As a result, Helios cannot currently verify it.");
+                  return;
+                }
+
+                tally[qnum] = $(q.answers).map(function(anum, a) {
+                    return 1;
+                });
+            });
+
+            result_append("loading list of voters...");
+            
+            // load voter list
+            load_ballot_list(election_url, [], null, function(ballot_list) {
+                result_append("loaded voter list, now loading ballots for each..");
+
+                // load all ballots
+                load_ballots(election_url, ballot_list, [], function(ballots) {
+                    result_append("");
+                    result_append("<h3>Ballots</h3>");
+                    // now load each ballot
+                    $(ballots).each(function(i, cast_vote){
+
+                        if (cast_vote.vote == null)
+                          return;
+
+                        var vote = HELIOS.EncryptedVote.fromJSONObject(cast_vote.vote, election);
+                        result_append("Voter #" + (i+1));
+                        result_append("-- UUID: " + cast_vote.voter_uuid);
+                        result_append("-- Ballot Tracking Number: " + vote.get_hash());
+
+                        vote.verifyProofs(election.public_key, function(answer_num, choice_num, result, choice) {
+                            overall_result = overall_result && result;
+                            if (choice_num != null) {
+                                // keep track of tally
+                                tally[answer_num][choice_num] = choice.multiply(tally[answer_num][choice_num]);
+
+                                result_append("Question #" + (answer_num+1) + ", Option #" + (choice_num+1) + " -- " + pretty_result(result));
+                            } else {
+                                result_append("Question #" + (answer_num+1) + " OVERALL -- " + pretty_result(result));
+                            }
+                        });
+
+                        result_append("");
+                    });
+
+                    // get the election result
+                    $.get(election_url + "/result", function(result) {
+                        var results = $.secureEvalJSON(result);
+
+                        // get the trustees and proofs
+                        $.get(election_url + "/trustees/", function(trustees_json) {
+                           trustees = $.secureEvalJSON(trustees_json);
+
+                           // create the Helios objects
+                           trustees = $(trustees).map(function(i, trustee) {return HELIOS.Trustee.fromJSONObject(trustee)});
+
+                           // the public key that we'll check
+                           var combined_key = 1;
+
+                           result_append("<h3>Trustees</h3>");
+                           // verify the keys
+                           $(trustees).each(function(i, trustee) {
+                              result_append("Trustee #" + (i+1) + ": " + trustee.email);
+                              if (trustee.public_key.verifyKnowledgeOfSecretKey(trustee.pok, ElGamal.fiatshamir_dlog_challenge_generator)) {
+                                  result_append("-- PK " + trustee.public_key_hash + " -- VERIFIED.");
+
+                                  // FIXME check the public key hash
+                              } else {
+                                  result_append("==== ERROR for PK of trustee " + trustee.email);
+                                  overall_result = false;
+                              }
+
+                              combined_key = trustee.public_key.multiply(combined_key);
+
+                              result_append("");
+                           });
+
+                           // verify the combination of the keys into the final public key
+                           if (combined_key.equals(election.public_key)) {
+                               result_append("election public key CORRECTLY FORMED");
+                           } else {
+                               result_append("==== ERROR, election public key doesn't match");
+                               overall_result = false;
+                           }
+
+                           result_append("<h3>Tally</h3>");
+
+                           $(tally).each(function(q_num, q) {
+                               result_append("Question #" + (q_num+1) + ": " + election.questions[q_num].short_name);
+                               $(q).each(function(a_num, a) {
+                                   var plaintext = new ElGamal.Plaintext(election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p), election.public_key);
+
+                                   var check = true;
+                                   result_append("Answer #" + (a_num + 1) + ": " + election.questions[q_num].answers[a_num] + " - COUNT = " + results[q_num][a_num]);
+
+                                   var decryption_factors = [];
+
+                                   // go through the trustees' decryption factors and verify each one
+                                   $(trustees).each(function(t_num, trustee) {
+                                       if (trustee.public_key.verifyDecryptionFactor(a, trustee.decryption_factors[q_num][a_num],
+                                                       trustee.decryption_proofs[q_num][a_num], ElGamal.fiatshamir_challenge_generator)) {
+                                           result_append("-- Trustee " + trustee.email + ": decryption factor verifies");
+                                       } else {
+                                           result_append("==== ERROR with Trustee " + trustee.email + ": decryption factor does not verify");
+                                           check= false;
+                                           overall_result = false;
+                                       }
+
+                                       decryption_factors.push(trustee.decryption_factors[q_num][a_num]);
+                                   });
+
+                                   // recheck decryption factors
+                                   var expected_value = election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p);                        
+                                   var recomputed_value = a.decrypt(decryption_factors).getM();
+                                   if (expected_value.equals(recomputed_value)) {
+                                   } else {
+                                       check = false;
+                                       overall_result = false;
+                                   }
+
+                                   result_append("-" + pretty_result(check));
+                               });
+
+                           });
+
+                           result_append("<h3>FINAL RESULT</h3>");
+
+                           if (overall_result) {
+                             result_append("ELECTION FULLY VERIFIED -- SUCCESS!");
+                           } else {
+                             result_append("VERIFICATION FAILED");
+                           }
+                        });
+                    });
+                });
+
+            });
+    } catch (error) {
+        result_append("<p>It appears that you are trying to verify a private election.</p>");
+        result_append('<p>You can log in as a valid voter or log in as the election admin.</p>');
+        result_append('<a class="btn" href="' + election_url + '">Log in as a valid voter </a>');
+        result_append('<a class="btn" href="/auth/?return_url=/verifier/verify.html?election_url=' + election_url + '">Log in as the election admin</a>');
+    }
+    });
+        
+}
+
+$(document).ready(function() {
+   var election_url = $.query.get('election_url');
+   $('#election_url').val(election_url); 
+});
+</script>
+</head>
+<body>
+<div id="wrapper">
+<div id="banner">
+    Helios Election Verifier
+</div>
+<div id="content">
+
+<div id="verifier_loading">
+loading verifier ...
+</div>
+
+<div id="verifier" align="center" style="display:none;">
+Enter the Election URL:
+<form onsubmit="try{load_election_and_ballots(this.election_url.value);} catch (e) {} return false;">
+    <input type="text" size="50" name="election_url" id="election_url" /><br />
+    <input type="submit" value="start verification" />
+</form>
+</div>
+
+<br /><br />
+<div id="results">
+</div>
+</div>
+</div>
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosverifier/pt-BR-verify.html helios-server-translation/heliosverifier/pt-BR-verify.html
--- helios-server-original/heliosverifier/pt-BR-verify.html	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/heliosverifier/pt-BR-verify.html	2019-10-29 03:15:08.031263282 -0300
@@ -0,0 +1,299 @@
+<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
+<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
+<head>
+  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
+  <title>Sistema Helios -- Verificador</title>
+  <link rel="stylesheet" type="text/css" href="css/booth.css" />
+  <link rel="stylesheet" type="text/css" href="css/forms.css" />
+  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
+  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
+  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
+  <script language="javascript" src="js/underscore-min.js"></script> 
+  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
+  <script language="javascript" src="js/jquery-jtemplates.js"></script>
+  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
+  <script language="javascript" src="js/jquery.json.min.js"></script>
+
+  <script language="javascript">
+    // required for jscrypto library
+    var JSCRYPTO_HOME = document.location.pathname.replace("verify.html", "js/jscrypto");
+  </script>
+
+  <script language="javascript" src="js/jscrypto/class.js"></script>  
+  <script language="javascript" src="js/jscrypto/bigint.js"></script>
+  <script language="javascript" src="js/jscrypto/random.js"></script>
+  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
+  <script language="javascript" src="js/jscrypto/sha1.js"></script>
+  <script language="javascript" src="js/jscrypto/sha2.js"></script>
+  <script language="javascript" src="js/jscrypto/helios.js"></script>
+
+<script language="javascript">
+$(document).ready(function() {
+    BigInt.setup(function() {
+       $('#verifier_loading').hide();
+       $('#verifier').show();
+    }, function() {
+       alert('A verificação via navegador requer suporte Java.');
+    });
+});    
+
+function result_append(str) {
+    $('#results').append(str).append("<br />");
+}
+
+function pretty_result(result) {return result? "OK" : "ERRO";}
+
+function load_ballots(election_url, ballot_list, ballots, final_callback) {
+    // the ballots array is the place where we build up the list of ballots
+
+    // end of the iteration?
+    if (ballot_list.length == ballots.length) {
+        final_callback(ballots);
+        return;
+    }
+    
+    result_append("carregando cédula para o eleitor #" + (ballots.length + 1));
+        
+    // get the next ballot
+    $.get(election_url + "/ballots/" + ballot_list[ballots.length].uuid + '/last', function(result) {
+        var new_ballot = jQuery.secureEvalJSON(result);
+        ballots.push(new_ballot);
+        if (new_ballot.vote == null)
+          result_append("nenhuma cédula para o eleitor #" + ballots.length);
+        else
+          result_append("CÉDULA ENCONTRADA para o eleitor #" + ballots.length);
+        load_ballots(election_url, ballot_list, ballots, final_callback);
+    });
+}
+
+// load the ballot list in increments of 50, for long ballots
+function load_ballot_list(election_url, ballot_list, after, final_callback) {
+    var url = election_url + "/voters/?limit=50";
+    if (after)
+        url+= "&after=" + after;
+        
+    $.get(url, function(result) {
+        var new_ballot_list = jQuery.secureEvalJSON(result);
+
+        // done, no more ballots?
+        if (new_ballot_list.length == 0)
+            return final_callback(ballot_list);
+            
+        // not done, add to the list
+        ballot_list = ballot_list.concat(new_ballot_list);
+        after = ballot_list[ballot_list.length - 1].uuid;
+        
+        // and iterate
+        load_ballot_list(election_url, ballot_list, after, final_callback);
+    });
+}
+
+function load_election_and_ballots(election_url) {
+        
+    result_append("<h3>Eleição</h3>");
+    result_append("carregando eleição...");
+
+    var overall_result = true;
+    
+    // the hash will be computed within the setup function call now
+    $.get(election_url, function(raw_json) {
+        try {
+            election = HELIOS.Election.fromJSONString(raw_json);
+            result_append("eleição carregada: " + election.name);
+            result_append("identificador: " + election.get_hash());
+
+            var tally = [];
+
+            $(election.questions).each(function(qnum, q) {
+                if (q.tally_type != "homomorphic") {
+                  result_append("ERRO: esta eleição não apresenta totalização homomórfica. O Helios não pode verificá-la.");
+                  return;
+                }
+
+                tally[qnum] = $(q.answers).map(function(anum, a) {
+                    return 1;
+                });
+            });
+
+            result_append("carregando lista de eleitores...");
+            
+            // load voter list
+            load_ballot_list(election_url, [], null, function(ballot_list) {
+                result_append("lista de eleitores carregada; carregando as cédulas de cada eleitor..");
+
+                // load all ballots
+                load_ballots(election_url, ballot_list, [], function(ballots) {
+                    result_append("");
+                    result_append("<h3>Cédulas</h3>");
+                    // now load each ballot
+                    $(ballots).each(function(i, cast_vote){
+
+                        if (cast_vote.vote == null)
+                          return;
+
+                        var vote = HELIOS.EncryptedVote.fromJSONObject(cast_vote.vote, election);
+                        result_append("Eleitor #" + (i+1));
+                        result_append("-- UUID: " + cast_vote.voter_uuid);
+                        result_append("-- Número de rastreio de cédula: " + vote.get_hash());
+
+                        vote.verifyProofs(election.public_key, function(answer_num, choice_num, result, choice) {
+                            overall_result = overall_result && result;
+                            if (choice_num != null) {
+                                // keep track of tally
+                                tally[answer_num][choice_num] = choice.multiply(tally[answer_num][choice_num]);
+
+                                result_append("Pleito #" + (answer_num+1) + ", Alternativa #" + (choice_num+1) + " -- " + pretty_result(result));
+                            } else {
+                                result_append("Pleito #" + (answer_num+1) + " TOTAL -- " + pretty_result(result));
+                            }
+                        });
+
+                        result_append("");
+                    });
+
+                    // get the election result
+                    $.get(election_url + "/result", function(result) {
+                        var results = $.secureEvalJSON(result);
+
+                        // get the trustees and proofs
+                        $.get(election_url + "/trustees/", function(trustees_json) {
+                           trustees = $.secureEvalJSON(trustees_json);
+
+                           // create the Helios objects
+                           trustees = $(trustees).map(function(i, trustee) {return HELIOS.Trustee.fromJSONObject(trustee)});
+
+                           // the public key that we'll check
+                           var combined_key = 1;
+
+                           result_append("<h3>Guardiões</h3>");
+                           // verify the keys
+                           $(trustees).each(function(i, trustee) {
+                              var trustee_email = trustee.email;
+                              if(typeof trustee_email == "undefined") trustee_email = "sem email";
+                              result_append("Guardião #" + (i+1) + ": " + trustee_email);
+                              if (trustee.public_key.verifyKnowledgeOfSecretKey(trustee.pok, ElGamal.fiatshamir_dlog_challenge_generator)) {
+                                  result_append("-- CHAVE PÚBLICA " + trustee.public_key_hash + " -- VERIFICADA.");
+
+                                  // FIXME check the public key hash
+                                  } else {
+                                  var trustee_email = trustee.email;
+                                  if(typeof trustee_email == "undefined") trustee_email = "sem email";
+                                  result_append("==== ERRO na CHAVE PÚBLICA do guardião " + trustee_email);
+                                  overall_result = false;
+                              }
+
+                              combined_key = trustee.public_key.multiply(combined_key);
+
+                              result_append("");
+                           });
+
+                           // verify the combination of the keys into the final public key
+                           if (combined_key.equals(election.public_key)) {
+                               result_append("chave pública da eleição CORRETA");
+                           } else {
+                               result_append("==== ERRO: chave pública da eleição não confere");
+                               overall_result = false;
+                           }
+
+                           result_append("<h3>Resultado</h3>");
+
+                           $(tally).each(function(q_num, q) {
+                               result_append("Pleito #" + (q_num+1) + ": " + election.questions[q_num].short_name);
+                               $(q).each(function(a_num, a) {
+                                   var plaintext = new ElGamal.Plaintext(election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p), election.public_key);
+
+                                   var check = true;
+                                   result_append("Alternativa #" + (a_num + 1) + ": " + election.questions[q_num].answers[a_num] + " - CONTAGEM = " + results[q_num][a_num]);
+
+                                   var decryption_factors = [];
+
+                                   // go through the trustees' decryption factors and verify each one
+                                   $(trustees).each(function(t_num, trustee) {
+                                       if (trustee.public_key.verifyDecryptionFactor(a, trustee.decryption_factors[q_num][a_num],
+                                                       trustee.decryption_proofs[q_num][a_num], ElGamal.fiatshamir_challenge_generator)) {
+
+                                           var trustee_email = trustee.email;
+                                           if(typeof trustee_email == "undefined") trustee_email = "sem email";
+                                           result_append("-- Guardião " + trustee_email + ": fator de decifração confere");
+                                       } else {
+                                           var trustee_email = trustee.email;
+                                           if(typeof trustee_email == "undefined") trustee_email = "sem email";
+
+                                           result_append("==== ERRO com Guardião " + trustee_email + ": fator de decifração não confere");
+                                           check= false;
+                                           overall_result = false;
+                                       }
+
+                                       decryption_factors.push(trustee.decryption_factors[q_num][a_num]);
+                                   });
+
+                                   // recheck decryption factors
+                                   var expected_value = election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p);                        
+                                   var recomputed_value = a.decrypt(decryption_factors).getM();
+                                   if (expected_value.equals(recomputed_value)) {
+                                   } else {
+                                       check = false;
+                                       overall_result = false;
+                                   }
+
+                                   result_append("-" + pretty_result(check));
+                               });
+
+                           });
+
+                           result_append("<h3>RESULTADO FINAL</h3>");
+
+                           if (overall_result) {
+                             result_append("ELEIÇÃO TOTALMENTE VERIFICADA -- SUCESSO!!");
+                           } else {
+                             result_append("FALHA NA VERIFICAÇÃO DA ELEIÇÃO");
+                           }
+                        });
+                    });
+                });
+
+            });
+    } catch (error) {
+        result_append("<p>Você está tentando verificar uma eleição privada.</p>");
+        result_append('<p>Você pode se conectar ao sistema como um eleitor válido para esta eleição ou como seu administrador.</p>');
+        result_append('<a class="btn" href="' + election_url + '">Conectar como eleitor válido </a>');
+        result_append('<a class="btn" href="/auth/?return_url=/verifier/verify.html?election_url=' + election_url + '">Conectar como administrador da eleição</a>');
+    }
+    });
+        
+}
+
+$(document).ready(function() {
+   var election_url = $.query.get('election_url');
+   $('#election_url').val(election_url); 
+});
+</script>
+</head>
+<body>
+<div id="wrapper">
+<div id="banner">
+    Verificador de Eleições do Helios
+</div>
+<div id="content">
+
+<div id="verifier_loading">
+carregando verificador ...
+</div>
+
+<div id="verifier" align="center" style="display:none;">
+URL da Eleição:
+<form onsubmit="try{load_election_and_ballots(this.election_url.value);} catch (e) {} return false;">
+    <input type="text" size="50" name="election_url" id="election_url" /><br />
+    <input type="submit" value="iniciar verificação" />
+</form>
+</div>
+
+<br /><br />
+<div id="results">
+</div>
+</div>
+</div>
+<div id="applet_div">
+</div>
+</body>
+</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/heliosverifier/verify.html helios-server-translation/heliosverifier/verify.html
--- helios-server-original/heliosverifier/verify.html	2019-10-28 03:15:16.935087172 -0300
+++ helios-server-translation/heliosverifier/verify.html	2019-10-29 03:15:08.031263282 -0300
@@ -1,289 +1,30 @@
-<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
-<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
-<head>
-  <META HTTP-EQUIV="Content-Type" CONTENT="text/html; charset=UTF-8" /> 
-  <title>Helios Voting System -- Verifier</title>
-  <link rel="stylesheet" type="text/css" href="css/booth.css" />
-  <link rel="stylesheet" type="text/css" href="css/forms.css" />
-  <script language="javascript" src="js/jscrypto/jsbn.js"></script>
-  <script language="javascript" src="js/jscrypto/jsbn2.js"></script>
-  <script language="javascript" src="js/jscrypto/sjcl.js"></script>
-  <script language="javascript" src="js/underscore-min.js"></script> 
-  <script language="javascript" src="js/jquery-1.2.2.min.js"></script>
-  <script language="javascript" src="js/jquery-jtemplates.js"></script>
-  <script language="javascript" src="js/jquery.query-2.1.5.js"></script>
-  <script language="javascript" src="js/jquery.json.min.js"></script>
-
-  <script language="javascript">
-    // required for jscrypto library
-    var JSCRYPTO_HOME = document.location.pathname.replace("verify.html", "js/jscrypto");
-  </script>
-
-  <script language="javascript" src="js/jscrypto/class.js"></script>  
-  <script language="javascript" src="js/jscrypto/bigint.js"></script>
-  <script language="javascript" src="js/jscrypto/random.js"></script>
-  <script language="javascript" src="js/jscrypto/elgamal.js"></script>
-  <script language="javascript" src="js/jscrypto/sha1.js"></script>
-  <script language="javascript" src="js/jscrypto/sha2.js"></script>
-  <script language="javascript" src="js/jscrypto/helios.js"></script>
+<!doctype html>
 
 <script language="javascript">
-$(document).ready(function() {
-    BigInt.setup(function() {
-       $('#verifier_loading').hide();
-       $('#verifier').show();
-    }, function() {
-       alert('sorry, in-browser verification requires Java Support at this time.');
-    });
-});    
-
-function result_append(str) {
-    $('#results').append(str).append("<br />");
-}
-
-function pretty_result(result) {return result? "VERIFIED" : "FAIL";}
-
-function load_ballots(election_url, ballot_list, ballots, final_callback) {
-    // the ballots array is the place where we build up the list of ballots
-
-    // end of the iteration?
-    if (ballot_list.length == ballots.length) {
-        final_callback(ballots);
-        return;
-    }
-    
-    result_append("loading ballot for voter #" + (ballots.length + 1));
-        
-    // get the next ballot
-    $.get(election_url + "/ballots/" + ballot_list[ballots.length].uuid + '/last', function(result) {
-        var new_ballot = jQuery.secureEvalJSON(result);
-        ballots.push(new_ballot);
-        if (new_ballot.vote == null)
-          result_append("no ballot for this voter #" + ballots.length);
-        else
-          result_append("FOUND a ballot for voter #" + ballots.length);
-        load_ballots(election_url, ballot_list, ballots, final_callback);
-    });
-}
-
-// load the ballot list in increments of 50, for long ballots
-function load_ballot_list(election_url, ballot_list, after, final_callback) {
-    var url = election_url + "/voters/?limit=50";
-    if (after)
-        url+= "&after=" + after;
-        
-    $.get(url, function(result) {
-        var new_ballot_list = jQuery.secureEvalJSON(result);
-
-        // done, no more ballots?
-        if (new_ballot_list.length == 0)
-            return final_callback(ballot_list);
-            
-        // not done, add to the list
-        ballot_list = ballot_list.concat(new_ballot_list);
-        after = ballot_list[ballot_list.length - 1].uuid;
-        
-        // and iterate
-        load_ballot_list(election_url, ballot_list, after, final_callback);
-    });
-}
-
-function load_election_and_ballots(election_url) {
-        
-    result_append("<h3>Election</h3>");
-    result_append("loading election...");
-
-    var overall_result = true;
-    
-    // the hash will be computed within the setup function call now
-    $.get(election_url, function(raw_json) {
-        try {
-            election = HELIOS.Election.fromJSONString(raw_json);
-            result_append("loaded election: " + election.name);
-            result_append("election fingerprint: " + election.get_hash());
-
-            var tally = [];
-
-            $(election.questions).each(function(qnum, q) {
-                if (q.tally_type != "homomorphic") {
-                  result_append("PROBLEM: this election is not a straight-forward homomorphic-tally election. As a result, Helios cannot currently verify it.");
-                  return;
-                }
-
-                tally[qnum] = $(q.answers).map(function(anum, a) {
-                    return 1;
-                });
-            });
-
-            result_append("loading list of voters...");
-            
-            // load voter list
-            load_ballot_list(election_url, [], null, function(ballot_list) {
-                result_append("loaded voter list, now loading ballots for each..");
-
-                // load all ballots
-                load_ballots(election_url, ballot_list, [], function(ballots) {
-                    result_append("");
-                    result_append("<h3>Ballots</h3>");
-                    // now load each ballot
-                    $(ballots).each(function(i, cast_vote){
-
-                        if (cast_vote.vote == null)
-                          return;
-
-                        var vote = HELIOS.EncryptedVote.fromJSONObject(cast_vote.vote, election);
-                        result_append("Voter #" + (i+1));
-                        result_append("-- UUID: " + cast_vote.voter_uuid);
-                        result_append("-- Ballot Tracking Number: " + vote.get_hash());
-
-                        vote.verifyProofs(election.public_key, function(answer_num, choice_num, result, choice) {
-                            overall_result = overall_result && result;
-                            if (choice_num != null) {
-                                // keep track of tally
-                                tally[answer_num][choice_num] = choice.multiply(tally[answer_num][choice_num]);
-
-                                result_append("Question #" + (answer_num+1) + ", Option #" + (choice_num+1) + " -- " + pretty_result(result));
-                            } else {
-                                result_append("Question #" + (answer_num+1) + " OVERALL -- " + pretty_result(result));
-                            }
-                        });
-
-                        result_append("");
-                    });
-
-                    // get the election result
-                    $.get(election_url + "/result", function(result) {
-                        var results = $.secureEvalJSON(result);
-
-                        // get the trustees and proofs
-                        $.get(election_url + "/trustees/", function(trustees_json) {
-                           trustees = $.secureEvalJSON(trustees_json);
 
-                           // create the Helios objects
-                           trustees = $(trustees).map(function(i, trustee) {return HELIOS.Trustee.fromJSONObject(trustee)});
-
-                           // the public key that we'll check
-                           var combined_key = 1;
-
-                           result_append("<h3>Trustees</h3>");
-                           // verify the keys
-                           $(trustees).each(function(i, trustee) {
-                              result_append("Trustee #" + (i+1) + ": " + trustee.email);
-                              if (trustee.public_key.verifyKnowledgeOfSecretKey(trustee.pok, ElGamal.fiatshamir_dlog_challenge_generator)) {
-                                  result_append("-- PK " + trustee.public_key_hash + " -- VERIFIED.");
-
-                                  // FIXME check the public key hash
-                              } else {
-                                  result_append("==== ERROR for PK of trustee " + trustee.email);
-                                  overall_result = false;
-                              }
-
-                              combined_key = trustee.public_key.multiply(combined_key);
-
-                              result_append("");
-                           });
-
-                           // verify the combination of the keys into the final public key
-                           if (combined_key.equals(election.public_key)) {
-                               result_append("election public key CORRECTLY FORMED");
-                           } else {
-                               result_append("==== ERROR, election public key doesn't match");
-                               overall_result = false;
-                           }
-
-                           result_append("<h3>Tally</h3>");
-
-                           $(tally).each(function(q_num, q) {
-                               result_append("Question #" + (q_num+1) + ": " + election.questions[q_num].short_name);
-                               $(q).each(function(a_num, a) {
-                                   var plaintext = new ElGamal.Plaintext(election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p), election.public_key);
-
-                                   var check = true;
-                                   result_append("Answer #" + (a_num + 1) + ": " + election.questions[q_num].answers[a_num] + " - COUNT = " + results[q_num][a_num]);
-
-                                   var decryption_factors = [];
-
-                                   // go through the trustees' decryption factors and verify each one
-                                   $(trustees).each(function(t_num, trustee) {
-                                       if (trustee.public_key.verifyDecryptionFactor(a, trustee.decryption_factors[q_num][a_num],
-                                                       trustee.decryption_proofs[q_num][a_num], ElGamal.fiatshamir_challenge_generator)) {
-                                           result_append("-- Trustee " + trustee.email + ": decryption factor verifies");
-                                       } else {
-                                           result_append("==== ERROR with Trustee " + trustee.email + ": decryption factor does not verify");
-                                           check= false;
-                                           overall_result = false;
-                                       }
-
-                                       decryption_factors.push(trustee.decryption_factors[q_num][a_num]);
-                                   });
-
-                                   // recheck decryption factors
-                                   var expected_value = election.public_key.g.modPow(BigInt.fromInt(results[q_num][a_num]), election.public_key.p);                        
-                                   var recomputed_value = a.decrypt(decryption_factors).getM();
-                                   if (expected_value.equals(recomputed_value)) {
-                                   } else {
-                                       check = false;
-                                       overall_result = false;
-                                   }
-
-                                   result_append("-" + pretty_result(check));
-                               });
-
-                           });
-
-                           result_append("<h3>FINAL RESULT</h3>");
-
-                           if (overall_result) {
-                             result_append("ELECTION FULLY VERIFIED -- SUCCESS!");
-                           } else {
-                             result_append("VERIFICATION FAILED");
-                           }
-                        });
-                    });
-                });
-
-            });
-    } catch (error) {
-        result_append("<p>It appears that you are trying to verify a private election.</p>");
-        result_append('<p>You can log in as a valid voter or log in as the election admin.</p>');
-        result_append('<a class="btn" href="' + election_url + '">Log in as a valid voter </a>');
-        result_append('<a class="btn" href="/auth/?return_url=/verifier/verify.html?election_url=' + election_url + '">Log in as the election admin</a>');
-    }
-    });
-        
-}
-
-$(document).ready(function() {
-   var election_url = $.query.get('election_url');
-   $('#election_url').val(election_url); 
-});
+  // Fetch URL parameters
+  var parameters = window.location.search;
+  
+  // Start static HTML language decision block
+  var lang = navigator.language || navigator.userLanguage;
+  var base = "verify.html";
+  var file = "./" + lang + "-" + base;
+
+  var xhr = new XMLHttpRequest();
+  xhr.open('HEAD', file, false);
+
+  try{
+  xhr.send();
+  if (xhr.status == "200"){
+  window.location = file + parameters;
+  }
+  else{
+  window.location = "./default-" + base + parameters;
+  }
+  }
+  
+  catch (e){
+  window.location = "./default-" + base + parameters;
+  }
+  
 </script>
-</head>
-<body>
-<div id="wrapper">
-<div id="banner">
-    Helios Election Verifier
-</div>
-<div id="content">
-
-<div id="verifier_loading">
-loading verifier ...
-</div>
-
-<div id="verifier" align="center" style="display:none;">
-Enter the Election URL:
-<form onsubmit="try{load_election_and_ballots(this.election_url.value);} catch (e) {} return false;">
-    <input type="text" size="50" name="election_url" id="election_url" /><br />
-    <input type="submit" value="start verification" />
-</form>
-</div>
-
-<br /><br />
-<div id="results">
-</div>
-</div>
-</div>
-<div id="applet_div">
-</div>
-</body>
-</html>
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/locale/pt_BR/LC_MESSAGES/django.po helios-server-translation/locale/pt_BR/LC_MESSAGES/django.po
--- helios-server-original/locale/pt_BR/LC_MESSAGES/django.po	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/locale/pt_BR/LC_MESSAGES/django.po	2019-10-29 03:26:06.116257006 -0300
@@ -0,0 +1,1967 @@
+# SOME DESCRIPTIVE TITLE.
+# Copyright (C) YEAR THE PACKAGE'S COPYRIGHT HOLDER
+# This file is distributed under the same license as the PACKAGE package.
+# FIRST AUTHOR <EMAIL@ADDRESS>, YEAR.
+#
+#, fuzzy
+msgid ""
+msgstr ""
+"Project-Id-Version: PACKAGE VERSION\n"
+"Report-Msgid-Bugs-To: \n"
+"POT-Creation-Date: 2019-10-29 03:06-0300\n"
+"PO-Revision-Date: YEAR-MO-DA HO:MI+ZONE\n"
+"Last-Translator: FULL NAME <EMAIL@ADDRESS>\n"
+"Language-Team: LANGUAGE <LL@li.org>\n"
+"Language: \n"
+"MIME-Version: 1.0\n"
+"Content-Type: text/plain; charset=UTF-8\n"
+"Content-Transfer-Encoding: 8bit\n"
+"Plural-Forms: nplurals=2; plural=(n > 1);\n"
+
+#: helios/forms.py:15
+msgid "Short Name"
+msgstr "Nome para URL"
+
+#: helios/forms.py:15
+msgid "no spaces, will be part of the URL for your election, e.g. my-club-2010"
+msgstr ""
+"Este nome fará parte da URL da votação. Não use espaços. Ex.: pleito-2022"
+
+#: helios/forms.py:16 helios/templates/new_trustee.html:9
+#: helios/templates/voters_list.html:121
+msgid "Name"
+msgstr "Nome"
+
+#: helios/forms.py:16
+msgid "the pretty name for your election, e.g. My Club 2010 Election"
+msgstr "Este é o nome de apresentação da votação. Ex.: Eleições Internas 2022"
+
+#: helios/forms.py:17
+msgid "Description"
+msgstr "Descrição"
+
+#: helios/forms.py:18
+msgid "type"
+msgstr "Tipo"
+
+#: helios/forms.py:19
+msgid "Use voter aliases"
+msgstr "Usar pseudônimos"
+
+#: helios/forms.py:19
+msgid ""
+"If selected, voter identities will be replaced with aliases, e.g. \"V12\", "
+"in the ballot tracking center"
+msgstr ""
+"Se marcado, as identidades reais dos votantes são substituídas por "
+"pseudônimos (como 'V1', 'V2', 'V3' etc) na Central de Gerenciamento de "
+"Cédulas"
+
+#: helios/forms.py:21
+msgid "Randomize answer order"
+msgstr "Alternativas em ordem aleatória"
+
+#: helios/forms.py:21
+msgid ""
+"enable this if you want the answers to questions to appear in random order "
+"for each voter"
+msgstr ""
+"Caso marcado, as alternativas mostradas ao votante aparecem em ordem "
+"aleatória"
+
+#: helios/forms.py:22
+msgid "Private?"
+msgstr "Votação privada"
+
+#: helios/forms.py:22
+msgid "A private election is only visible to registered voters."
+msgstr "Uma votação privada é visível apenas para votantes registrados"
+
+#: helios/forms.py:23
+msgid "Help Email Address"
+msgstr "Email de ajuda"
+
+#: helios/forms.py:23
+msgid "An email address voters should contact if they need help."
+msgstr "Um endereço de email para contato caso os votantes precisem de ajuda"
+
+#: helios/forms.py:26
+msgid "Election Info Download URL"
+msgstr "Mais informações"
+
+#: helios/forms.py:26
+msgid ""
+"the URL of a PDF document that contains extra election information, e.g. "
+"candidate bios and statements"
+msgstr ""
+"Uma URL para download de informações adicionais sobre a votação, como "
+"históricos de candidatos e respectivas propostas"
+
+#: helios/forms.py:29
+msgid "Voting starts at"
+msgstr "A votação se inicia em"
+
+#: helios/forms.py:29
+msgid "UTC date and time when voting begins"
+msgstr "Data e hora (UTC) de início da votação"
+
+#: helios/forms.py:31
+msgid "Voting ends at"
+msgstr "A votação termina em"
+
+#: helios/forms.py:31
+msgid "UTC date and time when voting ends"
+msgstr "Data e hora (UTC) de fim da votação"
+
+#: helios/forms.py:35
+msgid "Voting extended until"
+msgstr "Votação estendida até"
+
+#: helios/forms.py:35
+msgid "UTC date and time voting extended to"
+msgstr "Data e hora (UTC) de extensão da votação"
+
+#: helios/forms.py:39 helios/forms.py:44
+msgid "Subject"
+msgstr "Assunto"
+
+#: helios/forms.py:40 helios/forms.py:45
+msgid "Body"
+msgstr "Mensagem"
+
+#: helios/forms.py:41 helios/forms.py:46
+msgid "Send To"
+msgstr "Enviar para"
+
+#: helios/forms.py:41 helios/forms.py:46
+msgid "all voters"
+msgstr "Todos os votantes"
+
+#: helios/forms.py:41
+msgid "voters who have cast a ballot"
+msgstr "Votantes que já enviaram cédulas"
+
+#: helios/forms.py:41
+msgid "voters who have not yet cast a ballot"
+msgstr "Votantes que ainda não enviaram cédulas"
+
+#: helios/forms.py:46
+msgid "only voters who cast a ballot"
+msgstr "apenas votantes que já enviaram cédulas"
+
+#: helios/forms.py:46
+msgid "no one -- are you sure about this?"
+msgstr "ninguém"
+
+#: helios/forms.py:49
+msgid "Voter ID"
+msgstr "Login"
+
+#: helios/models.py:54
+msgid "election"
+msgstr "votação"
+
+#: helios/models.py:54
+msgid "Election"
+msgstr "Votação"
+
+#: helios/models.py:55
+msgid "referendum"
+msgstr "referendo"
+
+#: helios/models.py:55
+msgid "Referendum"
+msgstr "Referendo"
+
+#: helios/models.py:325
+msgid "Anyone can vote."
+msgstr "Qualquer um pode votar"
+
+#: helios/models.py:334
+#, python-format
+msgid "<li> any %s user</li>"
+msgstr "<li> qualquer usuário com acesso por: %s</li>"
+
+#: helios/models.py:360
+msgid "add questions to the ballot"
+msgstr "adicione um pleito à cédula de votação"
+
+#: helios/models.py:367
+msgid "add at least one trustee"
+msgstr "adicione pelo menos um guardião"
+
+#: helios/models.py:374
+#, python-format
+msgid "have trustee %s generate a keypair"
+msgstr ""
+
+#: helios/models.py:380
+msgid "enter your voter list (or open registration to the public)"
+msgstr "entre com uma lista de votantes (ou abra a votação ao público)"
+
+#: helios/models.py:485
+msgid "password"
+msgstr "senha"
+
+#: helios/templates/_castconfirm_password.html:3
+msgid "Please provide the voter ID and password you received by email."
+msgstr "Informe abaixo seu login e a senha que você recebeu."
+
+#: helios/templates/_castconfirm_password.html:13
+msgid "bad voter ID or password, please try again."
+msgstr "Login ou senha não conferem; tente novamente."
+
+#: helios/templates/_castconfirm_password.html:18
+msgid ""
+"You may cast as many ballots as you wish:<br />only the last one counts."
+msgstr ""
+"Você pode enviar quantas cédulas desejar:<br />apenas a última cédula "
+"enviada conta."
+
+#: helios/templates/_castconfirm_password.html:20
+msgid "cast ballot"
+msgstr "enviar cédula"
+
+#: helios/templates/_castconfirm_password.html:22
+#: server_ui/templates/base.html:99
+msgid "log in"
+msgstr "autenticar"
+
+#: helios/templates/cast_done.html:4
+#: helios/templates/election_cast_confirm.html:4
+msgid "Confirm Vote"
+msgstr "Confirmar Voto"
+
+#: helios/templates/cast_done.html:7
+msgid "Vote Successfully Cast!"
+msgstr "Cédula Enviada com Sucesso"
+
+#: helios/templates/cast_done.html:10
+msgid "Congratulations, your vote has been <b><u>successfully cast</u></b>!"
+msgstr "Sua cédula foi <b><u>enviada com sucesso</u></b>!"
+
+#: helios/templates/cast_done.html:14
+#: helios/templates/election_cast_confirm.html:104
+msgid "Your smart ballot tracker is:"
+msgstr "O localizador da sua cédula é:"
+
+#: helios/templates/cast_done.html:19
+msgid "For your safety, we have logged you out."
+msgstr "Para sua segurança você foi desconectado."
+
+#: helios/templates/cast_done.html:25
+msgid "return to election info"
+msgstr "retornar para a página principal da votação"
+
+#: helios/templates/castvote.html:6
+msgctxt "vote id follows"
+msgid "Cast Vote"
+msgstr "Cédula Enviada"
+
+#: helios/templates/castvote.html:7
+msgctxt "election name follows"
+msgid "cast in"
+msgstr "enviada para"
+
+#: helios/templates/castvote.html:8
+msgid "Fingerprint:"
+msgstr "Identificador:"
+
+#: helios/templates/castvote.html:9
+msgctxt "voter name follows"
+msgid "by"
+msgstr "por"
+
+#: helios/templates/castvote.html:17
+msgid "details"
+msgstr "detalhes"
+
+#: helios/templates/combine_decryptions.html:4
+#: helios/templates/combine_decryptions.html:6
+msgid "Compute Tally"
+msgstr "Começar apuração"
+
+#: helios/templates/combine_decryptions.html:6
+#: helios/templates/election_edit.html:6 helios/templates/new_trustee.html:5
+#: helios/templates/release_result.html:6
+msgid "cancel"
+msgstr "cancelar"
+
+#: helios/templates/combine_decryptions.html:9
+msgid ""
+"You are about to compute the tally for this election. You only will then see "
+"the results."
+msgstr "Começar apuração. Apenas você poderá ver os resultados."
+
+#: helios/templates/combine_decryptions.html:14
+msgid "compute the tally!"
+msgstr "computar apuração"
+
+#: helios/templates/election_audited_ballots.html:4
+msgctxt "name follows"
+msgid "Audited Ballots for"
+msgstr "Cédulas Auditadas para"
+
+#: helios/templates/election_audited_ballots.html:7
+#: helios/templates/election_audited_ballots.html:23
+#: helios/templates/election_view.html:279
+msgid "Audited Ballots"
+msgstr "Cédulas Auditadas"
+
+#: helios/templates/election_audited_ballots.html:7
+#: helios/templates/election_compute_tally.html:28
+#: helios/templates/election_questions.html:6
+#: helios/templates/list_trustees.html:7 helios/templates/voters_email.html:13
+#: helios/templates/voters_list.html:6 helios/templates/voters_upload.html:5
+#: helios/templates/voters_upload_confirm.html:5
+msgid "back to election"
+msgstr "voltar"
+
+#: helios/templates/election_audited_ballots.html:10
+msgid ""
+"When you prepare a ballot with Helios, you immediately receive a smart "
+"ballot tracker. Before you choose to cast that ballot, you have the option "
+"to ask Helios to \"break open\" that encrypted ballot and verify that Helios "
+"encrypted your ballot correctly. Once that's done, you can post that opened "
+"ballot here, on the audited ballots' list, for everyone to verify (your "
+"identity is not included). Once you've done this, you have to re-encrypt "
+"your choices and obtain a different smart ballot tracker. This helps reduce "
+"the chance that someone might coerce you to vote differently from your true "
+"choice."
+msgstr ""
+"Ao preparar uma cédula com as suas escolhas no Helios/INSCRYPT você receberá "
+"um número único que lhe permite localizar sua cédula. Antes de enviá-la ao "
+"sistema para que seja contabilizada, você pode optar por abrir a sua cédula "
+"cifrada para verificar que ela de fato foi cifrada corretamente. Isto feito, "
+"você poderá postar essa sua cédula aberta aqui para que outros possam também "
+"verificar a correta cifração. Nesse processo, a sua identidade jamais será "
+"revelada. Após auditar sua cédula, você deverá cifrar suas escolhas "
+"novamente para obter uma nova cédula com um novo localizador contendo suas "
+"escolhas."
+
+#: helios/templates/election_audited_ballots.html:14
+msgid ""
+"These ballots are <em>not cast</em>, and they will not be counted. They are "
+"just here for auditing purposes, to spot-check that Helios is properly "
+"encrypting voter's choices."
+msgstr ""
+"As cédulas aqui listadas <em>não serão apuradas</em>. Elas se encontram aqui "
+"apenas para fins de auditoria, permitindo assim verificar que o "
+"Helios/INSCRYPT está cifrando corretamente as alternativas dos votantes."
+
+#: helios/templates/election_audited_ballots.html:18
+msgctxt "ballot verifier name follows"
+msgid ""
+"To verify an audited ballot, copy its entire content and paste it in the"
+msgstr ""
+"Para verificar uma cédula auditada, copie seu conteúdo integralmente e cole "
+"no"
+
+#: helios/templates/election_audited_ballots.html:18
+msgid "single ballot verifier"
+msgstr "verificador de cédulas"
+
+#: helios/templates/election_audited_ballots.html:35
+msgctxt "number of ballots follows"
+msgid "next"
+msgstr "próximas"
+
+#: helios/templates/election_audited_ballots.html:39
+#: helios/templates/voters_list.html:148
+msgid "view"
+msgstr "visualizar"
+
+#: helios/templates/election_audited_ballots.html:43
+msgid "no audited ballots yet"
+msgstr "não há cédulas auditadas até o momento"
+
+#: helios/templates/election_cast_confirm.html:29
+msgid ""
+"You have not yet cast your ballot! Make sure to complete the voting process "
+"if you want your vote to count."
+msgstr ""
+"Você não enviou sua cédula ainda. Assegure-se de completar o processo de "
+"votação se quiser que sua cédula seja contabilizada."
+
+#: helios/templates/election_cast_confirm.html:39
+msgctxt "name follows"
+msgid "Submitting your vote for"
+msgstr "Enviando sua cédula para a votação"
+
+#: helios/templates/election_cast_confirm.html:42
+msgid "Verifying and Casting your ballot"
+msgstr "Verificando e enviando sua cédula"
+
+#: helios/templates/election_cast_confirm.html:56
+msgid "Wait!"
+msgstr "Espere!"
+
+#: helios/templates/election_cast_confirm.html:58
+msgctxt "name follows"
+msgid "You are logged in as"
+msgstr "Você está autenticado como"
+
+#: helios/templates/election_cast_confirm.html:58
+msgctxt "complement follows"
+msgid "but this election"
+msgstr "mas esta votação"
+
+#: helios/templates/election_cast_confirm.html:59
+msgid "requires election-specific credentials."
+msgstr "requer credenciais específicas."
+
+#: helios/templates/election_cast_confirm.html:69
+msgid "Sorry!"
+msgstr ""
+
+#: helios/templates/election_cast_confirm.html:70
+msgid "Sorry, you are"
+msgstr "Você"
+
+#: helios/templates/election_cast_confirm.html:72
+msgid "<u>not eligible</u></em> for this election."
+msgstr "<u>não está apto</u></em> a votar nesta votação."
+
+#: helios/templates/election_cast_confirm.html:74
+msgid "not registered</em> for this election, and registration is closed."
+msgstr "não está registrado</em> para votar nesta votação."
+
+#: helios/templates/election_cast_confirm.html:78
+msgid "return to the main election page"
+msgstr "retornar para a página principal da votação"
+
+#: helios/templates/election_compute_tally.html:4
+msgctxt "name follows"
+msgid "Compute Encryted Tally for"
+msgstr "Computar apuração para a votação"
+
+#: helios/templates/election_compute_tally.html:7
+msgctxt "name follows"
+msgid "Compute Tally for Election:"
+msgstr "Computar Apuração para Votação:"
+
+#: helios/templates/election_compute_tally.html:12
+msgctxt "name follows"
+msgid "You are about to compute the encrypted tally for election"
+msgstr "Computar a apuração para a votação"
+
+#: helios/templates/election_compute_tally.html:16
+msgid "Once you do this, voters will no longer be able to cast a ballot."
+msgstr ""
+"Após o início da apuração, os votantes não poderão mais enviar cédulas."
+
+#: helios/templates/election_compute_tally.html:19
+msgid "'ok, tally has begun'"
+msgstr "apuração iniciada"
+
+#: helios/templates/election_compute_tally.html:22
+msgid "compute encrypted tally!"
+msgstr "computar apuração"
+
+#: helios/templates/election_compute_tally.html:23
+#: helios/templates/election_freeze.html:40
+msgid "never mind"
+msgstr "cancelar"
+
+#: helios/templates/election_compute_tally.html:27
+msgid ""
+"No votes have been cast in this election. At least one vote must be cast "
+"before you compute the tally."
+msgstr ""
+"Nenhuma cédula foi enviada ainda para esta votação. Pelo menos uma cédula "
+"deve ser enviado para que a apuração possa ser iniciada."
+
+#: helios/templates/election_edit.html:6 helios/templates/election_edit.html:20
+msgid "Update"
+msgstr "Editar"
+
+#: helios/templates/election_freeze.html:5
+msgid "Freeze Ballot"
+msgstr "Confirmar a configuração do(s) pleito(s)"
+
+#: helios/templates/election_freeze.html:7
+msgid ""
+"Once the ballot is frozen, the questions and options can no longer be "
+"modified."
+msgstr ""
+"Uma vez confirmada a configuração da cédula, o(s) pleito(s) e alternativas "
+"disponíveis para escolha não mais poderão ser alteradas."
+
+#: helios/templates/election_freeze.html:8
+msgid "The list of trustees and their public keys will also be frozen."
+msgstr ""
+"A lista de guardiões e suas respectivas chaves públicas também não poderão "
+"mais sofrer alterações."
+
+#: helios/templates/election_freeze.html:13
+msgid ""
+"Registration for your election is currently <b>open</b>, which means anyone "
+"can vote, even after you freeze the ballot."
+msgstr ""
+"O registro para sua votação se encontra <b>público</b>, de modo que qualquer "
+"um pode votar."
+
+#: helios/templates/election_freeze.html:15
+msgid ""
+"Registration for your election is currently <b>closed</b>, which means only "
+"the voters you designate will be able to cast a ballot. As the "
+"administrator, you will still be able to modify that voter list as the "
+"election progresses."
+msgstr ""
+"O registro para sua votação está <b>fechado</b>, de modo que apenas votantes "
+"designados por você poderão enviar cédulas. Como administrador, você ainda "
+"poderá modificar a lista de votantes durante a votação."
+
+#: helios/templates/election_freeze.html:21
+msgid "You must freeze the ballot before you can contact voters."
+msgstr ""
+"Você precisa confirmar a configuração do(s) pleito(s) antes de contactar os "
+"votantes."
+
+#: helios/templates/election_freeze.html:27
+msgid "Before you can freeze the election, you will need to:"
+msgstr "Antes de confirmar a configuração do(s) pleito(s) você precisa:"
+
+#: helios/templates/election_freeze.html:33
+msgid "go back to the election"
+msgstr "voltar"
+
+#: helios/templates/election_freeze.html:39
+msgid "Freeze the ballot"
+msgstr "Confirmar a configuração do(s) pleito(s)"
+
+#: helios/templates/election_keygenerator.html:103
+#: helios/templates/list_trustees.html:41
+msgid "Trustee"
+msgstr "Guardião"
+
+#: helios/templates/election_keygenerator.html:103
+msgid "Key Setup"
+msgstr "Preparação de Chave"
+
+#: helios/templates/election_keygenerator.html:106
+msgid "As a trustee, it's time to set up your key for this election."
+msgstr "Como um guardião, você deve preparar uma chave para esta votação."
+
+#: helios/templates/election_keygenerator.html:110
+msgid "Please wait for the generator to load..."
+msgstr "Aguarde enquanto o gerador é carregado..."
+
+#: helios/templates/election_keygenerator.html:115
+msgid "Generate Election Keys"
+msgstr "Gerar Chave"
+
+#: helios/templates/election_keygenerator.html:118
+msgctxt "action follows"
+msgid "If you've already generated a keypair, you can"
+msgstr "Caso você já tenha gerado uma chave, você pode"
+
+#: helios/templates/election_keygenerator.html:118
+msgctxt "action"
+msgid "reuse it"
+msgstr "reusá-la"
+
+#: helios/templates/election_keygenerator.html:122
+msgid "Reusing a Key"
+msgstr "Reusando uma Chave"
+
+#: helios/templates/election_keygenerator.html:125
+msgid "Enter your complete secret key below:"
+msgstr "Informe a sua chave secreta por completo abaixo:"
+
+#: helios/templates/election_keygenerator.html:131
+msgid "reuse"
+msgstr "reusar"
+
+#: helios/templates/election_keygenerator.html:136
+msgid "Your Secret Key"
+msgstr "Sua Chave Secreta"
+
+#: helios/templates/election_keygenerator.html:138
+msgctxt "action follows"
+msgid "Your key has been generated, but you may choose to"
+msgstr "Uma chave foi gerada para você, mas você pode optar por"
+
+#: helios/templates/election_keygenerator.html:138
+msgctxt "action"
+msgid "clear it from memory and start from scratch"
+msgstr "limpá-la da memória e recomeçar"
+
+#: helios/templates/election_keygenerator.html:138
+msgid "if you prefer."
+msgstr "caso assim prefira."
+
+#: helios/templates/election_keygenerator.html:142
+msgid "Show my secret key"
+msgstr "Mostrar chave secreta"
+
+#: helios/templates/election_keygenerator.html:147
+msgid ""
+"Bellow is your trustee secret key content. Please copy its content and save "
+"it securely."
+msgstr ""
+"Abaixo se encontra a chave secreta que foi gerada para você. Copie todo o "
+"conteúdo e guarde-o em um local seguro."
+
+#: helios/templates/election_keygenerator.html:148
+msgid "You can also click to dowload it to a file."
+msgstr "Você também pode optar por salvar a chave como um arquivo."
+
+#: helios/templates/election_keygenerator.html:149
+msgid ""
+"And please don't lose it! Otherwise it will not be possible to decrypt the "
+"election tally."
+msgstr ""
+"Assegure-se de não perdê-la! A perda dessa chave inviabiliza a decifração da "
+"apuração da eleição."
+
+#: helios/templates/election_keygenerator.html:156
+msgid "download private key to a file"
+msgstr "salvar chave como arquivo"
+
+#: helios/templates/election_keygenerator.html:159
+msgid "ok, I've saved the key, let's move on"
+msgstr "já salvei a chave; continuar"
+
+#: helios/templates/election_keygenerator.html:164
+msgid "Your Public Key"
+msgstr "Sua Chave Pública"
+
+#: helios/templates/election_keygenerator.html:166
+msgid "It's time to upload the public key to the server."
+msgstr "Você deve agora enviar a sua chave pública para o servidor."
+
+#: helios/templates/election_keygenerator.html:169
+msgid "The fingerprint of your public key is:"
+msgstr "O identificador da sua chave pública é:"
+
+#: helios/templates/election_keygenerator.html:170
+msgid ""
+"You may want to save this to confirm that your public key was properly "
+"stored by the server."
+msgstr ""
+"É importante salvar esta informação para confirmar que a sua chave pública "
+"foi devidamente armazenada no servidor."
+
+#: helios/templates/election_keygenerator.html:171
+msgid ""
+"(Your public key is not currently being displayed because you do not need to "
+"save it, the fingerprint is sufficient.)"
+msgstr ""
+"(Sua chave pública não está sendo mostrada aqui porque você não precisa "
+"salvá-la; apenas o identificador é suficiente.)"
+
+#: helios/templates/election_keygenerator.html:175
+msgid "Upload your public key"
+msgstr "Enviar sua chave pública"
+
+#: helios/templates/election_new.html:6
+msgid "Next"
+msgstr "Próximo"
+
+#: helios/templates/election_new.html:8
+msgid "Create a New Election"
+msgstr "Criar Nova Votação"
+
+#: helios/templates/election_new.html:22
+#, python-format
+msgid " %(next)s &gt;&gt;"
+msgstr ""
+
+#: helios/templates/election_not_started.html:6
+msgctxt "name follows"
+msgid "Election"
+msgstr "A votação"
+
+#: helios/templates/election_not_started.html:6
+msgid "Not Yet Open"
+msgstr "ainda não está aberta"
+
+#: helios/templates/election_not_started.html:9
+msgid ""
+"This election is not yet open. You probably got here from the Ballot Preview."
+msgstr ""
+"Esta votação ainda não está aberta para votação. Tente novamente mais tarde."
+
+#: helios/templates/election_not_started.html:13
+msgid "back to the election"
+msgstr "voltar"
+
+#: helios/templates/election_questions.html:4
+msgctxt "election name follows"
+msgid "Questions for"
+msgstr "Pleito(s) para"
+
+#: helios/templates/election_questions.html:6
+msgid "Questions"
+msgstr "Pleito(s)"
+
+#: helios/templates/election_questions.html:120
+msgid "Answer"
+msgstr "Alternativa"
+
+#: helios/templates/election_questions.html:120
+msgid "Link (optional, <tt>http</tt> or <tt>https</tt> only):"
+msgstr "Link (opcional, use apenas <tt>http</tt> ou <tt>https</tt>):"
+
+#: helios/templates/election_tallied.html:6
+msgctxt "election name follows"
+msgid "Election"
+msgstr "Votação"
+
+#: helios/templates/election_tallied.html:6
+msgctxt "election name before"
+msgid "Already Tallied"
+msgstr "Totalizada"
+
+#: helios/templates/election_tallied.html:9
+msgid "This election has already been tallied, you can no longer cast a vote."
+msgstr "Esta votação já foi apurada e não pode mais receber cédulas."
+
+#: helios/templates/election_tallied.html:13
+msgid "view the election tally"
+msgstr "visualizar resultados"
+
+#: helios/templates/election_view.html:10
+msgid "edit"
+msgstr "editar"
+
+#: helios/templates/election_view.html:14
+msgctxt "visibility"
+msgid "private"
+msgstr "visibilidade privada;"
+
+#: helios/templates/election_view.html:14
+msgctxt "visibility"
+msgid "public"
+msgstr "visibilidade pública;"
+
+#: helios/templates/election_view.html:14
+msgid "created by"
+msgstr "criada por"
+
+#: helios/templates/election_view.html:16
+msgid "[archived]"
+msgstr "arquivada"
+
+#: helios/templates/election_view.html:19
+msgid "unarchive it"
+msgstr "desarquivar"
+
+#: helios/templates/election_view.html:19
+msgid "archive it"
+msgstr "arquivar"
+
+#: helios/templates/election_view.html:20
+msgid "Are you sure you want to copy this election?"
+msgstr "Deseja realmente copiar esta votação?"
+
+#: helios/templates/election_view.html:21
+msgid "copy"
+msgstr "copiar"
+
+#: helios/templates/election_view.html:27
+#: helios/templates/election_view.html:32
+msgctxt "election type"
+msgid "this"
+msgstr "Esta"
+
+#: helios/templates/election_view.html:27
+msgid "is featured on the front page."
+msgstr "é destaque na página principal."
+
+#: helios/templates/election_view.html:32
+msgid "is <u>not</u> featured on the front page."
+msgstr "<u>não é destaque</u> na página principal."
+
+#: helios/templates/election_view.html:57
+msgid "questions"
+msgstr "Pleitos"
+
+#: helios/templates/election_view.html:59
+msgid "voters &amp; ballots"
+msgstr "Votantes &amp; Cédulas"
+
+#: helios/templates/election_view.html:61
+msgid "trustees"
+msgstr "Guardiões"
+
+#: helios/templates/election_view.html:85
+msgid "Next Step:"
+msgstr "Próximo Passo:"
+
+#: helios/templates/election_view.html:90
+msgctxt "after each action"
+msgid ", and"
+msgstr ";"
+
+#: helios/templates/election_view.html:93
+msgid "freeze ballot and open election."
+msgstr "Concluir configurações e iniciar a votação."
+
+#: helios/templates/election_view.html:96
+msgid ""
+"once you do this, the election will be ready for voting and will open "
+"automatically"
+msgstr ""
+"Ao fazer isto, a votação estará pronta para votação e será aberta "
+"automaticamente"
+
+#: helios/templates/election_view.html:97
+msgctxt "time"
+msgid "at"
+msgstr "às"
+
+#: helios/templates/election_view.html:97
+msgid "as per your settings."
+msgstr "conforme suas configurações."
+
+#: helios/templates/election_view.html:99
+msgid "once you do this, the election will be immediately open for voting."
+msgstr "Ao fazer isto, a votação será imediatamente aberta para votação."
+
+#: helios/templates/election_view.html:107
+msgid "Tally computation is under way."
+msgstr "Totalização em progresso."
+
+#: helios/templates/election_view.html:108
+msgid "Reload this page in a couple of minutes."
+msgstr "Atualize esta página dáqui a pouco."
+
+#: helios/templates/election_view.html:110
+msgid "compute encrypted tally"
+msgstr "Apurar votação"
+
+#: helios/templates/election_view.html:111
+msgid ""
+"The encrypted votes will be combined into an encrypted tally. Once this is "
+"done,"
+msgstr ""
+"As cédulas cifradas serão combinadas em uma apuração cifrada. Isto feito, "
+
+#: helios/templates/election_view.html:112
+msgid "trustees will be asked to provide their share of the decryption."
+msgstr ""
+"os guardiões serão então solicitados a prover suas respectivas partes para "
+"decifração."
+
+#: helios/templates/election_view.html:117
+msgid "release result"
+msgstr "Publicar resultados"
+
+#: helios/templates/election_view.html:118
+msgid "The result displayed below is visible only to you."
+msgstr "O resultado mostrado abaixo está visível apenas para você."
+
+#: helios/templates/election_view.html:119
+msgid "Once you release the result, it will be visible to everyone."
+msgstr "Após publicação, o resultado estará visível para todos."
+
+#: helios/templates/election_view.html:125
+msgid "compute results"
+msgstr "Computar resultados"
+
+#: helios/templates/election_view.html:127
+msgid "combine trustee decryptions and compute results"
+msgstr "combinar partes dos guardiões e computar apuração"
+
+#: helios/templates/election_view.html:131
+msgid "The result will be computed and shown to you, the administrator, only."
+msgstr "O resultado será computado e mostrado unicamente a você (admin)."
+
+#: helios/templates/election_view.html:133
+msgid ""
+"The decryption shares from the trustees will be combined and the tally "
+"computed."
+msgstr ""
+"As partes decifradas por cada guardião serão combinadas e a apuração será "
+"então computada."
+
+#: helios/templates/election_view.html:134
+msgid ""
+"Once you do this, the tally will be visible to you, the administrator, only."
+msgstr "A apuração computada será mostrada unicamente a você, o administrador"
+
+#: helios/templates/election_view.html:137
+msgid "trustees (for decryption)"
+msgstr "guardiões (para decifração)"
+
+#: helios/templates/election_view.html:158
+msgid "This election is complete."
+msgstr "Esta votação está concluída."
+
+#: helios/templates/election_view.html:162
+msgid "Tally"
+msgstr "Resultado"
+
+#: helios/templates/election_view.html:164
+msgid "Question"
+msgstr "Pleito"
+
+#: helios/templates/election_view.html:176
+msgid "Election closed. Results will be released soon."
+msgstr "Votação encerrada. Os resultados serão publicados em breve."
+
+#: helios/templates/election_view.html:181
+msgctxt "election type"
+msgid "Vote in this"
+msgstr "Participe desta"
+
+#: helios/templates/election_view.html:185
+#: helios/templates/election_view.html:190
+#: helios/templates/election_view.html:192
+msgctxt "election type"
+msgid "This"
+msgstr "Esta"
+
+#: helios/templates/election_view.html:185
+msgctxt "time"
+msgid "was initially scheduled to end at"
+msgstr "estava inicialmente prevista para terminar em"
+
+#: helios/templates/election_view.html:186
+msgctxt "time"
+msgid "but has been extended until"
+msgstr "mas teve sua duração estendida até"
+
+#: helios/templates/election_view.html:190
+msgctxt "time"
+msgid "is scheduled to end at"
+msgstr "está agendada para terminar em"
+
+#: helios/templates/election_view.html:192
+msgid "ends at the administrator's discretion."
+msgstr "termina apenas quando encerrada pelo administrador."
+
+#: helios/templates/election_view.html:199
+msgctxt "name follows"
+msgid "This election is <em>private</em>. You are signed in as eligible voter"
+msgstr "Esta votação é <em>privada</em>. Você está acessando como votante"
+
+#: helios/templates/election_view.html:205
+msgctxt "webpage"
+msgid "Visit"
+msgstr "Acesse"
+
+#: helios/templates/election_view.html:205
+msgctxt "prerequisite"
+msgid "to install it."
+msgstr "para instalação."
+
+#: helios/templates/election_view.html:210
+msgid "voting is not yet open"
+msgstr "a votação ainda não está aberta"
+
+#: helios/templates/election_view.html:217
+msgctxt "election type"
+msgid "You are registered to vote in this"
+msgstr "Você está registrado para votar nesta"
+
+#: helios/templates/election_view.html:219
+msgctxt "name follows"
+msgid "Your voter alias is"
+msgstr "O seu pseudônimo é"
+
+#: helios/templates/election_view.html:228
+msgid "You are eligible to vote in this election."
+msgstr "Você está apto a votar nesta votação."
+
+#: helios/templates/election_view.html:230
+#: helios/templates/election_view.html:233
+msgid "You are <em>not eligible</em> to vote in this"
+msgstr "Você <em>não pode votar</em> nesta"
+
+#: helios/templates/election_view.html:242
+msgid "This election is open to:"
+msgstr "Esta votação está aberta para:"
+
+#: helios/templates/election_view.html:243
+msgid "Log in"
+msgstr "Conecte-se"
+
+#: helios/templates/election_view.html:243
+msgid "to check your eligibility."
+msgstr "para verificar se você pode votar."
+
+#: helios/templates/election_view.html:245
+msgid "Anyone can vote in this election."
+msgstr "Qualquer um pode votar nesta votação"
+
+#: helios/templates/election_view.html:256
+msgid "extend voting"
+msgstr "estender prazo da votação"
+
+#: helios/templates/election_view.html:260
+msgid "Audit Info"
+msgstr "Informações de Auditoria"
+
+#: helios/templates/election_view.html:262
+msgid "Election URL:"
+msgstr "URL da votação:"
+
+#: helios/templates/election_view.html:267
+msgid "Election Fingerprint:"
+msgstr "Identificador da votação:"
+
+#: helios/templates/election_view.html:272
+msgid "Your smart ballot tracker:"
+msgstr "O localizador da sua cédula:"
+
+#: helios/templates/election_view.html:278
+msgid "Ballot Tracking Center"
+msgstr "Gerenciamento de Cédulas"
+
+#: helios/templates/election_view.html:286
+msgid "preview booth"
+msgstr "Pré-visualizar cabine de votação"
+
+#: helios/templates/election_view.html:293
+msgctxt "election tally link follows"
+msgid "verify"
+msgstr "verificar"
+
+#: helios/templates/election_view.html:293
+msgid "election tally"
+msgstr "apuração da votação"
+
+#: helios/templates/election_view.html:296
+msgctxt "voting booth"
+msgid "review the"
+msgstr "revisar a"
+
+#: helios/templates/election_view.html:296
+msgid "voting booth"
+msgstr "cabine de votação"
+
+#: helios/templates/elections_administered.html:5
+msgid "Elections you Administer"
+msgstr "Eleições administradas por você"
+
+#: helios/templates/elections_administered.html:5
+#: helios/templates/elections_voted.html:5
+msgid "back to home"
+msgstr "voltar"
+
+#: helios/templates/elections_administered.html:9
+msgctxt "number before"
+msgid "voters"
+msgstr "votante(s)"
+
+#: helios/templates/elections_administered.html:9
+msgctxt "number before"
+msgid "cast votes"
+msgstr "cédulas recebidas"
+
+#: helios/templates/elections_voted.html:5
+msgid "Elections where you voted"
+msgstr "Eleições nas quais você votou"
+
+#: helios/templates/list_trustees.html:4
+msgctxt "election name follows"
+msgid "Trustees for"
+msgstr "Guardiões de"
+
+#: helios/templates/list_trustees.html:7
+msgid "Trustees"
+msgstr "Guardiões"
+
+#: helios/templates/list_trustees.html:10
+msgid "Trustees are responsible for decrypting the election result."
+msgstr "Guardiões são os responsáveis por decifrar os resultados da votação."
+
+#: helios/templates/list_trustees.html:11
+msgid ""
+"Each trustee generates a keypair and submits the public portion to Helios."
+msgstr ""
+"Cada guardião gera um par de chaves e envia a chave pública para o "
+"Helios/INSCRYPT."
+
+#: helios/templates/list_trustees.html:12
+msgid ""
+"When it's time to decrypt, each trustee needs to provide their secret key."
+msgstr ""
+"No momento de decifrar os resultados, cada guardião usa sua chave privada "
+"para decifrar sua parte."
+
+#: helios/templates/list_trustees.html:18
+msgid ""
+"Helios is automatically your first trustee and will handle its keypair "
+"generation and decryption automatically."
+msgstr ""
+"O servidor é automaticamente configurado como um guardião, sendo responsável "
+"pela geração de um par de chaves e o uso deste para decifrar os resultados."
+
+#: helios/templates/list_trustees.html:19
+msgid ""
+"You may add additional trustees if you want, and you can even remove the "
+"Helios trustee."
+msgstr ""
+"Você pode adicionar guardiões para a votação, caso queira. Você pode também "
+"excluir o servidor do papel de guardião."
+
+#: helios/templates/list_trustees.html:20
+msgid ""
+"However, we recommend you do this only if you have a solid understanding of "
+"the trustee's role."
+msgstr ""
+"No entanto, isto é recomendado apenas para quem compreendem o papel de um "
+"guardião e suas respectivas responsabilidades."
+
+#: helios/templates/list_trustees.html:24
+msgid ""
+"Adding your own trustee requires a good bit more work to tally the election."
+"\\nYou will need to have trustees generate keypairs and safeguard their "
+"secret key.\\n\\nIf you are not sure what that means, we strongly recommend"
+"\\nclicking Cancel and letting Helios tally the election for you."
+msgstr ""
+"Adicionar seus próprios guardiões requer trabalho adicional na apuração ."
+"\\nÉ necessário que cada guardião gere um par de chaves e que guarde "
+"cuidadosamente sua chave secreta.\\n\\nSe você não compreende o significado "
+"ou a importância disto, recomenda-se fortemente\\ncancelar esta operação e "
+"deixar o servidor apurar a votação para você."
+
+#: helios/templates/list_trustees.html:26
+msgid "add a trustee"
+msgstr "adicionar guardião"
+
+#: helios/templates/list_trustees.html:30
+msgid "add Helios as a trustee"
+msgstr "adicionar o servidor como guardião"
+
+#: helios/templates/list_trustees.html:45
+msgid "Are you sure you want to remove Helios as a trustee?"
+msgstr "Deseja realmente excluir o servidor do papel de guardião?"
+
+#: helios/templates/list_trustees.html:50
+msgid "Are you sure you want to remove this Trustee?"
+msgstr "Deseja realmente excluir este guardião?"
+
+#: helios/templates/list_trustees.html:52
+msgid "Are you sure you want to send this trustee his/her admin URL?"
+msgstr "Deseja realmente enviar a este guardião suas credenciais?"
+
+#: helios/templates/list_trustees.html:53
+msgid "send login"
+msgstr "enviar credenciais"
+
+#: helios/templates/list_trustees.html:60
+#: helios/templates/trustee_decrypt_and_prove.html:139
+msgid "Public Key Fingerprint:"
+msgstr "Identificador da chave pública:"
+
+#: helios/templates/list_trustees.html:62
+msgid "No public key uploaded yet."
+msgstr "Nenhuma chave pública enviada até o momento."
+
+#: helios/templates/list_trustees.html:68
+msgid "tally recorded for this trustee."
+msgstr "apuração parcial registrada para este guardião."
+
+#: helios/templates/list_trustees.html:70
+msgid "waiting for this trustee's tally"
+msgstr "aguardando apuração parcial para este guardião"
+
+#: helios/templates/new_trustee.html:5
+msgid "New Trustee"
+msgstr "Novo Guardião"
+
+#: helios/templates/new_trustee.html:10
+msgid "Email"
+msgstr ""
+
+#: helios/templates/new_trustee.html:12
+msgid "Add Trustee"
+msgstr "Adicionar Guardião"
+
+#: helios/templates/release_result.html:4
+#: helios/templates/release_result.html:6
+msgid "Release Result"
+msgstr "Publicar Resultado"
+
+#: helios/templates/release_result.html:9
+msgid "You are about to release the result for this election."
+msgstr "Publicar o resultado desta votação."
+
+#: helios/templates/release_result.html:15
+msgid "Send email to voters reporting release of result."
+msgstr "Enviar email aos votantes informando a publicação dos resultados."
+
+#: helios/templates/release_result.html:17
+msgid "release result!"
+msgstr "publicar resultado"
+
+#: helios/templates/trustee_check_sk.html:39
+msgid "try again"
+msgstr "tentar novamente"
+
+#: helios/templates/trustee_check_sk.html:41
+msgid "your secret key matches!"
+msgstr "SUCESSO: chave secreta confere!"
+
+#: helios/templates/trustee_check_sk.html:43
+msgid "OH OH, your key is bad."
+msgstr "ERRO: chave secreta não confere!"
+
+#: helios/templates/trustee_check_sk.html:47
+#: helios/templates/trustee_home.html:5
+msgctxt "trustee name follows"
+msgid "Trustee"
+msgstr "Guardião"
+
+#: helios/templates/trustee_check_sk.html:47
+msgid "Check Secret Key"
+msgstr "Verificar Chave Secreta"
+
+#: helios/templates/trustee_check_sk.html:47
+msgid "back to trustee home"
+msgstr "voltar"
+
+#: helios/templates/trustee_check_sk.html:50
+#: helios/templates/trustee_home.html:10
+msgid "Your public key fingerprint is:"
+msgstr "Identificador da chave pública:"
+
+#: helios/templates/trustee_check_sk.html:54
+msgid "loading crypto functions..."
+msgstr "carregando funções criptográficas..."
+
+#: helios/templates/trustee_check_sk.html:59
+msgid "To verify that you have the right secret key, paste it here:"
+msgstr "Para verificar se sua chave secreta está correta, copie-a aqui:"
+
+#: helios/templates/trustee_check_sk.html:66
+msgid "check"
+msgstr "verificar"
+
+#: helios/templates/trustee_check_sk.html:71
+msgid "checking your secret key..."
+msgstr "verificando sua chave secreta..."
+
+#: helios/templates/trustee_decrypt_and_prove.html:135
+msgctxt "name follows"
+msgid "Trustee"
+msgstr "Guardião"
+
+#: helios/templates/trustee_decrypt_and_prove.html:135
+msgctxt "election name follows"
+msgid "Decrypt Result for"
+msgstr "Decifrar Resultados para"
+
+#: helios/templates/trustee_decrypt_and_prove.html:138
+msgid "Trustee:"
+msgstr "Guardião:"
+
+#: helios/templates/trustee_decrypt_and_prove.html:140
+msgid "Encrypted Tally Fingerprint:"
+msgstr "Identificador da votação:"
+
+#: helios/templates/trustee_decrypt_and_prove.html:144
+msgid "The encrypted tally for your election has been computed."
+msgstr "A apuração cifrada para esta votação está pronta."
+
+#: helios/templates/trustee_decrypt_and_prove.html:145
+msgid "Now it's time to compute and submit your partial decryption."
+msgstr "Agora é o momento de computar e enviar sua parcela de decifração."
+
+#: helios/templates/trustee_decrypt_and_prove.html:149
+msgid "This process is performed in two steps."
+msgstr "Este processo é realizado em dois passos."
+
+#: helios/templates/trustee_decrypt_and_prove.html:150
+msgid ""
+"<u>First</u>, your secret key is used to decrypt the tally <em>inside</em> "
+"your browser, without connecting to the network."
+msgstr ""
+"<u>Primeiro</u>, a sua chave secreta é usada para decifrar a totalização "
+"<em>dentro do ambiente do seu navegador</em>, isto é, sem qualquer conexão "
+"com a rede."
+
+#: helios/templates/trustee_decrypt_and_prove.html:151
+msgid ""
+"You can choose to take your browser \"offline\" for this step, if you'd like."
+msgstr ""
+"Você pode optar por se desconectar da rede para este passo, caso prefira."
+
+#: helios/templates/trustee_decrypt_and_prove.html:152
+msgid ""
+"<u>Second</u>, once your decryption factors have been computed, your browser "
+"will need to be \"online\" to submit them to the server."
+msgstr ""
+"<u>Segundo</u>, após computar seus fatores de decifração, você precisará se "
+"conectar à rede para poder enviá-los ao servidor."
+
+#: helios/templates/trustee_decrypt_and_prove.html:153
+msgid ""
+"If you'd like, you can compute your decryption factors, copy them to your "
+"clipboard, restart your browser, and"
+msgstr ""
+"Caso prefira também, você pode computar seus fatores de decifração, copiá-"
+"los, reiniciar seu navegador e"
+
+#: helios/templates/trustee_decrypt_and_prove.html:154
+msgid ""
+"skip to the second step so that your browser is never online when you enter "
+"your secret key."
+msgstr ""
+"seguir para o segundo passo para que seu navegador jamais fique conectado "
+"quando você informar sua chave secreta."
+
+#: helios/templates/trustee_decrypt_and_prove.html:160
+msgid "FIRST STEP: enter your secret key"
+msgstr "PRIMEIRO PASSO: informe a sua chave secreta abaixo"
+
+#: helios/templates/trustee_decrypt_and_prove.html:164
+msgctxt "button"
+msgid "Generate partial decryption"
+msgstr "Gerar decifração parcial"
+
+#: helios/templates/trustee_decrypt_and_prove.html:168
+msgid "skip to the second step"
+msgstr "avançar para o segundo passo"
+
+#: helios/templates/trustee_decrypt_and_prove.html:169
+msgid "(you need to have already computed your decryption factors.)"
+msgstr "(é necessário que você já tenha computado seus fatores de decifração)"
+
+#: helios/templates/trustee_decrypt_and_prove.html:174
+msgid "Generating partial decryption factors and proofs..."
+msgstr "Gerando fatores de decifração parcial e respectivas provas..."
+
+#: helios/templates/trustee_decrypt_and_prove.html:179
+msgid "Submitting and checking your decryption factors and proofs..."
+msgstr "Enviando e verificando seus fatores de decifração e provas..."
+
+#: helios/templates/trustee_decrypt_and_prove.html:184
+msgid "SECOND STEP: upload your partial decryption"
+msgstr "SEGUNDO PASSO: envie sua decifração parcial ao servidor"
+
+#: helios/templates/trustee_decrypt_and_prove.html:186
+msgid "Your partial decryption factors and proofs have been generated below."
+msgstr "Seus fatores de decifração parcial e provas foram gerados abaixo."
+
+#: helios/templates/trustee_decrypt_and_prove.html:187
+msgid "Your secret key has been cleared from memory."
+msgstr "Sua chave secreta foi removida da memória."
+
+#: helios/templates/trustee_decrypt_and_prove.html:188
+msgid "When you're ready, you can submit this result to the server."
+msgstr "Quando quiser, você pode enviar seus resultados para o servidor."
+
+#: helios/templates/trustee_decrypt_and_prove.html:190
+msgid "Your partial decryption:"
+msgstr "Sua decifração parcial:"
+
+#: helios/templates/trustee_decrypt_and_prove.html:193
+msgid "Upload decryption factors to server"
+msgstr "Enviar fatores de decifração para o servidor"
+
+#: helios/templates/trustee_decrypt_and_prove.html:195
+msgid "reset and restart decryption process"
+msgstr "reiniciar processo de decifração"
+
+#: helios/templates/trustee_decrypt_and_prove.html:199
+msgid "Done!"
+msgstr "Concluído!"
+
+#: helios/templates/trustee_decrypt_and_prove.html:199
+msgid "Back to election"
+msgstr "voltar"
+
+#: helios/templates/trustee_decrypt_and_prove.html:203
+msgid "Error: your secret key was likely incorrect."
+msgstr "Erro: chave secreta incorreta."
+
+#: helios/templates/trustee_home.html:5
+msgctxt "trustee name before"
+msgid "Home"
+msgstr " "
+
+#: helios/templates/trustee_home.html:9
+msgid "You have successfully uploaded your public key."
+msgstr "Chave pública enviada com sucesso."
+
+#: helios/templates/trustee_home.html:11
+msgctxt "action follows"
+msgid "You can"
+msgstr "Você pode"
+
+#: helios/templates/trustee_home.html:11
+msgctxt "action link"
+msgid "verify that you have the right secret key"
+msgstr "verificar se sua chave está correta"
+
+#: helios/templates/trustee_home.html:13
+msgctxt "action"
+msgid "setup your key"
+msgstr "preparar chave"
+
+#: helios/templates/trustee_home.html:20
+msgid "You have successfully uploaded your decryption."
+msgstr "Parcela de decifração enviada com sucesso."
+
+#: helios/templates/trustee_home.html:22
+msgid "The encrypted tally for this election is ready."
+msgstr "A apuração cifrada para esta votação está pronta."
+
+#: helios/templates/trustee_home.html:23
+msgctxt "action"
+msgid "decrypt with your key"
+msgstr "decifrar com sua chave"
+
+#: helios/templates/trustee_home.html:26
+msgid ""
+"Once the tally is computed, come back here to provide your secret key for "
+"decryption purposes."
+msgstr ""
+"Quando a apuração estiver computada, retorne aqui para prover sua chave "
+"secreta para decifração."
+
+#: helios/templates/trustee_home.html:27
+msgid ""
+"You should keep the email with your trustee homepage link, which contains "
+"the credentials needed to get back here."
+msgstr ""
+"Assegure-se de guardar o email que lhe foi enviado com o link para sua "
+"homepage de guardião: este email contém as credenciais necessárias para que "
+"você possa retornar aqui."
+
+#: helios/templates/voters_email.html:4
+msgctxt "name follows"
+msgid "Contact Voters for"
+msgstr "Contactar votantes de"
+
+#: helios/templates/voters_email.html:13
+msgid "Contact Voters"
+msgstr "Contactar Votantes"
+
+#: helios/templates/voters_email.html:17
+msgctxt "name follows"
+msgid "You are sending this email to a specific user:"
+msgstr "Este email será enviado para um usuário específico:"
+
+#: helios/templates/voters_email.html:22
+msgctxt "email"
+msgid "Templates"
+msgstr "Modelos"
+
+#: helios/templates/voters_email.html:34
+msgctxt "email"
+msgid "Subject:"
+msgstr "Assunto:"
+
+#: helios/templates/voters_email.html:40
+msgid ""
+"You may tweak the subject and add a custom message using the form below."
+msgstr ""
+"Você também pode modificar o assunto do email e adicionar uma mensagem "
+"customizada usando os campos abaixo."
+
+#: helios/templates/voters_email.html:51
+msgid "Send"
+msgstr "Enviar"
+
+#: helios/templates/voters_list.html:4
+msgctxt "election name follows"
+msgid "Voters &amp; Ballot Tracking Center for"
+msgstr "Votantes e cédulas recebidas de "
+
+#: helios/templates/voters_list.html:6
+msgid "Voters and Ballot Tracking Center"
+msgstr "Gestão de votantes e cédulas"
+
+#: helios/templates/voters_list.html:9
+msgid "Who can vote?"
+msgstr "Quem pode votar?"
+
+#: helios/templates/voters_list.html:13
+msgid "Only the voters listed here"
+msgstr "Apenas votantes listados aqui"
+
+#: helios/templates/voters_list.html:19
+msgid ""
+"Your election is marked private, which means you cannot open registration up "
+"more widely"
+msgstr ""
+"Votação está marcada como fechada; o registro de votantes está limitado."
+
+#: helios/templates/voters_list.html:21
+msgid "You can change this setting:"
+msgstr "A seguinte configuração pode ser alterada:"
+
+#: helios/templates/voters_list.html:24
+msgid "anyone can vote"
+msgstr "qualquer um pode votar"
+
+#: helios/templates/voters_list.html:25
+msgid "only voters listed explicitly below can vote"
+msgstr "apenas os votantes listados abaixo podem votar"
+
+#: helios/templates/voters_list.html:27
+msgctxt "categories follow"
+msgid "only voters who are members of"
+msgstr "apenas votantes que sejam membros das seguintes categorias:"
+
+#: helios/templates/voters_list.html:35
+msgid "update"
+msgstr "Atualizar"
+
+#: helios/templates/voters_list.html:41
+msgid "email voters"
+msgstr "enviar emails aos votantes"
+
+#: helios/templates/voters_list.html:47
+msgctxt "voter name follows"
+msgid "searching for"
+msgstr "buscando votante"
+
+#: helios/templates/voters_list.html:47
+msgid "clear search"
+msgstr "limpar busca"
+
+#: helios/templates/voters_list.html:49
+msgid "search"
+msgstr "buscar"
+
+#: helios/templates/voters_list.html:58
+msgid "bulk upload voters"
+msgstr "carregar arquivo de votantes"
+
+#: helios/templates/voters_list.html:62
+msgid "Prior Bulk Uploads:"
+msgstr "Arquivos previamente carregados:"
+
+#: helios/templates/voters_list.html:71
+msgctxt "date"
+msgid "at"
+msgstr "em"
+
+#: helios/templates/voters_list.html:73
+msgid "done processing:"
+msgstr "processamento terminado:"
+
+#: helios/templates/voters_list.html:73
+msgctxt "number before"
+msgid "voters loaded"
+msgstr "votantes carregados"
+
+#: helios/templates/voters_list.html:77
+msgid "currently processing"
+msgstr "em processamento"
+
+#: helios/templates/voters_list.html:79
+msgid "not yet processed"
+msgstr "ainda não processado"
+
+#: helios/templates/voters_list.html:95
+msgid "cast vote"
+msgstr "votar"
+
+#: helios/templates/voters_list.html:97
+msgid "no votes yet"
+msgstr "nenhuma cédula ainda"
+
+#: helios/templates/voters_list.html:103
+msgid "previous"
+msgstr "anterior"
+
+#: helios/templates/voters_list.html:107
+msgid "Voters"
+msgstr "Votantes"
+
+#: helios/templates/voters_list.html:107
+msgctxt "total number follows"
+msgid "(of"
+msgstr "(de um total de"
+
+#: helios/templates/voters_list.html:110
+msgctxt "feminine"
+msgid "next"
+msgstr "próxima"
+
+#: helios/templates/voters_list.html:117
+msgid "Actions"
+msgstr "Ações"
+
+#: helios/templates/voters_list.html:118
+msgctxt "username"
+msgid "Login"
+msgstr "Login"
+
+#: helios/templates/voters_list.html:119
+msgid "Email Address"
+msgstr "Email"
+
+#: helios/templates/voters_list.html:125
+msgid "Alias"
+msgstr "Pseudônimo"
+
+#: helios/templates/voters_list.html:127
+msgid "Smart Ballot Tracker"
+msgstr "Localizador"
+
+#: helios/templates/voters_list.html:135
+msgid "email"
+msgstr ""
+
+#: helios/templates/voters_list.html:137
+msgctxt "voter name follows"
+msgid "are you sure you want to remove"
+msgstr "Deseja realmente excluir"
+
+#: helios/templates/voters_list.html:155
+msgid "no voters"
+msgstr "nenhum votante registrado"
+
+#: helios/templates/voters_upload.html:5
+msgid "Bulk Upload Voters"
+msgstr "Carregar Votantes"
+
+#: helios/templates/voters_upload.html:9
+msgid ""
+"If you would like to specify your list of voters by name and email address,"
+msgstr "Caso deseje especificar uma lista de votantes por nome e email,"
+
+#: helios/templates/voters_upload.html:10
+msgid "you can bulk upload a list of such voters here."
+msgstr "você pode carregar um arquivo com essas informações aqui."
+
+#: helios/templates/voters_upload.html:12
+msgid "Please prepare a text file of comma-separated values with the fields:"
+msgstr ""
+"Basta preparar um arquivo de texto com valores separador por vírgulas (CSV) "
+"contendo os seguintes campos:"
+
+#: helios/templates/voters_upload.html:15
+msgid "&lt;unique_id&gt;,&lt;email&gt,&lt;full name&gt;"
+msgstr "&lt;login&gt;,&lt;email&gt,&lt;nome completo&gt;"
+
+#: helios/templates/voters_upload.html:19
+msgid "For example:"
+msgstr "Por exemplo:"
+
+#: helios/templates/voters_upload.html:22
+msgctxt "example"
+msgid "benadida,ben@adida.net,Ben Adida"
+msgstr "login1,email1@dom.com,Nome do Votante_1"
+
+#: helios/templates/voters_upload.html:23
+msgctxt "example"
+msgid "bobsmith,bob@acme.org,Bob Smith"
+msgstr "login2,email2@dom.com,Nome do Votante_2"
+
+#: helios/templates/voters_upload.html:28
+msgid ""
+"The easiest way to prepare such a file is to use a spreadsheet program and "
+"to export as &quot;CSV&quot;."
+msgstr ""
+"Para preparar um arquivo nesse formato você pode preparar uma planilha e "
+"escolher a opção de exportar como &quot;CSV&quot;."
+
+#: helios/templates/voters_upload.html:36
+msgctxt "label bulk upload"
+msgid "go"
+msgstr "Carregar arquivo CSV"
+
+#: helios/templates/voters_upload.html:43
+msgctxt "voter upload"
+msgid "OK, done uploading."
+msgstr "Arquivo carregado com sucesso."
+
+#: helios/templates/voters_upload.html:44
+msgctxt "action follows"
+msgid "You can now"
+msgstr "Você pode agora"
+
+#: helios/templates/voters_upload.html:44
+msgid "view the list of voters"
+msgstr "visualizar a lista de votantes"
+
+#: helios/templates/voters_upload_confirm.html:5
+msgid "Bulk Upload Voters &mdash; Confirm"
+msgstr "Carregar Votantes &mdash; Confirmação"
+
+#: helios/templates/voters_upload_confirm.html:8
+msgid ""
+"You have uploaded a file of voters. The first few rows of this file are:"
+msgstr "Arquivo de votantes carregado com sucesso. As primeiras linhas são:"
+
+#: helios/templates/voters_upload_confirm.html:12
+msgctxt "voter listing"
+msgid "Voter Login"
+msgstr "Login"
+
+#: helios/templates/voters_upload_confirm.html:12
+msgctxt "voter listing"
+msgid "Email Address"
+msgstr "Email"
+
+#: helios/templates/voters_upload_confirm.html:12
+msgctxt "voter listing"
+msgid "Name"
+msgstr "Nome"
+
+#: helios/templates/voters_upload_confirm.html:27
+msgid "never mind, upload a different file"
+msgstr ""
+
+#: helios/templates/voters_upload_confirm.html:33
+msgid "Does this look right to you?"
+msgstr "Confirmar?"
+
+#: helios/templates/voters_upload_confirm.html:35
+msgid "Yes, let's go"
+msgstr "Sim"
+
+#: helios/templates/voters_upload_confirm.html:38
+msgid "no, let me upload a different file"
+msgstr "Cancelar"
+
+#: helios/views.py:94
+msgid "no admin users!"
+msgstr "não há usuários administradores"
+
+#: helios/views.py:97
+msgid "no users!"
+msgstr "não há usuários"
+
+#: helios/views.py:154
+msgid ""
+"\n"
+"  A key generator with the current params, like the trustee home but without "
+"a specific election.\n"
+"  "
+msgstr ""
+"\n"
+"  Um gerador de chaves com os parâmetros atuais mas sem especificação de uma "
+"votação.\n"
+"  "
+
+#: helios/views.py:164
+msgid "only an administrator has elections to administer"
+msgstr "apenas administradores possuem votações para administrar"
+
+#: helios/views.py:182
+msgid "only an administrator can create an election"
+msgstr "apenas administradores podem criar votações"
+
+#: helios/views.py:214 helios/views.py:247
+#, python-format
+msgid "An election with short name %s already exists"
+msgstr "Já existe uma eleição com o nome %s"
+
+#: helios/views.py:216
+msgid "No special characters allowed in the short name."
+msgstr "Caracteres especiais não são permitidos no nome da URL."
+
+#: helios/views.py:332
+#, python-format
+msgid "Vote in %s"
+msgstr "Participe da eleição %s"
+
+#: helios/views.py:334
+#, python-format
+msgid "Register to vote in %s"
+msgstr "Registre-se para votar na eleição %s"
+
+#: helios/views.py:338
+#, python-format
+msgid "Results are in for %s"
+msgstr "Resultados já estão disponíveis para eleição %s"
+
+#: helios/views.py:407
+msgid ""
+"\n"
+"  Make Helios a trustee of the election\n"
+"  "
+msgstr ""
+"\n"
+"  Tornar o Helios/INSCRYPT um guardião dessa votação\n"
+"  "
+
+#: helios/views.py:438
+#, python-format
+msgid ""
+"\n"
+"\n"
+"You are a trustee for %s.\n"
+"\n"
+"Your trustee dashboard is at\n"
+"\n"
+"  %s\n"
+"  \n"
+"--\n"
+"Helios  \n"
+msgstr ""
+"\n"
+"\n"
+"Você é um guardião da votação %s.\n"
+"\n"
+"Sua dashboard está acessível em\n"
+"\n"
+"  %s\n"
+"  \n"
+"--\n"
+"Helios/INSCRYPT  \n"
+
+#: helios/views.py:450
+#, python-format
+msgid "your trustee homepage for %s"
+msgstr "Sua Homepage de Guardião para %s"
+
+#: helios/views.py:1134 helios/views.py:1341
+msgid "result"
+msgstr "pt-BR/result"
+
+#: helios/views.py:1318
+msgid "no voter file specified, try again"
+msgstr "Selecione um arquivo de votantes"
+
+#: helios/views.py:1338 helios/views.py:1344
+msgid "vote"
+msgstr "pt-BR/vote"
+
+#: helios/views.py:1338
+msgid "Time to Vote"
+msgstr "Votar"
+
+#: helios/views.py:1339
+msgid "simple"
+msgstr "pt-BR/simple"
+
+#: helios/views.py:1339
+msgid "Simple"
+msgstr "Simples"
+
+#: helios/views.py:1340
+msgid "info"
+msgstr "pt-BR/info"
+
+#: helios/views.py:1340
+msgid "Additional Info"
+msgstr "Informações Adicionais"
+
+#: helios/views.py:1341
+msgid "Election Result"
+msgstr "Resultados da Votação"
+
+#: helios_auth/auth_systems/password.py:30
+msgid "Username"
+msgstr "Login"
+
+#: helios_auth/auth_systems/password.py:31
+msgid "Password"
+msgstr "Senha"
+
+#: helios_auth/auth_systems/password.py:65
+msgid "Bad Username or Password"
+msgstr "Usuário ou senha inválido"
+
+#: helios_auth/templates/password/login.html:24
+msgid "Login"
+msgstr "Autenticar"
+
+#: server_ui/templates/base.html:51
+msgid "About Helios"
+msgstr "Sobre o Helios/INSCRYPT"
+
+#: server_ui/templates/base.html:70
+msgid "Help!"
+msgstr "Ajuda"
+
+#: server_ui/templates/base.html:92
+msgid "logged in as"
+msgstr "Autenticado como"
+
+#: server_ui/templates/base.html:93
+msgid "logout"
+msgstr "desconectar"
+
+#: server_ui/templates/base.html:99
+msgid "not logged in."
+msgstr "não autenticado."
+
+#: server_ui/templates/base.html:101 settings.py:204
+msgid "Helios Voting"
+msgstr "Principal"
+
+#: server_ui/templates/index.html:32
+msgid "create an election"
+msgstr "criar uma votação"
+
+#: server_ui/templates/index.html:50
+msgid "no featured elections at the moment"
+msgstr "Não há votações em destaque no momento."
+
+#: server_ui/templates/index.html:62
+msgid "create election"
+msgstr "Criar Votação"
+
+#: server_ui/templates/index.html:63
+msgid "Administration"
+msgstr "Eleições Administradas"
+
+#: server_ui/templates/index.html:71
+msgctxt "feminine"
+msgid "none yet"
+msgstr "nenhuma"
+
+#: server_ui/templates/index.html:74
+msgctxt "feminine"
+msgid "see all"
+msgstr "ver todas"
+
+#: server_ui/templates/index.html:79
+msgid "Recent Votes"
+msgstr "Votos Recentes"
+
+#: server_ui/templates/index.html:87
+msgctxt "masculine"
+msgid "none yet"
+msgstr "nenhum"
+
+#: server_ui/templates/index.html:89
+msgctxt "masculine"
+msgid "see all"
+msgstr "ver todos"
+
+#: server_ui/templates/index.html:93
+msgid "Log In to Start Voting"
+msgstr "Autentique-se para votar"
+
+#: server_ui/templates/index.html:97
+msgid ""
+"Select an election to start voting. You will be asked for your voting "
+"credentials after you complete a ballot."
+msgstr ""
+"Escolha uma votação para iniciar o processo de votação. Você será solicitado "
+"a informar suas credenciais de votante depois de completar sua cédula."
+
+#: settings.py:212
+msgid "This is the default message"
+msgstr "Bem-vindo ao Helios/INSCRYPT!"
+
+#~| msgid "Trustee"
+#~ msgctxt "prompt"
+#~ msgid "Trustee"
+#~ msgstr "Guardião"
+
+#, fuzzy
+#~| msgid "Public Key Fingerprint:"
+#~ msgctxt "prompt"
+#~ msgid "Public Key Fingerprint"
+#~ msgstr "Identificador da chave pública:"
+
+#~ msgid "%(election_name)s - vote cast"
+#~ msgstr "%(election_name)s - Voto Enviado"
+
+#~ msgid "email/cast_vote_subject.txt"
+#~ msgstr "email/pt-BR/cast_vote_subject.txt"
+
+#~ msgid "email/cast_vote_body.txt"
+#~ msgstr "email/pt-BR/cast_vote_body.txt"
+
+#~ msgid " - vote cast"
+#~ msgstr " - voto enviado"
+
+#~ msgid "email/"
+#~ msgstr "email/pt-BR/"
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/server_ui/glue.py helios-server-translation/server_ui/glue.py
--- helios-server-original/server_ui/glue.py	2019-10-28 03:15:16.935087172 -0300
+++ helios-server-translation/server_ui/glue.py	2019-10-29 03:15:08.031263282 -0300
@@ -13,15 +13,15 @@
 def vote_cast_send_message(user, voter, election, cast_vote, **kwargs):
   ## FIXME: this doesn't work for voters that are not also users
   # prepare the message
-  subject_template = 'email/cast_vote_subject.txt'
-  body_template = 'email/cast_vote_body.txt'
+  subject_template = "email/pt-BR/cast_vote_subject.txt"
+  body_template = "email/pt-BR/cast_vote_body.txt"
   
   extra_vars = {
     'election' : election,
     'voter': voter,
     'cast_vote': cast_vote,
     'cast_vote_url': helios.views.get_castvote_url(cast_vote),
-    'custom_subject' : "%s - vote cast" % election.name
+    'custom_subject' : "%s - Voto Enviado" % election.name
   }
   subject = render_template_raw(None, subject_template, extra_vars)
   body = render_template_raw(None, body_template, extra_vars)
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/server_ui/templates/base.html helios-server-translation/server_ui/templates/base.html
--- helios-server-original/server_ui/templates/base.html	2019-10-28 03:15:16.937087172 -0300
+++ helios-server-translation/server_ui/templates/base.html	2019-10-29 03:15:08.032263282 -0300
@@ -1,3 +1,5 @@
+{% load i18n %}
+
 <!DOCTYPE html>
 <!--[if IE 8]> 				 <html class="no-js lt-ie9" lang="en" > <![endif]-->
 <!--[if gt IE 8]><!--> <html class="no-js" lang="en" > <!--<![endif]-->
@@ -46,7 +48,7 @@
 	<li class="divider"></li>
 	{% endif %}
 	{% if not settings.MASTER_HELIOS %}
-	<li><a href="http://heliosvoting.org">About Helios</a></li>
+	<li><a href="http://heliosvoting.org">{% trans "About Helios" %}</a></li>
 	{% endif %}
       </ul>
       
@@ -65,7 +67,7 @@
         <li><a href="/privacy">Privacy</a></li>
 	{% endif %}
         <li class="divider"></li>
-        <li><a href="mailto:{{settings.HELP_EMAIL_ADDRESS}}">Help!</a></li>
+        <li><a href="mailto:{{settings.HELP_EMAIL_ADDRESS}}">{% trans "Help!" %}</a></li>
       </ul>
     </section>
   </nav>
@@ -87,16 +89,16 @@
       {% endif %}
     </span>-->
     {% if user %}
-    logged in as <b>{{user.display_html_small|safe}}</b>&nbsp;&nbsp;
-    <a class="tiny button" href="{% url "helios_auth.views.logout" %}?return_url={{CURRENT_URL}}">logout</a><br />
+    {% trans "logged in as" %} <b>{{user.display_html_small|safe}}</b>&nbsp;&nbsp;
+    <a class="tiny button" href="{% url "helios_auth.views.logout" %}?return_url={{CURRENT_URL}}">{% trans "logout" %}</a><br />
     {% else %}
     {% if voter %}
     You are signed in as voter <u>{% if voter.alias %}{{voter.alias}}{% else %}{{voter.name}}{% endif %}</u> in election <u>{{voter.election.name}}</u>. [<a href="{{settings.SECURE_URL_HOST}}{% url "helios_auth.views.logout" %}?return_url={{CURRENT_URL}}">sign out</a>]
     {% else %}
 {% if settings.SHOW_LOGIN_OPTIONS %}
-    not logged in. <a class="tiny button" href="{{settings.SECURE_URL_HOST}}{% url "helios_auth.views.index" %}?return_url={{CURRENT_URL}}">log in</a>
+    {% trans "not logged in." %} <a class="tiny button" href="{{settings.SECURE_URL_HOST}}{% url "helios_auth.views.index" %}?return_url={{CURRENT_URL}}">{% trans "log in" %}</a>
 {% else %}
-powered by <a href="http://heliosvoting.org">Helios Voting</a>.
+powered by <a href="http://heliosvoting.org">{% trans "Helios Voting" %}</a>.
 {% endif %}
     {% endif %}
     {% endif %}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/server_ui/templates/email/pt-BR/cast_vote_body.txt helios-server-translation/server_ui/templates/email/pt-BR/cast_vote_body.txt
--- helios-server-original/server_ui/templates/email/pt-BR/cast_vote_body.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/server_ui/templates/email/pt-BR/cast_vote_body.txt	2019-10-29 03:15:08.032263282 -0300
@@ -0,0 +1,13 @@
+Prezado(a) {{voter.name}},
+
+Seu voto na votação {{election.name}} foi enviado com sucesso.
+
+Sua cédula se encontra registrada em: {{cast_vote_url}}
+
+{% if election.use_voter_aliases %}
+Esta votação faz uso de pseudônimos para proteger sua privacidade.
+O seu pseudônimo é: {{voter.alias}}
+{% endif %}
+
+--
+Helios/INSCRYPT
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/server_ui/templates/email/pt-BR/cast_vote_subject.txt helios-server-translation/server_ui/templates/email/pt-BR/cast_vote_subject.txt
--- helios-server-original/server_ui/templates/email/pt-BR/cast_vote_subject.txt	1969-12-31 21:00:00.000000000 -0300
+++ helios-server-translation/server_ui/templates/email/pt-BR/cast_vote_subject.txt	2019-10-29 03:15:08.032263282 -0300
@@ -0,0 +1 @@
+{{custom_subject|safe}}
diff -ruN '--exclude=.git' '--exclude=.gitignore' '--exclude=.travis.yml' '--exclude=*.pyc' '--exclude=reset.sh' '--exclude=settings.py' '--exclude=venv' '--exclude=translation.patch' helios-server-original/server_ui/templates/index.html helios-server-translation/server_ui/templates/index.html
--- helios-server-original/server_ui/templates/index.html	2019-10-28 03:15:16.937087172 -0300
+++ helios-server-translation/server_ui/templates/index.html	2019-10-29 03:15:08.032263282 -0300
@@ -1,4 +1,5 @@
 {% extends 'server_ui/templates/base.html' %}
+{% load i18n %}
 {% block title %}{{ settings.SITE_TITLE }}{% endblock %}
 
 {% block header %}
@@ -28,7 +29,7 @@
 </p>
 
 {% if create_p %}
-<a class="button" href="{% url "helios.views.election_new" %}">create an election</a>
+<a class="button" href="{% url "helios.views.election_new" %}">{% trans "create an election" %}</a>
 {% endif %}
 
   {% else %}
@@ -46,7 +47,7 @@
     {% endfor %}
   </p>
   {% else %}
-  <h4>no featured elections at the moment</h4>
+  <h4>{% trans "no featured elections at the moment" %}</h4>
   {% endif %}
 {% endif %}
   
@@ -58,8 +59,8 @@
 {% if user %}
 <!--<div class="row right">{{user.display_html_big|safe}}</div>-->
 {% if create_p %}
-<a class="small button" href="{% url "helios.views.election_new" %}">create election</a>
-<h5 class="subheader">Administration</h5>
+<a class="small button" href="{% url "helios.views.election_new" %}">{% trans "create election" %}</a>
+<h5 class="subheader">{% trans "Administration" %}</h5>
 {% if elections_administered %}
 <ul>
 {% for election in elections_administered %}
@@ -67,15 +68,15 @@
 {% endfor %}
 </ul>
 {% else %}
-<em>none yet</em>
+<em>{% trans "none yet" context "feminine" %}</em>
 {% endif %}
 <div class="row right">
-<a class="tiny button" href="{% url "helios.views.elections_administered" %}">see all</a>
+<a class="tiny button" href="{% url "helios.views.elections_administered" %}">{% trans "see all" context "feminine" %}</a>
 </div>
 <div class="row"></div>
 {% endif %}
 
-<h5 class="subheader">Recent Votes</h5>
+<h5 class="subheader">{% trans "Recent Votes" %}</h5>
 {% if elections_voted %}
 <ul>
 {% for election in elections_voted %}
@@ -83,17 +84,17 @@
 {% endfor %}
 </ul>
 {% else %}
-<em>none yet</em>
+<em>{% trans "none yet" context "masculine" %}</em>
 {% endif %}
-<div class="row right"><a class="tiny button" href="{% url "helios.views.elections_voted" %}">see all</a></div>
+<div class="row right"><a class="tiny button" href="{% url "helios.views.elections_voted" %}">{% trans "see all" context "masculine" %}</a></div>
 <div class="row"></div>
 {% else %}
 {% if settings.SHOW_LOGIN_OPTIONS %}
-<h5>Log In to Start Voting</h5>
+<h5>{% trans "Log In to Start Voting" %}</h5>
 {{login_box|safe}}
 {% else %}
 <p>
-Select an election to start voting. You will be asked for your voting credentials after you complete a ballot.
+{% trans "Select an election to start voting. You will be asked for your voting credentials after you complete a ballot." %}
 </p>
 {% endif %}
 {% endif %}
